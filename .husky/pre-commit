#!/bin/sh

# TypeScript 타입 체크 (실패 시 커밋 중단)
pnpm typecheck || exit 1

# ESLint 검사 (경고는 허용, 오류만 커밋 중단)
# 각 워크스페이스의 lint 스크립트에 --max-warnings=-1 옵션이 포함되어 있어
# 경고만 있으면 통과하고 오류만 있으면 실패합니다.
pnpm lint || exit 1
