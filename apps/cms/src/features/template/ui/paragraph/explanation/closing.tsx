/**
 * 마무리글형 — 카드 본문 슬롯(`paragraph-card__slot`)에는 JSX를 넘기지 않습니다.
 * (`render-form-paragraph-body.tsx`의 `case 'closing'` → `null`)
 *
 * 마무리 문구(`body`): 좌측 `ParagraphCard` 타이틀 줄(`ParagraphInput` title)과 동일 UX로 편집.
 * 우측 패널: `설명글_마무리글형` 섹션 제목 + 유형 셀렉트(비활성)만.
 */

export {}
