/**
 * Generated by orval v8.17.0 🍺
 * Do not edit manually.
 * JA Korea CMS Backend API — Forms-Surveys subset
 * Filtered for CMS form template / survey Orval codegen.
 * OpenAPI spec version: v9
 */
import type {
  ApiResponseBulkDownloadJobResponse,
  FormAutoFillKeyResponse,
  FormResponseCreateRequest,
  FormResponseDocumentSnapshotResponse,
  FormResponseProjectionResponse,
  FormResponseResponse,
  FormSubmissionBulkDownloadRequest,
  FormSubmissionFileDownloadResponse,
  FormTemplateCreateOptionResponse,
  FormTemplateCreateRequest,
  FormTemplateLatestPayloadResponse,
  FormTemplateResponse,
  FormTemplateUpdateRequest,
  FormTemplateVersionCopyRequest,
  FormTemplateVersionCreateRequest,
  FormTemplateVersionPublishRequest,
  FormTemplateVersionResponse,
  FormTemplateVersionSummaryResponse,
  FormTemplateVersionUpdateRequest,
  FormVersionAdminResponse,
  ListBusinessProjectionsParams,
  ListResponsesParams,
  ListTemplatesParams,
  PageResponseFormResponseResponse,
  PageResponseFormTemplateListItemResponse,
  ProgramFormBindingRequest,
  ProgramFormBindingResponse,
  Render1200
} from './schemas';

import { customInstance } from '../../orval-mutator';


type SecondParameter<T extends (...args: never) => unknown> = Parameters<T>[1];


  export const getJAKoreaCMSBackendAPIFormsSurveysSubset = () => {
/**
 * ### 이 API가 하는 일
 * - 관리자 조회
 * - API 분류: 피그마/프론트 화면에서 사용하는 화면 API
 * - 사용하는 화면: 폼/템플릿/설문 (`SCR_FORM`)
 * - 프론트 담당 영역: forms-surveys (`forms-surveys`)
 * - 호출 방식: `GET /api/admin/form-template-versions/{versionId}`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 필터/페이지네이션/선택 행에서 요청값 전달
 * - 응답 사용 위치: 조회 캐시 및 화면 목록·상세 상태 갱신
 * - 프론트 조회 키: `get_admin_form-template-versions_versionId`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 라우트 준비 완료
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 기본 스모크 검증 대상
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 forms-surveys API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 조회 API는 응답 원본 status/code 값을 화면 배지 라벨과 분리해서 보관합니다. 라벨은 프론트 표시용, 원본 값은 후속 API 호출 조건으로 사용합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary 관리자 조회
 */
const getVersion = (
    versionId: number,
 options?: SecondParameter<typeof customInstance<FormTemplateVersionResponse>>,) => {
      return customInstance<FormTemplateVersionResponse>(
      {url: `/api/admin/form-template-versions/${versionId}`, method: 'GET'
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - PUT /api/admin/form-template-versions/{versionId}
 * - API 분류: 피그마/프론트 화면에서 사용하는 화면 API
 * - 사용하는 화면: 폼/템플릿/설문 (`SCR_FORM`)
 * - 프론트 담당 영역: forms-surveys (`forms-surveys`)
 * - 호출 방식: `PUT /api/admin/form-template-versions/{versionId}`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 폼 상태 or 선택 행 action payload
 * - 응답 사용 위치: 변경 결과 then 관련 조회 키 갱신
 * - 프론트 조회 키: `put_admin_form-template-versions_versionId`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 라우트 준비 완료
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 기본 스모크 검증 대상
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 forms-surveys API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary PUT /api/admin/form-template-versions/{versionId}
 */
const updateVersion = (
    versionId: number,
    formTemplateVersionUpdateRequest: FormTemplateVersionUpdateRequest,
 options?: SecondParameter<typeof customInstance<void>>,) => {
      return customInstance<void>(
      {url: `/api/admin/form-template-versions/${versionId}`, method: 'PUT',
      headers: {'Content-Type': 'application/json', },
      data: formTemplateVersionUpdateRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 프로그램 조회
 * - API 분류: 피그마/프론트 화면에서 사용하는 화면 API
 * - 사용하는 화면: 폼/템플릿/설문 (`SCR_FORM`)
 * - 프론트 담당 영역: forms-surveys (`forms-surveys`)
 * - 호출 방식: `GET /api/admin/programs/{programId}/form-bindings`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 프로그램 상세 선택값, 템플릿 유형, 일정형/커리큘럼형 대상 구간, 사용자 참여 context
 * - 응답 사용 위치: 프로그램 상세 설문/만족도 바인딩 목록 조회 결과로 화면 목록/모달/응답 상태를 갱신
 * - 프론트 조회 키: `get_programs_programId_form_bindings`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 라우트 준비 완료
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: STAGE178_SURVEY_SCREEN_MAPPING_SMOKE
 * - 목데이터 대체: 프로그램 상세 신규 설문조사 등록 모달 및 사용자 설문 참여 화면을 API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 조회 API는 응답 원본 status/code 값을 화면 배지 라벨과 분리해서 보관합니다. 라벨은 프론트 표시용, 원본 값은 후속 API 호출 조건으로 사용합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary 프로그램 조회
 */
const formBindings = (
    programId: string,
 options?: SecondParameter<typeof customInstance<ProgramFormBindingResponse[]>>,) => {
      return customInstance<ProgramFormBindingResponse[]>(
      {url: `/api/admin/programs/${programId}/form-bindings`, method: 'GET'
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - POST /api/admin/programs/{programId}/form-bindings
 * - API 분류: 피그마/프론트 화면에서 사용하는 화면 API
 * - 사용하는 화면: 폼/템플릿/설문 (`SCR_FORM`)
 * - 프론트 담당 영역: forms-surveys (`forms-surveys`)
 * - 호출 방식: `POST /api/admin/programs/{programId}/form-bindings`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 프로그램 상세 선택값, 템플릿 유형, 일정형/커리큘럼형 대상 구간, 사용자 참여 context
 * - 응답 사용 위치: 프로그램 상세 설문/만족도 바인딩 등록 결과로 화면 목록/모달/응답 상태를 갱신
 * - 프론트 조회 키: `post_programs_programId_form_bindings`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 라우트 준비 완료
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: STAGE178_SURVEY_SCREEN_MAPPING_SMOKE
 * - 목데이터 대체: 프로그램 상세 신규 설문조사 등록 모달 및 사용자 설문 참여 화면을 API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary POST /api/admin/programs/{programId}/form-bindings
 */
const createFormBinding = (
    programId: string,
    programFormBindingRequest: ProgramFormBindingRequest,
 options?: SecondParameter<typeof customInstance<ProgramFormBindingResponse>>,) => {
      return customInstance<ProgramFormBindingResponse>(
      {url: `/api/admin/programs/${programId}/form-bindings`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: programFormBindingRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 관리자 조회
 * - API 분류: 피그마/프론트 화면에서 사용하는 화면 API
 * - 사용하는 화면: 폼/템플릿/설문 (`SCR_FORM`)
 * - 프론트 담당 영역: forms-surveys (`forms-surveys`)
 * - 호출 방식: `GET /api/admin/form-templates`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 필터/페이지네이션/선택 행에서 요청값 전달
 * - 응답 사용 위치: 조회 캐시 및 화면 목록·상세 상태 갱신
 * - 프론트 조회 키: `get_admin_form-templates`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 라우트 준비 완료
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 기본 스모크 검증 대상
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 forms-surveys API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 조회 API는 응답 원본 status/code 값을 화면 배지 라벨과 분리해서 보관합니다. 라벨은 프론트 표시용, 원본 값은 후속 API 호출 조건으로 사용합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary 관리자 조회
 */
const listTemplates = (
    params?: ListTemplatesParams,
 options?: SecondParameter<typeof customInstance<PageResponseFormTemplateListItemResponse>>,) => {
      return customInstance<PageResponseFormTemplateListItemResponse>(
      {url: `/api/admin/form-templates`, method: 'GET',
        params
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - POST /api/admin/form-templates
 * - API 분류: 피그마/프론트 화면에서 사용하는 화면 API
 * - 사용하는 화면: 폼/템플릿/설문 (`SCR_FORM`)
 * - 프론트 담당 영역: forms-surveys (`forms-surveys`)
 * - 호출 방식: `POST /api/admin/form-templates`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 폼 상태 or 선택 행 action payload
 * - 응답 사용 위치: 변경 결과 then 관련 조회 키 갱신
 * - 프론트 조회 키: `post_admin_form-templates`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 라우트 준비 완료
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 기본 스모크 검증 대상
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 forms-surveys API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary POST /api/admin/form-templates
 */
const createTemplate2 = (
    formTemplateCreateRequest: FormTemplateCreateRequest,
 options?: SecondParameter<typeof customInstance<FormTemplateResponse>>,) => {
      return customInstance<FormTemplateResponse>(
      {url: `/api/admin/form-templates`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: formTemplateCreateRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 관리자 조회
 * - API 분류: 피그마/프론트 화면에서 사용하는 화면 API
 * - 사용하는 화면: 폼/템플릿/설문 (`SCR_FORM`)
 * - 프론트 담당 영역: forms-surveys (`forms-surveys`)
 * - 호출 방식: `GET /api/admin/form-templates/{templateId}/versions`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 필터/페이지네이션/선택 행에서 요청값 전달
 * - 응답 사용 위치: 조회 캐시 및 화면 목록·상세 상태 갱신
 * - 프론트 조회 키: `get_admin_form-templates_templateId_versions`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 라우트 준비 완료
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 기본 스모크 검증 대상
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 forms-surveys API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 조회 API는 응답 원본 status/code 값을 화면 배지 라벨과 분리해서 보관합니다. 라벨은 프론트 표시용, 원본 값은 후속 API 호출 조건으로 사용합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary 관리자 조회
 */
const listVersions = (
    templateId: number,
 options?: SecondParameter<typeof customInstance<FormTemplateVersionSummaryResponse[]>>,) => {
      return customInstance<FormTemplateVersionSummaryResponse[]>(
      {url: `/api/admin/form-templates/${templateId}/versions`, method: 'GET'
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - POST /api/admin/form-templates/{templateId}/versions
 * - API 분류: 피그마/프론트 화면에서 사용하는 화면 API
 * - 사용하는 화면: 폼/템플릿/설문 (`SCR_FORM`)
 * - 프론트 담당 영역: forms-surveys (`forms-surveys`)
 * - 호출 방식: `POST /api/admin/form-templates/{templateId}/versions`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 폼 상태 or 선택 행 action payload
 * - 응답 사용 위치: 변경 결과 then 관련 조회 키 갱신
 * - 프론트 조회 키: `post_admin_form-templates_templateId_versions`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 라우트 준비 완료
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 기본 스모크 검증 대상
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 forms-surveys API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary POST /api/admin/form-templates/{templateId}/versions
 */
const createVersion1 = (
    templateId: number,
    formTemplateVersionCreateRequest: FormTemplateVersionCreateRequest,
 options?: SecondParameter<typeof customInstance<FormTemplateVersionResponse>>,) => {
      return customInstance<FormTemplateVersionResponse>(
      {url: `/api/admin/form-templates/${templateId}/versions`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: formTemplateVersionCreateRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - POST /api/admin/form-templates/{templateId}/versions/copy
 * - API 분류: 피그마/프론트 화면에서 사용하는 화면 API
 * - 사용하는 화면: 폼/템플릿/설문 (`SCR_FORM`)
 * - 프론트 담당 영역: forms-surveys (`forms-surveys`)
 * - 호출 방식: `POST /api/admin/form-templates/{templateId}/versions/copy`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 폼 상태 or 선택 행 action payload
 * - 응답 사용 위치: 변경 결과 then 관련 조회 키 갱신
 * - 프론트 조회 키: `post_admin_form-templates_templateId_versions_copy`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 라우트 준비 완료
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 기본 스모크 검증 대상
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 forms-surveys API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary POST /api/admin/form-templates/{templateId}/versions/copy
 */
const copyVersion = (
    templateId: number,
    formTemplateVersionCopyRequest: FormTemplateVersionCopyRequest,
 options?: SecondParameter<typeof customInstance<FormVersionAdminResponse>>,) => {
      return customInstance<FormVersionAdminResponse>(
      {url: `/api/admin/form-templates/${templateId}/versions/copy`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: formTemplateVersionCopyRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - POST /api/admin/form-template-versions/{versionId}/publish
 * - API 분류: 피그마/프론트 화면에서 사용하는 화면 API
 * - 사용하는 화면: 폼/템플릿/설문 (`SCR_FORM`)
 * - 프론트 담당 영역: forms-surveys (`forms-surveys`)
 * - 호출 방식: `POST /api/admin/form-template-versions/{versionId}/publish`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 폼 상태 or 선택 행 action payload
 * - 응답 사용 위치: 변경 결과 then 관련 조회 키 갱신
 * - 프론트 조회 키: `post_admin_form-template-versions_versionId_publish`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 라우트 준비 완료
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 기본 스모크 검증 대상
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 forms-surveys API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary POST /api/admin/form-template-versions/{versionId}/publish
 */
const publishVersion = (
    versionId: number,
    formTemplateVersionPublishRequest: FormTemplateVersionPublishRequest,
 options?: SecondParameter<typeof customInstance<FormTemplateVersionResponse>>,) => {
      return customInstance<FormTemplateVersionResponse>(
      {url: `/api/admin/form-template-versions/${versionId}/publish`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: formTemplateVersionPublishRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 과제/설문 파일 제출물 일괄 Worker ZIP 생성
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 화면 직접 호출보다는 운영/진단 또는 내부 처리에서 사용합니다.
 * - 호출 방식: `POST /api/admin/form-submission-files/bulk-download/jobs`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: Swagger 요청 폼 또는 화면 필터/선택값
 * - 응답 사용 위치: 응답 본문을 화면 상태와 조회 캐시에 반영
 * - 프론트 조회 키: 화면별 조회 키 정책에 따름
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 준비 상태 정보 없음
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 스테이징 기본 검증 대상
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: FILE_DOWNLOAD 권한 필요
 * - 접근 범위: PROGRAM_ASSIGNED 범위 정책
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: 기본 마스킹 응답
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 개인정보/파일 API는 마스킹 응답과 원문 접근을 구분합니다. 원문 조회, export, 민감파일 다운로드는 감사로그가 저장되어야 성공으로 취급합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Notion 파일형 과제 제출물 일괄 다운로드 Worker ZIP
 * @summary 과제/설문 파일 제출물 일괄 Worker ZIP 생성
 */
const formSubmissionFiles = (
    formSubmissionBulkDownloadRequest: FormSubmissionBulkDownloadRequest,
 options?: SecondParameter<typeof customInstance<ApiResponseBulkDownloadJobResponse>>,) => {
      return customInstance<ApiResponseBulkDownloadJobResponse>(
      {url: `/api/admin/form-submission-files/bulk-download/jobs`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: formSubmissionBulkDownloadRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - POST /api/admin/form-responses/submit
 * - API 분류: 피그마/프론트 화면에서 사용하는 화면 API
 * - 사용하는 화면: 폼/템플릿/설문 (`SCR_FORM`)
 * - 프론트 담당 영역: forms-surveys (`forms-surveys`)
 * - 호출 방식: `POST /api/admin/form-responses/submit`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 폼 상태 or 선택 행 action payload
 * - 응답 사용 위치: 변경 결과 then 관련 조회 키 갱신
 * - 프론트 조회 키: `post_admin_form-responses_submit`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 라우트 준비 완료
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 기본 스모크 검증 대상
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 forms-surveys API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary POST /api/admin/form-responses/submit
 */
const submitResponse = (
    formResponseCreateRequest: FormResponseCreateRequest,
 options?: SecondParameter<typeof customInstance<FormResponseResponse>>,) => {
      return customInstance<FormResponseResponse>(
      {url: `/api/admin/form-responses/submit`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: formResponseCreateRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 프로그램 삭제
 * - API 분류: 피그마/프론트 화면에서 사용하는 화면 API
 * - 사용하는 화면: 폼/템플릿/설문 (`SCR_FORM`)
 * - 프론트 담당 영역: forms-surveys (`forms-surveys`)
 * - 호출 방식: `DELETE /api/admin/programs/{programId}/form-bindings/{bindingId}`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 프로그램 상세 선택값, 템플릿 유형, 일정형/커리큘럼형 대상 구간, 사용자 참여 context
 * - 응답 사용 위치: 프로그램 상세 설문/만족도 바인딩 비활성화 결과로 화면 목록/모달/응답 상태를 갱신
 * - 프론트 조회 키: `delete_programs_programId_form_bindings_bindingId`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 라우트 준비 완료
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: STAGE178_SURVEY_SCREEN_MAPPING_SMOKE
 * - 목데이터 대체: 프로그램 상세 신규 설문조사 등록 모달 및 사용자 설문 참여 화면을 API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary 프로그램 삭제
 */
const deactivateFormBinding = (
    programId: string,
    bindingId: number,
 options?: SecondParameter<typeof customInstance<ProgramFormBindingResponse>>,) => {
      return customInstance<ProgramFormBindingResponse>(
      {url: `/api/admin/programs/${programId}/form-bindings/${bindingId}`, method: 'DELETE'
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 프로그램 부분 수정
 * - API 분류: 피그마/프론트 화면에서 사용하는 화면 API
 * - 사용하는 화면: 폼/템플릿/설문 (`SCR_FORM`)
 * - 프론트 담당 영역: forms-surveys (`forms-surveys`)
 * - 호출 방식: `PATCH /api/admin/programs/{programId}/form-bindings/{bindingId}`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 프로그램 상세 선택값, 템플릿 유형, 일정형/커리큘럼형 대상 구간, 사용자 참여 context
 * - 응답 사용 위치: 프로그램 상세 설문/만족도 바인딩 수정 결과로 화면 목록/모달/응답 상태를 갱신
 * - 프론트 조회 키: `patch_programs_programId_form_bindings_bindingId`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 라우트 준비 완료
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: STAGE178_SURVEY_SCREEN_MAPPING_SMOKE
 * - 목데이터 대체: 프로그램 상세 신규 설문조사 등록 모달 및 사용자 설문 참여 화면을 API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary 프로그램 부분 수정
 */
const updateFormBinding = (
    programId: string,
    bindingId: number,
    programFormBindingRequest: ProgramFormBindingRequest,
 options?: SecondParameter<typeof customInstance<ProgramFormBindingResponse>>,) => {
      return customInstance<ProgramFormBindingResponse>(
      {url: `/api/admin/programs/${programId}/form-bindings/${bindingId}`, method: 'PATCH',
      headers: {'Content-Type': 'application/json', },
      data: programFormBindingRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 관리자 조회
 * - API 분류: 피그마/프론트 화면에서 사용하는 화면 API
 * - 사용하는 화면: 폼/템플릿/설문 (`SCR_FORM`)
 * - 프론트 담당 영역: forms-surveys (`forms-surveys`)
 * - 호출 방식: `GET /api/admin/form-templates/{templateId}`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 필터/페이지네이션/선택 행에서 요청값 전달
 * - 응답 사용 위치: 조회 캐시 및 화면 목록·상세 상태 갱신
 * - 프론트 조회 키: `get_admin_form-templates_templateId`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 라우트 준비 완료
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 기본 스모크 검증 대상
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 forms-surveys API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 조회 API는 응답 원본 status/code 값을 화면 배지 라벨과 분리해서 보관합니다. 라벨은 프론트 표시용, 원본 값은 후속 API 호출 조건으로 사용합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary 관리자 조회
 */
const getTemplate1 = (
    templateId: number,
 options?: SecondParameter<typeof customInstance<FormTemplateResponse>>,) => {
      return customInstance<FormTemplateResponse>(
      {url: `/api/admin/form-templates/${templateId}`, method: 'GET'
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 관리자 삭제
 * - API 분류: 피그마/프론트 화면에서 사용하는 화면 API
 * - 사용하는 화면: 폼/템플릿/설문 (`SCR_FORM`)
 * - 프론트 담당 영역: forms-surveys (`forms-surveys`)
 * - 호출 방식: `DELETE /api/admin/form-templates/{templateId}`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 폼 상태 or 선택 행 action payload
 * - 응답 사용 위치: 변경 결과 then 관련 조회 키 갱신
 * - 프론트 조회 키: `delete_admin_form-templates_templateId`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 라우트 준비 완료
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 기본 스모크 검증 대상
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 forms-surveys API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary 관리자 삭제
 */
const deleteTemplate = (
    templateId: number,
 options?: SecondParameter<typeof customInstance<FormTemplateResponse>>,) => {
      return customInstance<FormTemplateResponse>(
      {url: `/api/admin/form-templates/${templateId}`, method: 'DELETE'
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 관리자 부분 수정
 * - API 분류: 피그마/프론트 화면에서 사용하는 화면 API
 * - 사용하는 화면: 폼/템플릿/설문 (`SCR_FORM`)
 * - 프론트 담당 영역: forms-surveys (`forms-surveys`)
 * - 호출 방식: `PATCH /api/admin/form-templates/{templateId}`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 폼 상태 or 선택 행 action payload
 * - 응답 사용 위치: 변경 결과 then 관련 조회 키 갱신
 * - 프론트 조회 키: `patch_admin_form-templates_templateId`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 라우트 준비 완료
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 기본 스모크 검증 대상
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 forms-surveys API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary 관리자 부분 수정
 */
const updateTemplate = (
    templateId: number,
    formTemplateUpdateRequest: FormTemplateUpdateRequest,
 options?: SecondParameter<typeof customInstance<FormTemplateResponse>>,) => {
      return customInstance<FormTemplateResponse>(
      {url: `/api/admin/form-templates/${templateId}`, method: 'PATCH',
      headers: {'Content-Type': 'application/json', },
      data: formTemplateUpdateRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 템플릿 신규 생성 옵션 조회
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 화면 직접 호출보다는 운영/진단 또는 내부 처리에서 사용합니다.
 * - 호출 방식: `GET /api/admin/form-templates/create-options`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: Swagger 요청 폼 또는 화면 필터/선택값
 * - 응답 사용 위치: 응답 본문을 화면 상태와 조회 캐시에 반영
 * - 프론트 조회 키: 화면별 조회 키 정책에 따름
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 준비 상태 정보 없음
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 스테이징 기본 검증 대상
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: FORM_TEMPLATE_READ 권한 필요
 * - 접근 범위: 관리자 CMS 권한 범위
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: 개인정보 없음
 * - 감사로그 저장: 필수 아님
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 조회 API는 응답 원본 status/code 값을 화면 배지 라벨과 분리해서 보관합니다. 라벨은 프론트 표시용, 원본 값은 후속 API 호출 조건으로 사용합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Stage001 template creation option contract
 * @summary 템플릿 신규 생성 옵션 조회
 */
const createOptions = (

 options?: SecondParameter<typeof customInstance<FormTemplateCreateOptionResponse[]>>,) => {
      return customInstance<FormTemplateCreateOptionResponse[]>(
      {url: `/api/admin/form-templates/create-options`, method: 'GET'
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 템플릿 코드 기준 최신 schemaJson/extensionJson/settingsJson 조회
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 화면 직접 호출보다는 운영/진단 또는 내부 처리에서 사용합니다.
 * - 호출 방식: `GET /api/admin/form-templates/by-code/{templateCode}/payload`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: Swagger 요청 폼 또는 화면 필터/선택값
 * - 응답 사용 위치: 응답 본문을 화면 상태와 조회 캐시에 반영
 * - 프론트 조회 키: 화면별 조회 키 정책에 따름
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 준비 상태 정보 없음
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 스테이징 기본 검증 대상
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: FORM_TEMPLATE_READ 권한 필요
 * - 접근 범위: 관리자 CMS 권한 범위
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: 개인정보 없음
 * - 감사로그 저장: 필수 아님
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 조회 API는 응답 원본 status/code 값을 화면 배지 라벨과 분리해서 보관합니다. 라벨은 프론트 표시용, 원본 값은 후속 API 호출 조건으로 사용합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: FE templateCode contract for WritingFormDraft/settingsJson restore
 * @summary 템플릿 코드 기준 최신 schemaJson/extensionJson/settingsJson 조회
 */
const getTemplatePayloadByCode = (
    templateCode: string,
 options?: SecondParameter<typeof customInstance<FormTemplateLatestPayloadResponse>>,) => {
      return customInstance<FormTemplateLatestPayloadResponse>(
      {url: `/api/admin/form-templates/by-code/${templateCode}/payload`, method: 'GET'
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 관리자 조회
 * - API 분류: 피그마/프론트 화면에서 사용하는 화면 API
 * - 사용하는 화면: 폼/템플릿/설문 (`SCR_FORM`)
 * - 프론트 담당 영역: forms-surveys (`forms-surveys`)
 * - 호출 방식: `GET /api/admin/form-template-versions/{versionId}/render`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 필터/페이지네이션/선택 행에서 요청값 전달
 * - 응답 사용 위치: 조회 캐시 및 화면 목록·상세 상태 갱신
 * - 프론트 조회 키: `get_form-template-versions_versionId_render`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 라우트 준비 완료
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 기본 스모크 검증 대상
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 forms-surveys API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 조회 API는 응답 원본 status/code 값을 화면 배지 라벨과 분리해서 보관합니다. 라벨은 프론트 표시용, 원본 값은 후속 API 호출 조건으로 사용합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary 관리자 조회
 */
const render1 = (
    versionId: number,
 options?: SecondParameter<typeof customInstance<Render1200>>,) => {
      return customInstance<Render1200>(
      {url: `/api/admin/form-template-versions/${versionId}/render`, method: 'GET'
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 파일 조회
 * - API 분류: 피그마/프론트 화면에서 사용하는 화면 API
 * - 사용하는 화면: 폼/템플릿/설문 (`SCR_FORM`)
 * - 프론트 담당 영역: forms-surveys (`forms-surveys`)
 * - 호출 방식: `GET /api/admin/form-submission-files/{submissionFileId}/download`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 필터/페이지네이션/선택 행에서 요청값 전달
 * - 응답 사용 위치: 조회 캐시 및 화면 목록·상세 상태 갱신 (mock/local provider first, production provider after staging config)
 * - 프론트 조회 키: `get_admin_form-submission-files_submissionFileId_download`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 라우트 준비 완료
 * - 외부 연동 확인: S3/KMS_FILE_SCAN_OR_DOCUMENT_RENDERER 연동 검증 필요
 * - 스테이징 점검 기준: 기본 스모크 검증 대상
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 forms-surveys API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 개인정보/파일 API는 마스킹 응답과 원문 접근을 구분합니다. 원문 조회, export, 민감파일 다운로드는 감사로그가 저장되어야 성공으로 취급합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary 파일 조회
 */
const submissionFileDownload = (
    submissionFileId: number,
 options?: SecondParameter<typeof customInstance<FormSubmissionFileDownloadResponse>>,) => {
      return customInstance<FormSubmissionFileDownloadResponse>(
      {url: `/api/admin/form-submission-files/${submissionFileId}/download`, method: 'GET'
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 관리자 조회
 * - API 분류: 피그마/프론트 화면에서 사용하는 화면 API
 * - 사용하는 화면: 폼/템플릿/설문 (`SCR_FORM`)
 * - 프론트 담당 영역: forms-surveys (`forms-surveys`)
 * - 호출 방식: `GET /api/admin/form-responses`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 필터/페이지네이션/선택 행에서 요청값 전달
 * - 응답 사용 위치: 조회 캐시 및 화면 목록·상세 상태 갱신
 * - 프론트 조회 키: `get_admin_form-responses`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 라우트 준비 완료
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 기본 스모크 검증 대상
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 forms-surveys API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 조회 API는 응답 원본 status/code 값을 화면 배지 라벨과 분리해서 보관합니다. 라벨은 프론트 표시용, 원본 값은 후속 API 호출 조건으로 사용합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary 관리자 조회
 */
const listResponses = (
    params?: ListResponsesParams,
 options?: SecondParameter<typeof customInstance<PageResponseFormResponseResponse>>,) => {
      return customInstance<PageResponseFormResponseResponse>(
      {url: `/api/admin/form-responses`, method: 'GET',
        params
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 관리자 조회
 * - API 분류: 피그마/프론트 화면에서 사용하는 화면 API
 * - 사용하는 화면: 폼/템플릿/설문 (`SCR_FORM`)
 * - 프론트 담당 영역: forms-surveys (`forms-surveys`)
 * - 호출 방식: `GET /api/admin/form-responses/{responseId}`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 필터/페이지네이션/선택 행에서 요청값 전달
 * - 응답 사용 위치: 조회 캐시 및 화면 목록·상세 상태 갱신
 * - 프론트 조회 키: `get_admin_form-responses_responseId`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 라우트 준비 완료
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 기본 스모크 검증 대상
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 forms-surveys API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 조회 API는 응답 원본 status/code 값을 화면 배지 라벨과 분리해서 보관합니다. 라벨은 프론트 표시용, 원본 값은 후속 API 호출 조건으로 사용합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary 관리자 조회
 */
const getResponse = (
    responseId: number,
 options?: SecondParameter<typeof customInstance<FormResponseResponse>>,) => {
      return customInstance<FormResponseResponse>(
      {url: `/api/admin/form-responses/${responseId}`, method: 'GET'
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 제출 문서 원본/답변/projection 스냅샷 조회
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 화면 직접 호출보다는 운영/진단 또는 내부 처리에서 사용합니다.
 * - 호출 방식: `GET /api/admin/form-responses/{responseId}/document-snapshot`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: Swagger 요청 폼 또는 화면 필터/선택값
 * - 응답 사용 위치: 응답 본문을 화면 상태와 조회 캐시에 반영
 * - 프론트 조회 키: 화면별 조회 키 정책에 따름
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 준비 상태 정보 없음
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 스테이징 기본 검증 대상
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: FORM_RESPONSE_READ 권한 필요
 * - 접근 범위: 관리자 CMS 권한 범위
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: FORM_RESPONSE_DOCUMENT 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 조회 API는 응답 원본 status/code 값을 화면 배지 라벨과 분리해서 보관합니다. 라벨은 프론트 표시용, 원본 값은 후속 API 호출 조건으로 사용합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Added for template-authored document snapshot/projection reuse flow.
 * @summary 제출 문서 원본/답변/projection 스냅샷 조회
 */
const getDocumentSnapshot = (
    responseId: number,
 options?: SecondParameter<typeof customInstance<FormResponseDocumentSnapshotResponse>>,) => {
      return customInstance<FormResponseDocumentSnapshotResponse>(
      {url: `/api/admin/form-responses/${responseId}/document-snapshot`, method: 'GET'
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 제출 문서 업무용 projection 목록 조회
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 화면 직접 호출보다는 운영/진단 또는 내부 처리에서 사용합니다.
 * - 호출 방식: `GET /api/admin/form-responses/business-projections`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: Swagger 요청 폼 또는 화면 필터/선택값
 * - 응답 사용 위치: 응답 본문을 화면 상태와 조회 캐시에 반영
 * - 프론트 조회 키: 화면별 조회 키 정책에 따름
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 준비 상태 정보 없음
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 스테이징 기본 검증 대상
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: FORM_RESPONSE_READ 권한 필요
 * - 접근 범위: 관리자 CMS 권한 범위
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: FORM_RESPONSE_DOCUMENT 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 조회 API는 응답 원본 status/code 값을 화면 배지 라벨과 분리해서 보관합니다. 라벨은 프론트 표시용, 원본 값은 후속 API 호출 조건으로 사용합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Added for template-authored document snapshot/projection reuse flow.
 * @summary 제출 문서 업무용 projection 목록 조회
 */
const listBusinessProjections = (
    params?: ListBusinessProjectionsParams,
 options?: SecondParameter<typeof customInstance<FormResponseProjectionResponse[]>>,) => {
      return customInstance<FormResponseProjectionResponse[]>(
      {url: `/api/admin/form-responses/business-projections`, method: 'GET',
        params
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 관리자 조회
 * - API 분류: 피그마/프론트 화면에서 사용하는 화면 API
 * - 사용하는 화면: 폼/템플릿/설문 (`SCR_FORM`)
 * - 프론트 담당 영역: forms-surveys (`forms-surveys`)
 * - 호출 방식: `GET /api/admin/form-auto-fill-keys`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 필터/페이지네이션/선택 행에서 요청값 전달
 * - 응답 사용 위치: 조회 캐시 및 화면 목록·상세 상태 갱신
 * - 프론트 조회 키: `get_admin_form-auto-fill-keys`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 라우트 준비 완료
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 기본 스모크 검증 대상
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 forms-surveys API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 조회 API는 응답 원본 status/code 값을 화면 배지 라벨과 분리해서 보관합니다. 라벨은 프론트 표시용, 원본 값은 후속 API 호출 조건으로 사용합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary 관리자 조회
 */
const autoFillKeys = (

 options?: SecondParameter<typeof customInstance<FormAutoFillKeyResponse[]>>,) => {
      return customInstance<FormAutoFillKeyResponse[]>(
      {url: `/api/admin/form-auto-fill-keys`, method: 'GET'
    },
      options);
    }

return {getVersion,updateVersion,formBindings,createFormBinding,listTemplates,createTemplate2,listVersions,createVersion1,copyVersion,publishVersion,formSubmissionFiles,submitResponse,deactivateFormBinding,updateFormBinding,getTemplate1,deleteTemplate,updateTemplate,createOptions,getTemplatePayloadByCode,render1,submissionFileDownload,listResponses,getResponse,getDocumentSnapshot,listBusinessProjections,autoFillKeys}};
export type GetVersionResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIFormsSurveysSubset>['getVersion']>>>
export type UpdateVersionResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIFormsSurveysSubset>['updateVersion']>>>
export type FormBindingsResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIFormsSurveysSubset>['formBindings']>>>
export type CreateFormBindingResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIFormsSurveysSubset>['createFormBinding']>>>
export type ListTemplatesResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIFormsSurveysSubset>['listTemplates']>>>
export type CreateTemplate2Result = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIFormsSurveysSubset>['createTemplate2']>>>
export type ListVersionsResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIFormsSurveysSubset>['listVersions']>>>
export type CreateVersion1Result = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIFormsSurveysSubset>['createVersion1']>>>
export type CopyVersionResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIFormsSurveysSubset>['copyVersion']>>>
export type PublishVersionResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIFormsSurveysSubset>['publishVersion']>>>
export type FormSubmissionFilesResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIFormsSurveysSubset>['formSubmissionFiles']>>>
export type SubmitResponseResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIFormsSurveysSubset>['submitResponse']>>>
export type DeactivateFormBindingResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIFormsSurveysSubset>['deactivateFormBinding']>>>
export type UpdateFormBindingResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIFormsSurveysSubset>['updateFormBinding']>>>
export type GetTemplate1Result = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIFormsSurveysSubset>['getTemplate1']>>>
export type DeleteTemplateResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIFormsSurveysSubset>['deleteTemplate']>>>
export type UpdateTemplateResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIFormsSurveysSubset>['updateTemplate']>>>
export type CreateOptionsResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIFormsSurveysSubset>['createOptions']>>>
export type GetTemplatePayloadByCodeResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIFormsSurveysSubset>['getTemplatePayloadByCode']>>>
export type Render1Result = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIFormsSurveysSubset>['render1']>>>
export type SubmissionFileDownloadResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIFormsSurveysSubset>['submissionFileDownload']>>>
export type ListResponsesResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIFormsSurveysSubset>['listResponses']>>>
export type GetResponseResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIFormsSurveysSubset>['getResponse']>>>
export type GetDocumentSnapshotResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIFormsSurveysSubset>['getDocumentSnapshot']>>>
export type ListBusinessProjectionsResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIFormsSurveysSubset>['listBusinessProjections']>>>
export type AutoFillKeysResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIFormsSurveysSubset>['autoFillKeys']>>>
