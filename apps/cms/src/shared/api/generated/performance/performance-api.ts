/**
 * Generated by orval v8.17.0 🍺
 * Do not edit manually.
 * JA Korea CMS Backend API — Performance subset
 * Filtered for CMS performance (education-record) Orval codegen.
 * OpenAPI spec version: v9
 */
import type {
  ApiResponseBulkActionResponse,
  ApiResponsePerformanceClosingResponse,
  ApiResponsePerformanceCorrectionResponse,
  ApiResponsePerformanceExternalExportResponse,
  ApiResponsePerformanceRebuildResponse,
  ApiResponsePerformanceRecordFrontendResponse,
  ApiResponsePerformanceRuleResponse,
  ApiResponsePerformanceSnapshotRefreshResponse,
  ApiResponsePerformanceStatusChangeResponse,
  BulkDecisionRequest,
  ExportRecordsParams,
  GetPerformanceSummaryParams,
  ListCorrectionRequests1Params,
  ListExternalExportsParams,
  ListRecordsParams,
  ListRulesParams,
  PageResponsePerformanceCorrectionListItemResponse,
  PageResponsePerformanceExternalExportResponse,
  PageResponsePerformanceRuleResponse,
  PerformanceClosingRequest,
  PerformanceCorrectionDiffResponse,
  PerformanceCorrectionRequest,
  PerformanceExportResponse,
  PerformanceExternalExportRequest,
  PerformanceRebuildRequest,
  PerformanceRecordFrontendResponse,
  PerformanceRecordListItemResponse,
  PerformanceRecordUpdateRequest,
  PerformanceRevisionDetailResponse,
  PerformanceRuleUpsertRequest,
  PerformanceSnapshotLinkResponse,
  PerformanceStatsFrontendResponse,
  PerformanceStatusChangeRequest
} from './schemas';

import { customInstance } from '../../orval-mutator';


type SecondParameter<T extends (...args: never) => unknown> = Parameters<T>[1];


  export const getJAKoreaCMSBackendAPIPerformanceSubset = () => {
/**
 * ### 이 API가 하는 일
 * - 실적 조회
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 실적 관리 (`null`)
 * - 프론트 담당 영역: 실적 (`performance`)
 * - 호출 방식: `GET /api/admin/performance/rules`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 필터/페이지네이션/선택 행에서 요청값 전달
 * - 응답 사용 위치: 조회 캐시 및 화면 목록·상세 상태 갱신
 * - 프론트 조회 키: `get_admin_performance_rules`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: STAGING_VERIFY_REQUIRED
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: REQUIRED
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 performance API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 실적 상태는 생성/확정/마감/정정 흐름을 구분합니다. 화면에서는 원천 프로그램, 일정, 정산과의 추적 링크를 함께 보여주는 것을 권장합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary 실적 조회
 */
const listRules = (
    params?: ListRulesParams,
 options?: SecondParameter<typeof customInstance<PageResponsePerformanceRuleResponse>>,) => {
      return customInstance<PageResponsePerformanceRuleResponse>(
      {url: `/api/admin/performance/rules`, method: 'GET',
        params
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - POST /api/admin/performance/rules
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 실적 관리 (`null`)
 * - 프론트 담당 영역: 실적 (`performance`)
 * - 호출 방식: `POST /api/admin/performance/rules`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 폼 상태 or 선택 행 action payload
 * - 응답 사용 위치: 변경 결과 then 관련 조회 키 갱신
 * - 프론트 조회 키: `post_admin_performance_rules`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: STAGING_VERIFY_REQUIRED
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: REQUIRED
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 performance API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 실적 상태는 생성/확정/마감/정정 흐름을 구분합니다. 화면에서는 원천 프로그램, 일정, 정산과의 추적 링크를 함께 보여주는 것을 권장합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary POST /api/admin/performance/rules
 */
const upsertRule = (
    performanceRuleUpsertRequest: PerformanceRuleUpsertRequest,
 options?: SecondParameter<typeof customInstance<ApiResponsePerformanceRuleResponse>>,) => {
      return customInstance<ApiResponsePerformanceRuleResponse>(
      {url: `/api/admin/performance/rules`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: performanceRuleUpsertRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - POST /api/admin/performance/records/{performanceRecordId}/master-snapshots:refresh
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 실적 관리 (`null`)
 * - 프론트 담당 영역: 실적 (`performance`)
 * - 호출 방식: `POST /api/admin/performance/records/{performanceRecordId}/master-snapshots:refresh`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 폼 상태 or 선택 행 action payload
 * - 응답 사용 위치: 변경 결과 then 관련 조회 키 갱신
 * - 프론트 조회 키: `post_admin_performance_records_performanceRecordId_master-snapshots:refresh`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: STAGING_VERIFY_REQUIRED
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: REQUIRED
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 performance API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 실적 상태는 생성/확정/마감/정정 흐름을 구분합니다. 화면에서는 원천 프로그램, 일정, 정산과의 추적 링크를 함께 보여주는 것을 권장합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary POST /api/admin/performance/records/{performanceRecordId}/master-snapshots:refresh
 */
const refreshSnapshots = (
    performanceRecordId: number,
 options?: SecondParameter<typeof customInstance<ApiResponsePerformanceSnapshotRefreshResponse>>,) => {
      return customInstance<ApiResponsePerformanceSnapshotRefreshResponse>(
      {url: `/api/admin/performance/records/${performanceRecordId}/master-snapshots:refresh`, method: 'POST'
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 실적 조회
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 실적 관리 (`null`)
 * - 프론트 담당 영역: 실적 (`performance`)
 * - 호출 방식: `GET /api/admin/performance/external-exports`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 필터/페이지네이션/선택 행에서 요청값 전달
 * - 응답 사용 위치: 조회 캐시 및 화면 목록·상세 상태 갱신
 * - 프론트 조회 키: `get_admin_performance_external-exports`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: STAGING_VERIFY_REQUIRED
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: REQUIRED
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 performance API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 실적 상태는 생성/확정/마감/정정 흐름을 구분합니다. 화면에서는 원천 프로그램, 일정, 정산과의 추적 링크를 함께 보여주는 것을 권장합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary 실적 조회
 */
const listExternalExports = (
    params?: ListExternalExportsParams,
 options?: SecondParameter<typeof customInstance<PageResponsePerformanceExternalExportResponse>>,) => {
      return customInstance<PageResponsePerformanceExternalExportResponse>(
      {url: `/api/admin/performance/external-exports`, method: 'GET',
        params
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - POST /api/admin/performance/external-exports
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 실적 관리 (`null`)
 * - 프론트 담당 영역: 실적 (`performance`)
 * - 호출 방식: `POST /api/admin/performance/external-exports`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 폼 상태 or 선택 행 action payload
 * - 응답 사용 위치: 변경 결과 then 관련 조회 키 갱신
 * - 프론트 조회 키: `post_admin_performance_external-exports`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: STAGING_VERIFY_REQUIRED
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: REQUIRED
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 performance API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 실적 상태는 생성/확정/마감/정정 흐름을 구분합니다. 화면에서는 원천 프로그램, 일정, 정산과의 추적 링크를 함께 보여주는 것을 권장합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary POST /api/admin/performance/external-exports
 */
const requestExternalExport = (
    performanceExternalExportRequest: PerformanceExternalExportRequest,
 options?: SecondParameter<typeof customInstance<ApiResponsePerformanceExternalExportResponse>>,) => {
      return customInstance<ApiResponsePerformanceExternalExportResponse>(
      {url: `/api/admin/performance/external-exports`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: performanceExternalExportRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - POST /api/admin/performance-records/{performanceRecordId}/correction-requests
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 실적 관리 (`null`)
 * - 프론트 담당 영역: 실적 (`performance`)
 * - 호출 방식: `POST /api/admin/performance-records/{performanceRecordId}/correction-requests`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 경로/쿼리/요청 본문 from 선택 행, 필터, or provider callback
 * - 응답 사용 위치: 실적 정정 요청 생성 response drives 화면 state, modal, or audit detail
 * - 프론트 조회 키: `post_api_admin_performance_records_performancerecordid_correction_requests`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: STAGING_VERIFY_REQUIRED
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: PERFORMANCE_CORRECTION_CREATE_SMOKE
 * - 목데이터 대체: Replace localStorage state for 실적 정정 요청 생성 with API response/cache.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 실적 상태는 생성/확정/마감/정정 흐름을 구분합니다. 화면에서는 원천 프로그램, 일정, 정산과의 추적 링크를 함께 보여주는 것을 권장합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary POST /api/admin/performance-records/{performanceRecordId}/correction-requests
 */
const requestCorrection1 = (
    performanceRecordId: number,
    performanceCorrectionRequest: PerformanceCorrectionRequest,
 options?: SecondParameter<typeof customInstance<ApiResponsePerformanceCorrectionResponse>>,) => {
      return customInstance<ApiResponsePerformanceCorrectionResponse>(
      {url: `/api/admin/performance-records/${performanceRecordId}/correction-requests`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: performanceCorrectionRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - POST /api/admin/performance-records/rebuild
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 실적 관리 (`null`)
 * - 프론트 담당 영역: 실적 (`performance`)
 * - 호출 방식: `POST /api/admin/performance-records/rebuild`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 폼 상태 or 선택 행 action payload
 * - 응답 사용 위치: 변경 결과 then 관련 조회 키 갱신
 * - 프론트 조회 키: `post_admin_performance-records_rebuild`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: STAGING_VERIFY_REQUIRED
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: REQUIRED
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 performance API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 실적 상태는 생성/확정/마감/정정 흐름을 구분합니다. 화면에서는 원천 프로그램, 일정, 정산과의 추적 링크를 함께 보여주는 것을 권장합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary POST /api/admin/performance-records/rebuild
 */
const rebuild = (
    performanceRebuildRequest: PerformanceRebuildRequest,
 options?: SecondParameter<typeof customInstance<ApiResponsePerformanceRebuildResponse>>,) => {
      return customInstance<ApiResponsePerformanceRebuildResponse>(
      {url: `/api/admin/performance-records/rebuild`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: performanceRebuildRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 실적 일괄 제외
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 화면 직접 호출보다는 운영/진단 또는 내부 처리에서 사용합니다.
 * - 호출 방식: `POST /api/admin/performance-records/bulk-exclude`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: Swagger 요청 폼 또는 화면 필터/선택값
 * - 응답 사용 위치: 응답 본문을 화면 상태와 조회 캐시에 반영
 * - 프론트 조회 키: 화면별 조회 키 정책에 따름
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 준비 상태 정보 없음
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 스테이징 기본 검증 대상
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: PERFORMANCE_WRITE 권한 필요
 * - 접근 범위: 담당 프로그램 범위 내에서만 가능
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: NONE 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 실적 상태는 생성/확정/마감/정정 흐름을 구분합니다. 화면에서는 원천 프로그램, 일정, 정산과의 추적 링크를 함께 보여주는 것을 권장합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Figma selected delete maps to EXCLUDED status; ledger history retained
 * @summary 실적 일괄 제외
 */
const bulkExcludeRecords = (
    bulkDecisionRequest: BulkDecisionRequest,
 options?: SecondParameter<typeof customInstance<ApiResponseBulkActionResponse>>,) => {
      return customInstance<ApiResponseBulkActionResponse>(
      {url: `/api/admin/performance-records/bulk-exclude`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: bulkDecisionRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - POST /api/admin/performance-closings/reopen
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 실적 관리 (`null`)
 * - 프론트 담당 영역: 실적 (`performance`)
 * - 호출 방식: `POST /api/admin/performance-closings/reopen`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 경로/쿼리/요청 본문 from 선택 행, 필터, or provider callback
 * - 응답 사용 위치: 실적 월마감 재오픈 response drives 화면 state, modal, or audit detail
 * - 프론트 조회 키: `post_api_admin_performance_closings_reopen`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: STAGING_VERIFY_REQUIRED
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: PERFORMANCE_CLOSING_REOPEN_SMOKE
 * - 목데이터 대체: Replace localStorage state for 실적 월마감 재오픈 with API response/cache.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 실적 상태는 생성/확정/마감/정정 흐름을 구분합니다. 화면에서는 원천 프로그램, 일정, 정산과의 추적 링크를 함께 보여주는 것을 권장합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary POST /api/admin/performance-closings/reopen
 */
const reopenMonth = (
    performanceClosingRequest: PerformanceClosingRequest,
 options?: SecondParameter<typeof customInstance<ApiResponsePerformanceClosingResponse>>,) => {
      return customInstance<ApiResponsePerformanceClosingResponse>(
      {url: `/api/admin/performance-closings/reopen`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: performanceClosingRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - POST /api/admin/performance-closings/close
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 실적 관리 (`null`)
 * - 프론트 담당 영역: 실적 (`performance`)
 * - 호출 방식: `POST /api/admin/performance-closings/close`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 경로/쿼리/요청 본문 from 선택 행, 필터, or provider callback
 * - 응답 사용 위치: 실적 월마감 처리 response drives 화면 state, modal, or audit detail
 * - 프론트 조회 키: `post_api_admin_performance_closings_close`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: STAGING_VERIFY_REQUIRED
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: PERFORMANCE_CLOSING_CLOSE_SMOKE
 * - 목데이터 대체: Replace localStorage state for 실적 월마감 처리 with API response/cache.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 실적 상태는 생성/확정/마감/정정 흐름을 구분합니다. 화면에서는 원천 프로그램, 일정, 정산과의 추적 링크를 함께 보여주는 것을 권장합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary POST /api/admin/performance-closings/close
 */
const closeMonth = (
    performanceClosingRequest: PerformanceClosingRequest,
 options?: SecondParameter<typeof customInstance<ApiResponsePerformanceClosingResponse>>,) => {
      return customInstance<ApiResponsePerformanceClosingResponse>(
      {url: `/api/admin/performance-closings/close`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: performanceClosingRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 실적 조회
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 실적 관리 (`null`)
 * - 프론트 담당 영역: 실적 (`performance`)
 * - 호출 방식: `GET /api/admin/performance-records/{performanceRecordId}`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 필터/페이지네이션/선택 행에서 요청값 전달
 * - 응답 사용 위치: 조회 캐시 및 화면 목록·상세 상태 갱신
 * - 프론트 조회 키: `get_admin_performance-records_performanceRecordId`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: STAGING_VERIFY_REQUIRED
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: REQUIRED
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 performance API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 실적 상태는 생성/확정/마감/정정 흐름을 구분합니다. 화면에서는 원천 프로그램, 일정, 정산과의 추적 링크를 함께 보여주는 것을 권장합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary 실적 조회
 */
const getRecord = (
    performanceRecordId: number,
 options?: SecondParameter<typeof customInstance<PerformanceRecordFrontendResponse>>,) => {
      return customInstance<PerformanceRecordFrontendResponse>(
      {url: `/api/admin/performance-records/${performanceRecordId}`, method: 'GET'
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 실적 부분 수정
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 실적 관리 (`null`)
 * - 프론트 담당 영역: 실적 (`performance`)
 * - 호출 방식: `PATCH /api/admin/performance-records/{performanceRecordId}`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 폼 상태 or 선택 행 action payload
 * - 응답 사용 위치: 변경 결과 then 관련 조회 키 갱신
 * - 프론트 조회 키: `patch_admin_performance-records_performanceRecordId`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: STAGING_VERIFY_REQUIRED
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: REQUIRED
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 performance API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 실적 상태는 생성/확정/마감/정정 흐름을 구분합니다. 화면에서는 원천 프로그램, 일정, 정산과의 추적 링크를 함께 보여주는 것을 권장합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary 실적 부분 수정
 */
const updateRecord = (
    performanceRecordId: number,
    performanceRecordUpdateRequest: PerformanceRecordUpdateRequest,
 options?: SecondParameter<typeof customInstance<ApiResponsePerformanceRecordFrontendResponse>>,) => {
      return customInstance<ApiResponsePerformanceRecordFrontendResponse>(
      {url: `/api/admin/performance-records/${performanceRecordId}`, method: 'PATCH',
      headers: {'Content-Type': 'application/json', },
      data: performanceRecordUpdateRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 실적 부분 수정
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 실적 관리 (`null`)
 * - 프론트 담당 영역: 실적 (`performance`)
 * - 호출 방식: `PATCH /api/admin/performance-records/{performanceRecordId}/exclude`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 폼 상태 or 선택 행 action payload
 * - 응답 사용 위치: 변경 결과 then 관련 조회 키 갱신
 * - 프론트 조회 키: `patch_admin_performance-records_performanceRecordId_exclude`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: STAGING_VERIFY_REQUIRED
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: REQUIRED
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 performance API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 실적 상태는 생성/확정/마감/정정 흐름을 구분합니다. 화면에서는 원천 프로그램, 일정, 정산과의 추적 링크를 함께 보여주는 것을 권장합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary 실적 부분 수정
 */
const excludeRecord = (
    performanceRecordId: number,
    performanceStatusChangeRequest: PerformanceStatusChangeRequest,
 options?: SecondParameter<typeof customInstance<ApiResponsePerformanceStatusChangeResponse>>,) => {
      return customInstance<ApiResponsePerformanceStatusChangeResponse>(
      {url: `/api/admin/performance-records/${performanceRecordId}/exclude`, method: 'PATCH',
      headers: {'Content-Type': 'application/json', },
      data: performanceStatusChangeRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 실적 부분 수정
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 실적 관리 (`null`)
 * - 프론트 담당 영역: 실적 (`performance`)
 * - 호출 방식: `PATCH /api/admin/performance-records/{performanceRecordId}/confirm`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 폼 상태 or 선택 행 action payload
 * - 응답 사용 위치: 변경 결과 then 관련 조회 키 갱신
 * - 프론트 조회 키: `patch_admin_performance-records_performanceRecordId_confirm`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: STAGING_VERIFY_REQUIRED
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: REQUIRED
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 performance API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 실적 상태는 생성/확정/마감/정정 흐름을 구분합니다. 화면에서는 원천 프로그램, 일정, 정산과의 추적 링크를 함께 보여주는 것을 권장합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary 실적 부분 수정
 */
const confirmRecord = (
    performanceRecordId: number,
    performanceStatusChangeRequest: PerformanceStatusChangeRequest,
 options?: SecondParameter<typeof customInstance<ApiResponsePerformanceStatusChangeResponse>>,) => {
      return customInstance<ApiResponsePerformanceStatusChangeResponse>(
      {url: `/api/admin/performance-records/${performanceRecordId}/confirm`, method: 'PATCH',
      headers: {'Content-Type': 'application/json', },
      data: performanceStatusChangeRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 실적 부분 수정
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 실적 관리 (`null`)
 * - 프론트 담당 영역: 실적 (`performance`)
 * - 호출 방식: `PATCH /api/admin/performance-correction-requests/{correctionRequestId}/reject`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 경로/쿼리/요청 본문 from 선택 행, 필터, or provider callback
 * - 응답 사용 위치: 실적 정정 요청 반려 response drives 화면 state, modal, or audit detail
 * - 프론트 조회 키: `patch_api_admin_performance_correction_requests_correctionrequestid_reject`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: STAGING_VERIFY_REQUIRED
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: PERFORMANCE_CORRECTION_REJECT_SMOKE
 * - 목데이터 대체: Replace localStorage state for 실적 정정 요청 반려 with API response/cache.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 실적 상태는 생성/확정/마감/정정 흐름을 구분합니다. 화면에서는 원천 프로그램, 일정, 정산과의 추적 링크를 함께 보여주는 것을 권장합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary 실적 부분 수정
 */
const rejectCorrection1 = (
    correctionRequestId: number,
    performanceCorrectionRequest: PerformanceCorrectionRequest,
 options?: SecondParameter<typeof customInstance<ApiResponsePerformanceCorrectionResponse>>,) => {
      return customInstance<ApiResponsePerformanceCorrectionResponse>(
      {url: `/api/admin/performance-correction-requests/${correctionRequestId}/reject`, method: 'PATCH',
      headers: {'Content-Type': 'application/json', },
      data: performanceCorrectionRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 실적 부분 수정
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 실적 관리 (`null`)
 * - 프론트 담당 영역: 실적 (`performance`)
 * - 호출 방식: `PATCH /api/admin/performance-correction-requests/{correctionRequestId}/approve`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 경로/쿼리/요청 본문 from 선택 행, 필터, or provider callback
 * - 응답 사용 위치: 실적 정정 요청 승인 response drives 화면 state, modal, or audit detail
 * - 프론트 조회 키: `patch_api_admin_performance_correction_requests_correctionrequestid_approve`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: STAGING_VERIFY_REQUIRED
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: PERFORMANCE_CORRECTION_APPROVE_SMOKE
 * - 목데이터 대체: Replace localStorage state for 실적 정정 요청 승인 with API response/cache.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 실적 상태는 생성/확정/마감/정정 흐름을 구분합니다. 화면에서는 원천 프로그램, 일정, 정산과의 추적 링크를 함께 보여주는 것을 권장합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary 실적 부분 수정
 */
const approveCorrection1 = (
    correctionRequestId: number,
    performanceCorrectionRequest: PerformanceCorrectionRequest,
 options?: SecondParameter<typeof customInstance<ApiResponsePerformanceCorrectionResponse>>,) => {
      return customInstance<ApiResponsePerformanceCorrectionResponse>(
      {url: `/api/admin/performance-correction-requests/${correctionRequestId}/approve`, method: 'PATCH',
      headers: {'Content-Type': 'application/json', },
      data: performanceCorrectionRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 실적 조회
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 실적 관리 (`null`)
 * - 프론트 담당 영역: 실적 (`performance`)
 * - 호출 방식: `GET /api/admin/performance/summary`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 경로/쿼리 from 필터
 * - 응답 사용 위치: 조회 캐시 / 화면 summary state
 * - 프론트 조회 키: `get_performance_summary`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: STAGING_VERIFY_REQUIRED
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: STAGE103_PERFORMANCE_SUMMARY_SMOKE
 * - 목데이터 대체: Replace localStorage performance summary API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 실적 상태는 생성/확정/마감/정정 흐름을 구분합니다. 화면에서는 원천 프로그램, 일정, 정산과의 추적 링크를 함께 보여주는 것을 권장합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary 실적 조회
 */
const getPerformanceSummary = (
    params?: GetPerformanceSummaryParams,
 options?: SecondParameter<typeof customInstance<PerformanceStatsFrontendResponse>>,) => {
      return customInstance<PerformanceStatsFrontendResponse>(
      {url: `/api/admin/performance/summary`, method: 'GET',
        params
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 실적 조회
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 실적 관리 (`null`)
 * - 프론트 담당 영역: 실적 (`performance`)
 * - 호출 방식: `GET /api/admin/performance/records/{performanceRecordId}/master-snapshots`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 필터/페이지네이션/선택 행에서 요청값 전달
 * - 응답 사용 위치: 조회 캐시 및 화면 목록·상세 상태 갱신
 * - 프론트 조회 키: `get_admin_performance_records_performanceRecordId_master-snapshots`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: STAGING_VERIFY_REQUIRED
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: REQUIRED
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 performance API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 실적 상태는 생성/확정/마감/정정 흐름을 구분합니다. 화면에서는 원천 프로그램, 일정, 정산과의 추적 링크를 함께 보여주는 것을 권장합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary 실적 조회
 */
const listSnapshotLinks = (
    performanceRecordId: number,
 options?: SecondParameter<typeof customInstance<PerformanceSnapshotLinkResponse[]>>,) => {
      return customInstance<PerformanceSnapshotLinkResponse[]>(
      {url: `/api/admin/performance/records/${performanceRecordId}/master-snapshots`, method: 'GET'
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 실적 조회
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 실적 관리 (`null`)
 * - 프론트 담당 영역: 실적 (`performance`)
 * - 호출 방식: `GET /api/admin/performance-records`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 필터/페이지네이션/선택 행에서 요청값 전달
 * - 응답 사용 위치: 조회 캐시 및 화면 목록·상세 상태 갱신
 * - 프론트 조회 키: `get_admin_performance-records`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: STAGING_VERIFY_REQUIRED
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: REQUIRED
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 performance API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 실적 상태는 생성/확정/마감/정정 흐름을 구분합니다. 화면에서는 원천 프로그램, 일정, 정산과의 추적 링크를 함께 보여주는 것을 권장합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary 실적 조회
 */
const listRecords = (
    params?: ListRecordsParams,
 options?: SecondParameter<typeof customInstance<PerformanceRecordFrontendResponse[]>>,) => {
      return customInstance<PerformanceRecordFrontendResponse[]>(
      {url: `/api/admin/performance-records`, method: 'GET',
        params
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 실적 조회
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 실적 관리 (`null`)
 * - 프론트 담당 영역: 실적 (`performance`)
 * - 호출 방식: `GET /api/admin/performance-records/{performanceRecordId}/revisions`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 경로/쿼리/요청 본문 from 선택 행, 필터, or provider callback
 * - 응답 사용 위치: 실적 revision 목록 조회 response drives 화면 state, modal, or audit detail
 * - 프론트 조회 키: `get_api_admin_performance_records_performancerecordid_revisions`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: STAGING_VERIFY_REQUIRED
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: PERFORMANCE_REVISION_LIST_SMOKE
 * - 목데이터 대체: Replace localStorage state for 실적 revision 목록 조회 with API response/cache.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 실적 상태는 생성/확정/마감/정정 흐름을 구분합니다. 화면에서는 원천 프로그램, 일정, 정산과의 추적 링크를 함께 보여주는 것을 권장합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary 실적 조회
 */
const listRevisions = (
    performanceRecordId: number,
 options?: SecondParameter<typeof customInstance<PerformanceRecordListItemResponse[]>>,) => {
      return customInstance<PerformanceRecordListItemResponse[]>(
      {url: `/api/admin/performance-records/${performanceRecordId}/revisions`, method: 'GET'
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 실적 조회
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 실적 관리 (`null`)
 * - 프론트 담당 영역: 실적 (`performance`)
 * - 호출 방식: `GET /api/admin/performance-records/{performanceRecordId}/revisions/{revisionId}`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 경로/쿼리/요청 본문 from 선택 행, 필터, or provider callback
 * - 응답 사용 위치: 실적 revision 상세 조회 response drives 화면 state, modal, or audit detail
 * - 프론트 조회 키: `get_api_admin_performance_records_performancerecordid_revisions_revisionid`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: STAGING_VERIFY_REQUIRED
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: PERFORMANCE_REVISION_DETAIL_SMOKE
 * - 목데이터 대체: Replace localStorage state for 실적 revision 상세 조회 with API response/cache.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 실적 상태는 생성/확정/마감/정정 흐름을 구분합니다. 화면에서는 원천 프로그램, 일정, 정산과의 추적 링크를 함께 보여주는 것을 권장합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary 실적 조회
 */
const getRevisionDetail = (
    performanceRecordId: number,
    revisionId: number,
 options?: SecondParameter<typeof customInstance<PerformanceRevisionDetailResponse>>,) => {
      return customInstance<PerformanceRevisionDetailResponse>(
      {url: `/api/admin/performance-records/${performanceRecordId}/revisions/${revisionId}`, method: 'GET'
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 실적 조회
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 실적 관리 (`null`)
 * - 프론트 담당 영역: 실적 (`performance`)
 * - 호출 방식: `GET /api/admin/performance-records/export`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 필터/페이지네이션/선택 행에서 요청값 전달
 * - 응답 사용 위치: 조회 캐시 및 화면 목록·상세 상태 갱신
 * - 프론트 조회 키: `get_admin_performance-records_export`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: STAGING_VERIFY_REQUIRED
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: REQUIRED
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 performance API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 실적 상태는 생성/확정/마감/정정 흐름을 구분합니다. 화면에서는 원천 프로그램, 일정, 정산과의 추적 링크를 함께 보여주는 것을 권장합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary 실적 조회
 */
const exportRecords = (
    params?: ExportRecordsParams,
 options?: SecondParameter<typeof customInstance<PerformanceExportResponse>>,) => {
      return customInstance<PerformanceExportResponse>(
      {url: `/api/admin/performance-records/export`, method: 'GET',
        params
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 실적 조회
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 실적 관리 (`null`)
 * - 프론트 담당 영역: 실적 (`performance`)
 * - 호출 방식: `GET /api/admin/performance-correction-requests`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 경로/쿼리/요청 본문 from 선택 행, 필터, or provider callback
 * - 응답 사용 위치: 실적 정정 요청 목록 조회 response drives 화면 state, modal, or audit detail
 * - 프론트 조회 키: `get_api_admin_performance_correction_requests`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: STAGING_VERIFY_REQUIRED
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: PERFORMANCE_CORRECTION_LIST_SMOKE
 * - 목데이터 대체: Replace localStorage state for 실적 정정 요청 목록 조회 with API response/cache.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 실적 상태는 생성/확정/마감/정정 흐름을 구분합니다. 화면에서는 원천 프로그램, 일정, 정산과의 추적 링크를 함께 보여주는 것을 권장합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary 실적 조회
 */
const listCorrectionRequests1 = (
    params?: ListCorrectionRequests1Params,
 options?: SecondParameter<typeof customInstance<PageResponsePerformanceCorrectionListItemResponse>>,) => {
      return customInstance<PageResponsePerformanceCorrectionListItemResponse>(
      {url: `/api/admin/performance-correction-requests`, method: 'GET',
        params
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 실적 조회
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 실적 관리 (`null`)
 * - 프론트 담당 영역: 실적 (`performance`)
 * - 호출 방식: `GET /api/admin/performance-correction-requests/{correctionRequestId}/diff`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 경로/쿼리/요청 본문 from 선택 행, 필터, or provider callback
 * - 응답 사용 위치: 실적 정정 전후 diff 조회 response drives 화면 state, modal, or audit detail
 * - 프론트 조회 키: `get_api_admin_performance_correction_requests_correctionrequestid_diff`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: STAGING_VERIFY_REQUIRED
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: PERFORMANCE_CORRECTION_DIFF_SMOKE
 * - 목데이터 대체: Replace localStorage state for 실적 정정 전후 diff 조회 with API response/cache.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 실적 상태는 생성/확정/마감/정정 흐름을 구분합니다. 화면에서는 원천 프로그램, 일정, 정산과의 추적 링크를 함께 보여주는 것을 권장합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary 실적 조회
 */
const getCorrectionDiff = (
    correctionRequestId: number,
 options?: SecondParameter<typeof customInstance<PerformanceCorrectionDiffResponse>>,) => {
      return customInstance<PerformanceCorrectionDiffResponse>(
      {url: `/api/admin/performance-correction-requests/${correctionRequestId}/diff`, method: 'GET'
    },
      options);
    }

return {listRules,upsertRule,refreshSnapshots,listExternalExports,requestExternalExport,requestCorrection1,rebuild,bulkExcludeRecords,reopenMonth,closeMonth,getRecord,updateRecord,excludeRecord,confirmRecord,rejectCorrection1,approveCorrection1,getPerformanceSummary,listSnapshotLinks,listRecords,listRevisions,getRevisionDetail,exportRecords,listCorrectionRequests1,getCorrectionDiff}};
export type ListRulesResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIPerformanceSubset>['listRules']>>>
export type UpsertRuleResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIPerformanceSubset>['upsertRule']>>>
export type RefreshSnapshotsResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIPerformanceSubset>['refreshSnapshots']>>>
export type ListExternalExportsResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIPerformanceSubset>['listExternalExports']>>>
export type RequestExternalExportResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIPerformanceSubset>['requestExternalExport']>>>
export type RequestCorrection1Result = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIPerformanceSubset>['requestCorrection1']>>>
export type RebuildResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIPerformanceSubset>['rebuild']>>>
export type BulkExcludeRecordsResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIPerformanceSubset>['bulkExcludeRecords']>>>
export type ReopenMonthResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIPerformanceSubset>['reopenMonth']>>>
export type CloseMonthResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIPerformanceSubset>['closeMonth']>>>
export type GetRecordResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIPerformanceSubset>['getRecord']>>>
export type UpdateRecordResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIPerformanceSubset>['updateRecord']>>>
export type ExcludeRecordResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIPerformanceSubset>['excludeRecord']>>>
export type ConfirmRecordResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIPerformanceSubset>['confirmRecord']>>>
export type RejectCorrection1Result = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIPerformanceSubset>['rejectCorrection1']>>>
export type ApproveCorrection1Result = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIPerformanceSubset>['approveCorrection1']>>>
export type GetPerformanceSummaryResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIPerformanceSubset>['getPerformanceSummary']>>>
export type ListSnapshotLinksResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIPerformanceSubset>['listSnapshotLinks']>>>
export type ListRecordsResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIPerformanceSubset>['listRecords']>>>
export type ListRevisionsResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIPerformanceSubset>['listRevisions']>>>
export type GetRevisionDetailResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIPerformanceSubset>['getRevisionDetail']>>>
export type ExportRecordsResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIPerformanceSubset>['exportRecords']>>>
export type ListCorrectionRequests1Result = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIPerformanceSubset>['listCorrectionRequests1']>>>
export type GetCorrectionDiffResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIPerformanceSubset>['getCorrectionDiff']>>>
