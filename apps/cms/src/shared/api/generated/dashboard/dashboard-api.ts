/**
 * Generated by orval v8.17.0 🍺
 * Do not edit manually.
 * JA Korea CMS Backend API — Dashboard subset
 * Filtered for CMS dashboard Orval codegen (1st pilot).
 * OpenAPI spec version: v9
 */
import type {
  ApiResponse,
  ApiResponseDashboardWidgetProgramFiltersSaveResponse,
  DashboardHomeResponse,
  DashboardKpiProgressListResponse,
  DashboardKpiProgressParams,
  DashboardLogAlertListResponse,
  DashboardPreferencesResponse,
  DashboardPreferencesSaveRequest,
  DashboardProgramInquiriesParams,
  DashboardProgramInquiryListResponse,
  DashboardProgramScheduleListResponse,
  DashboardProgramSchedulesParams,
  DashboardRecruitmentListResponse,
  DashboardRecruitmentsParams,
  DashboardShortcutListResponse,
  DashboardShortcutVisibilitySaveRequest,
  DashboardWidgetLayoutSaveRequest,
  DashboardWidgetListResponse,
  DashboardWidgetProgramFiltersResponse,
  NotificationUnreadCountResponse,
  WidgetProgramFiltersSaveRequest
} from './schemas';

import { customInstance } from '../../orval-mutator';


type SecondParameter<T extends (...args: never) => unknown> = Parameters<T>[1];


  export const getJAKoreaCMSBackendAPIDashboardSubset = () => {
/**
 * ### 이 API가 하는 일
 * - 위젯별 프로그램 필터 조회
 * - API 분류: 피그마/프론트 화면에서 사용하는 화면 API
 * - 사용하는 화면: 대시보드 (`SCR_DASHBOARD`)
 * - 프론트 담당 영역: 대시보드 (`dashboard`)
 * - 호출 방식: `GET /api/admin/dashboard/widgets/{widgetKey}/program-filters`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 필터/페이지네이션/선택 행에서 요청값 전달
 * - 응답 사용 위치: 조회 캐시 및 화면 목록·상세 상태 갱신
 * - 프론트 조회 키: `get_admin_dashboard_widgets_widgetKey_program-filters`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: STAGING_VERIFY_REQUIRED
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: REQUIRED
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 dashboard API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: DASHBOARD_READ 권한 필요
 * - 접근 범위: 관리자 CMS 권한 범위
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: 개인정보 없음
 * - 감사로그 저장: 필수 아님
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 조회 API는 응답 원본 status/code 값을 화면 배지 라벨과 분리해서 보관합니다. 라벨은 프론트 표시용, 원본 값은 후속 API 호출 조건으로 사용합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: -
 * @summary 위젯별 프로그램 필터 조회
 */
const getWidgetProgramFilters = (
    widgetKey: string,
 options?: SecondParameter<typeof customInstance<DashboardWidgetProgramFiltersResponse>>,) => {
      return customInstance<DashboardWidgetProgramFiltersResponse>(
      {url: `/api/admin/dashboard/widgets/${widgetKey}/program-filters`, method: 'GET'
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 위젯별 프로그램 필터 저장
 * - API 분류: 피그마/프론트 화면에서 사용하는 화면 API
 * - 사용하는 화면: 대시보드 (`SCR_DASHBOARD`)
 * - 프론트 담당 영역: 대시보드 (`dashboard`)
 * - 호출 방식: `PUT /api/admin/dashboard/widgets/{widgetKey}/program-filters`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 폼 상태 or 선택 행 action payload
 * - 응답 사용 위치: 변경 결과 then 관련 조회 키 갱신
 * - 프론트 조회 키: `put_admin_dashboard_widgets_widgetKey_program-filters`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: STAGING_VERIFY_REQUIRED
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: REQUIRED
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 dashboard API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: DASHBOARD_WRITE 권한 필요
 * - 접근 범위: 관리자 CMS 권한 범위
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: 기본 마스킹 응답
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: -
 * @summary 위젯별 프로그램 필터 저장
 */
const saveWidgetProgramFilters = (
    widgetKey: string,
    widgetProgramFiltersSaveRequest?: WidgetProgramFiltersSaveRequest,
 options?: SecondParameter<typeof customInstance<ApiResponseDashboardWidgetProgramFiltersSaveResponse>>,) => {
      return customInstance<ApiResponseDashboardWidgetProgramFiltersSaveResponse>(
      {url: `/api/admin/dashboard/widgets/${widgetKey}/program-filters`, method: 'PUT',
      headers: {'Content-Type': 'application/json', },
      data: widgetProgramFiltersSaveRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 관리자별 위젯 레이아웃 저장
 * - API 분류: 피그마/프론트 화면에서 사용하는 화면 API
 * - 사용하는 화면: 대시보드 (`SCR_DASHBOARD`)
 * - 프론트 담당 영역: 대시보드 (`dashboard`)
 * - 호출 방식: `PUT /api/admin/dashboard/widgets/layout`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 폼 상태 or 선택 행 action payload
 * - 응답 사용 위치: 변경 결과 then 관련 조회 키 갱신
 * - 프론트 조회 키: `put_admin_dashboard_widgets_layout`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: STAGING_VERIFY_REQUIRED
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: REQUIRED
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 dashboard API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: DASHBOARD_WRITE 권한 필요
 * - 접근 범위: 관리자 CMS 권한 범위
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: 기본 마스킹 응답
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: -
 * @summary 관리자별 위젯 레이아웃 저장
 */
const saveDashboardWidgetLayout = (
    dashboardWidgetLayoutSaveRequest?: DashboardWidgetLayoutSaveRequest,
 options?: SecondParameter<typeof customInstance<ApiResponse>>,) => {
      return customInstance<ApiResponse>(
      {url: `/api/admin/dashboard/widgets/layout`, method: 'PUT',
      headers: {'Content-Type': 'application/json', },
      data: dashboardWidgetLayoutSaveRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 메뉴 바로가기 노출 설정 저장
 * - API 분류: 피그마/프론트 화면에서 사용하는 화면 API
 * - 사용하는 화면: 대시보드 (`SCR_DASHBOARD`)
 * - 프론트 담당 영역: 대시보드 (`dashboard`)
 * - 호출 방식: `PUT /api/admin/dashboard/shortcuts/visibility`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 폼 상태 or 선택 행 action payload
 * - 응답 사용 위치: 변경 결과 then 관련 조회 키 갱신
 * - 프론트 조회 키: `put_admin_dashboard_shortcuts_visibility`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: STAGING_VERIFY_REQUIRED
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: REQUIRED
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 dashboard API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: DASHBOARD_WRITE 권한 필요
 * - 접근 범위: 관리자 CMS 권한 범위
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: 기본 마스킹 응답
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: -
 * @summary 메뉴 바로가기 노출 설정 저장
 */
const saveDashboardShortcutVisibility = (
    dashboardShortcutVisibilitySaveRequest?: DashboardShortcutVisibilitySaveRequest,
 options?: SecondParameter<typeof customInstance<ApiResponse>>,) => {
      return customInstance<ApiResponse>(
      {url: `/api/admin/dashboard/shortcuts/visibility`, method: 'PUT',
      headers: {'Content-Type': 'application/json', },
      data: dashboardShortcutVisibilitySaveRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 대시보드 개인화 설정 조회
 * - API 분류: 피그마/프론트 화면에서 사용하는 화면 API
 * - 사용하는 화면: 관리자 대시보드 (`SCR_DASHBOARD`)
 * - 프론트 담당 영역: 대시보드 (`dashboard`)
 * - 호출 방식: `GET /api/admin/dashboard/preferences`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 대시보드 진입 시 현재 관리자 계정
 * - 응답 사용 위치: 대시보드 설정 모달, 바로가기 표시 여부, 위젯 순서/너비, 위젯별 프로그램 필터 복원
 * - 프론트 조회 키: `get_admin_dashboard_preferences`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 라우트 준비 완료
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: STAGE163_DASHBOARD_PREFERENCES_SMOKE
 * - 목데이터 대체: dashboard-settings와 dashboard-widget-order localStorage 초기값을 API 상태로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: DASHBOARD_READ 권한 필요
 * - 접근 범위: 관리자 CMS 권한 범위
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: 개인정보 없음
 * - 감사로그 저장: 필수 아님
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 조회 API는 응답 원본 status/code 값을 화면 배지 라벨과 분리해서 보관합니다. 라벨은 프론트 표시용, 원본 값은 후속 API 호출 조건으로 사용합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: 프론트 dashboard-settings/dashboard-widget-order 저장소 구조와 호환되는 계정별 설정 조회 API
 * @summary 대시보드 개인화 설정 조회
 */
const dashboardPreferences = (

 options?: SecondParameter<typeof customInstance<DashboardPreferencesResponse>>,) => {
      return customInstance<DashboardPreferencesResponse>(
      {url: `/api/admin/dashboard/preferences`, method: 'GET'
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 대시보드 개인화 설정 저장
 * - API 분류: 피그마/프론트 화면에서 사용하는 화면 API
 * - 사용하는 화면: 관리자 대시보드 (`SCR_DASHBOARD`)
 * - 프론트 담당 영역: 대시보드 (`dashboard`)
 * - 호출 방식: `PUT /api/admin/dashboard/preferences`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 설정 모달 저장, 위젯 드래그 종료, 위젯 너비 변경
 * - 응답 사용 위치: 저장 성공 후 대시보드 개인화 상태 캐시 갱신
 * - 프론트 조회 키: `save_admin_dashboard_preferences`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 라우트 준비 완료
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: STAGE163_DASHBOARD_PREFERENCES_SMOKE
 * - 목데이터 대체: dashboard-settings와 dashboard-widget-order localStorage 변경값을 API 저장으로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: DASHBOARD_WRITE 권한 필요
 * - 접근 범위: 관리자 CMS 권한 범위
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: 개인정보 없음
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: 프론트 localStorage 기반 대시보드 설정을 백엔드 계정별 저장값으로 전환
 * @summary 대시보드 개인화 설정 저장
 */
const saveDashboardPreferences = (
    dashboardPreferencesSaveRequest?: DashboardPreferencesSaveRequest,
 options?: SecondParameter<typeof customInstance<ApiResponse>>,) => {
      return customInstance<ApiResponse>(
      {url: `/api/admin/dashboard/preferences`, method: 'PUT',
      headers: {'Content-Type': 'application/json', },
      data: dashboardPreferencesSaveRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 관리자별 위젯 레이아웃 조회
 * - API 분류: 피그마/프론트 화면에서 사용하는 화면 API
 * - 사용하는 화면: 대시보드 (`SCR_DASHBOARD`)
 * - 프론트 담당 영역: 대시보드 (`dashboard`)
 * - 호출 방식: `GET /api/admin/dashboard/widgets`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 필터/페이지네이션/선택 행에서 요청값 전달
 * - 응답 사용 위치: 조회 캐시 및 화면 목록·상세 상태 갱신
 * - 프론트 조회 키: `get_admin_dashboard_widgets`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: STAGING_VERIFY_REQUIRED
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: REQUIRED
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 dashboard API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: DASHBOARD_READ 권한 필요
 * - 접근 범위: 관리자 CMS 권한 범위
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: 개인정보 없음
 * - 감사로그 저장: 필수 아님
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 조회 API는 응답 원본 status/code 값을 화면 배지 라벨과 분리해서 보관합니다. 라벨은 프론트 표시용, 원본 값은 후속 API 호출 조건으로 사용합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: -
 * @summary 관리자별 위젯 레이아웃 조회
 */
const dashboardWidgets = (

 options?: SecondParameter<typeof customInstance<DashboardWidgetListResponse>>,) => {
      return customInstance<DashboardWidgetListResponse>(
      {url: `/api/admin/dashboard/widgets`, method: 'GET'
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 메뉴 바로가기 조회
 * - API 분류: 피그마/프론트 화면에서 사용하는 화면 API
 * - 사용하는 화면: 대시보드 (`SCR_DASHBOARD`)
 * - 프론트 담당 영역: 대시보드 (`dashboard`)
 * - 호출 방식: `GET /api/admin/dashboard/shortcuts`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 필터/페이지네이션/선택 행에서 요청값 전달
 * - 응답 사용 위치: 조회 캐시 및 화면 목록·상세 상태 갱신
 * - 프론트 조회 키: `get_admin_dashboard_shortcuts`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: STAGING_VERIFY_REQUIRED
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: REQUIRED
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 dashboard API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: DASHBOARD_READ 권한 필요
 * - 접근 범위: 관리자 CMS 권한 범위
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: 개인정보 없음
 * - 감사로그 저장: 필수 아님
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 조회 API는 응답 원본 status/code 값을 화면 배지 라벨과 분리해서 보관합니다. 라벨은 프론트 표시용, 원본 값은 후속 API 호출 조건으로 사용합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: -
 * @summary 메뉴 바로가기 조회
 */
const dashboardShortcuts = (

 options?: SecondParameter<typeof customInstance<DashboardShortcutListResponse>>,) => {
      return customInstance<DashboardShortcutListResponse>(
      {url: `/api/admin/dashboard/shortcuts`, method: 'GET'
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 모집 신청 현황 위젯 조회
 * - API 분류: 피그마/프론트 화면에서 사용하는 화면 API
 * - 사용하는 화면: 대시보드 (`SCR_DASHBOARD`)
 * - 프론트 담당 영역: 대시보드 (`dashboard`)
 * - 호출 방식: `GET /api/admin/dashboard/recruitments`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 필터/페이지네이션/선택 행에서 요청값 전달
 * - 응답 사용 위치: 조회 캐시 및 화면 목록·상세 상태 갱신
 * - 프론트 조회 키: `get_admin_dashboard_recruitments`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: STAGING_VERIFY_REQUIRED
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: REQUIRED
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 dashboard API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: DASHBOARD_READ 권한 필요
 * - 접근 범위: 관리자 CMS 권한 범위
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: 개인정보 없음
 * - 감사로그 저장: 필수 아님
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 조회 API는 응답 원본 status/code 값을 화면 배지 라벨과 분리해서 보관합니다. 라벨은 프론트 표시용, 원본 값은 후속 API 호출 조건으로 사용합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: -
 * @summary 모집 신청 현황 위젯 조회
 */
const dashboardRecruitments = (
    params: DashboardRecruitmentsParams,
 options?: SecondParameter<typeof customInstance<DashboardRecruitmentListResponse>>,) => {
      return customInstance<DashboardRecruitmentListResponse>(
      {url: `/api/admin/dashboard/recruitments`, method: 'GET',
        params
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 프로그램 일정 위젯 조회
 * - API 분류: 피그마/프론트 화면에서 사용하는 화면 API
 * - 사용하는 화면: 대시보드 (`SCR_DASHBOARD`)
 * - 프론트 담당 영역: 대시보드 (`dashboard`)
 * - 호출 방식: `GET /api/admin/dashboard/program-schedules`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 필터/페이지네이션/선택 행에서 요청값 전달
 * - 응답 사용 위치: 조회 캐시 및 화면 목록·상세 상태 갱신
 * - 프론트 조회 키: `get_admin_dashboard_program-schedules`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: STAGING_VERIFY_REQUIRED
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: REQUIRED
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 dashboard API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: DASHBOARD_READ 권한 필요
 * - 접근 범위: 관리자 CMS 권한 범위
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: 개인정보 없음
 * - 감사로그 저장: 필수 아님
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 조회 API는 응답 원본 status/code 값을 화면 배지 라벨과 분리해서 보관합니다. 라벨은 프론트 표시용, 원본 값은 후속 API 호출 조건으로 사용합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: -
 * @summary 프로그램 일정 위젯 조회
 */
const dashboardProgramSchedules = (
    params: DashboardProgramSchedulesParams,
 options?: SecondParameter<typeof customInstance<DashboardProgramScheduleListResponse>>,) => {
      return customInstance<DashboardProgramScheduleListResponse>(
      {url: `/api/admin/dashboard/program-schedules`, method: 'GET',
        params
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 프로그램 문의 현황 위젯 조회
 * - API 분류: 피그마/프론트 화면에서 사용하는 화면 API
 * - 사용하는 화면: 공지/FAQ/문의 (`SCR_CONTENT`)
 * - 프론트 담당 영역: content-inquiries (`content-inquiries`)
 * - 호출 방식: `GET /api/admin/dashboard/program-inquiries`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 필터/페이지네이션/선택 행에서 요청값 전달
 * - 응답 사용 위치: 조회 캐시 및 화면 목록·상세 상태 갱신
 * - 프론트 조회 키: `get_admin_dashboard_program-inquiries`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 라우트 준비 완료
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 기본 스모크 검증 대상
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 content-inquiries API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: DASHBOARD_READ 권한 필요
 * - 접근 범위: 관리자 CMS 권한 범위
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: 개인정보 없음
 * - 감사로그 저장: 필수 아님
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 조회 API는 응답 원본 status/code 값을 화면 배지 라벨과 분리해서 보관합니다. 라벨은 프론트 표시용, 원본 값은 후속 API 호출 조건으로 사용합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: -
 * @summary 프로그램 문의 현황 위젯 조회
 */
const dashboardProgramInquiries = (
    params: DashboardProgramInquiriesParams,
 options?: SecondParameter<typeof customInstance<DashboardProgramInquiryListResponse>>,) => {
      return customInstance<DashboardProgramInquiryListResponse>(
      {url: `/api/admin/dashboard/program-inquiries`, method: 'GET',
        params
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 대시보드 알림 count 조회
 * - API 분류: 피그마/프론트 화면에서 사용하는 화면 API
 * - 사용하는 화면: 알림/발송관리 (`SCR_NOTIFICATION`)
 * - 프론트 담당 영역: notifications (`notifications`)
 * - 호출 방식: `GET /api/admin/dashboard/notifications/count`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 필터/페이지네이션/선택 행에서 요청값 전달
 * - 응답 사용 위치: 조회 캐시 및 화면 목록·상세 상태 갱신 (mock/local provider first, production provider after staging config)
 * - 프론트 조회 키: `get_admin_dashboard_notifications_count`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: STAGING_VERIFY_REQUIRED
 * - 외부 연동 확인: DIRECTSEND_SMS_EMAIL 연동 검증 필요
 * - 스테이징 점검 기준: REQUIRED
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 notifications API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: DASHBOARD_READ 권한 필요
 * - 접근 범위: 관리자 CMS 권한 범위
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: 개인정보 없음
 * - 감사로그 저장: 필수 아님
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 조회 API는 응답 원본 status/code 값을 화면 배지 라벨과 분리해서 보관합니다. 라벨은 프론트 표시용, 원본 값은 후속 API 호출 조건으로 사용합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: -
 * @summary 대시보드 알림 count 조회
 */
const dashboardNotificationCount = (

 options?: SecondParameter<typeof customInstance<NotificationUnreadCountResponse>>,) => {
      return customInstance<NotificationUnreadCountResponse>(
      {url: `/api/admin/dashboard/notifications/count`, method: 'GET'
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - MASTER 전용 로그 알림 요약 조회
 * - API 분류: 피그마/프론트 화면에서 사용하는 화면 API
 * - 사용하는 화면: 대시보드 (`SCR_DASHBOARD`)
 * - 프론트 담당 영역: 대시보드 (`dashboard`)
 * - 호출 방식: `GET /api/admin/dashboard/log-alerts`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 필터/페이지네이션/선택 행에서 요청값 전달
 * - 응답 사용 위치: 조회 캐시 및 화면 목록·상세 상태 갱신
 * - 프론트 조회 키: `get_admin_dashboard_log-alerts`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: STAGING_VERIFY_REQUIRED
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: REQUIRED
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 dashboard API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: DASHBOARD_READ 권한 필요
 * - 접근 범위: 관리자 CMS 권한 범위
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: 개인정보 없음
 * - 감사로그 저장: 필수 아님
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 조회 API는 응답 원본 status/code 값을 화면 배지 라벨과 분리해서 보관합니다. 라벨은 프론트 표시용, 원본 값은 후속 API 호출 조건으로 사용합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: -
 * @summary MASTER 전용 로그 알림 요약 조회
 */
const dashboardLogAlerts = (

 options?: SecondParameter<typeof customInstance<DashboardLogAlertListResponse>>,) => {
      return customInstance<DashboardLogAlertListResponse>(
      {url: `/api/admin/dashboard/log-alerts`, method: 'GET'
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 사업별 KPI 달성률 위젯 조회
 * - API 분류: 피그마/프론트 화면에서 사용하는 화면 API
 * - 사용하는 화면: 대시보드 (`SCR_DASHBOARD`)
 * - 프론트 담당 영역: 대시보드 (`dashboard`)
 * - 호출 방식: `GET /api/admin/dashboard/kpi-progress`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 필터/페이지네이션/선택 행에서 요청값 전달
 * - 응답 사용 위치: 조회 캐시 및 화면 목록·상세 상태 갱신
 * - 프론트 조회 키: `get_admin_dashboard_kpi-progress`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: STAGING_VERIFY_REQUIRED
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: REQUIRED
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 dashboard API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: DASHBOARD_READ 권한 필요
 * - 접근 범위: 관리자 CMS 권한 범위
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: 개인정보 없음
 * - 감사로그 저장: 필수 아님
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 조회 API는 응답 원본 status/code 값을 화면 배지 라벨과 분리해서 보관합니다. 라벨은 프론트 표시용, 원본 값은 후속 API 호출 조건으로 사용합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: -
 * @summary 사업별 KPI 달성률 위젯 조회
 */
const dashboardKpiProgress = (
    params: DashboardKpiProgressParams,
 options?: SecondParameter<typeof customInstance<DashboardKpiProgressListResponse>>,) => {
      return customInstance<DashboardKpiProgressListResponse>(
      {url: `/api/admin/dashboard/kpi-progress`, method: 'GET',
        params
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 대시보드 홈 초기 조회
 * - API 분류: 피그마/프론트 화면에서 사용하는 화면 API
 * - 사용하는 화면: 대시보드 (`SCR_DASHBOARD`)
 * - 프론트 담당 영역: 대시보드 (`dashboard`)
 * - 호출 방식: `GET /api/admin/dashboard/home`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 필터/페이지네이션/선택 행에서 요청값 전달
 * - 응답 사용 위치: 조회 캐시 및 화면 목록·상세 상태 갱신
 * - 프론트 조회 키: `get_admin_dashboard_home`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: STAGING_VERIFY_REQUIRED
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: REQUIRED
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 dashboard API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: DASHBOARD_READ 권한 필요
 * - 접근 범위: 관리자 CMS 권한 범위
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: 개인정보 없음
 * - 감사로그 저장: 필수 아님
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 조회 API는 응답 원본 status/code 값을 화면 배지 라벨과 분리해서 보관합니다. 라벨은 프론트 표시용, 원본 값은 후속 API 호출 조건으로 사용합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: -
 * @summary 대시보드 홈 초기 조회
 */
const dashboardHome = (

 options?: SecondParameter<typeof customInstance<DashboardHomeResponse>>,) => {
      return customInstance<DashboardHomeResponse>(
      {url: `/api/admin/dashboard/home`, method: 'GET'
    },
      options);
    }

return {getWidgetProgramFilters,saveWidgetProgramFilters,saveDashboardWidgetLayout,saveDashboardShortcutVisibility,dashboardPreferences,saveDashboardPreferences,dashboardWidgets,dashboardShortcuts,dashboardRecruitments,dashboardProgramSchedules,dashboardProgramInquiries,dashboardNotificationCount,dashboardLogAlerts,dashboardKpiProgress,dashboardHome}};
export type GetWidgetProgramFiltersResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIDashboardSubset>['getWidgetProgramFilters']>>>
export type SaveWidgetProgramFiltersResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIDashboardSubset>['saveWidgetProgramFilters']>>>
export type SaveDashboardWidgetLayoutResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIDashboardSubset>['saveDashboardWidgetLayout']>>>
export type SaveDashboardShortcutVisibilityResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIDashboardSubset>['saveDashboardShortcutVisibility']>>>
export type DashboardPreferencesResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIDashboardSubset>['dashboardPreferences']>>>
export type SaveDashboardPreferencesResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIDashboardSubset>['saveDashboardPreferences']>>>
export type DashboardWidgetsResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIDashboardSubset>['dashboardWidgets']>>>
export type DashboardShortcutsResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIDashboardSubset>['dashboardShortcuts']>>>
export type DashboardRecruitmentsResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIDashboardSubset>['dashboardRecruitments']>>>
export type DashboardProgramSchedulesResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIDashboardSubset>['dashboardProgramSchedules']>>>
export type DashboardProgramInquiriesResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIDashboardSubset>['dashboardProgramInquiries']>>>
export type DashboardNotificationCountResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIDashboardSubset>['dashboardNotificationCount']>>>
export type DashboardLogAlertsResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIDashboardSubset>['dashboardLogAlerts']>>>
export type DashboardKpiProgressResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIDashboardSubset>['dashboardKpiProgress']>>>
export type DashboardHomeResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIDashboardSubset>['dashboardHome']>>>
