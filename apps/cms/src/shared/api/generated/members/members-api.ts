/**
 * Generated by orval v8.17.0 🍺
 * Do not edit manually.
 * JA Korea CMS Backend API — Members subset
 * Filtered for CMS member management Orval codegen.
 * OpenAPI spec version: v9
 */
import type {
  AccountDirectoryBulkDeleteRequest,
  AdminAccountApprovalDecisionRequest,
  AdminAccountApprovalDetailResponse,
  AdminAccountBasicInfoUpdateRequest,
  AdminAccountCreateRequest,
  AdminAccountPasswordResetRequest,
  AdminAccountPrivacyResponse,
  AdminAccountStatusChangeRequest,
  AdminAccountVerificationRequest,
  AdminApprovalBulkDecisionRequest,
  AdminCommentResponse,
  AdminCommentUpdateRequest,
  AdminMemberBasicInfoUpdateRequest,
  AdminMemberCommentCreateRequest,
  AdminMemberDeleteRequest,
  AdminPermissionResponse,
  AdminPreRegisterIndividualRequest,
  AdminPreRegisterInstructorRequest,
  AdminPreRegisterSchoolRequest,
  AdminPrivacyUnmaskRequest,
  AdminProgramRoleBulkDeleteRequest,
  AdminProgramRoleOptionResponse,
  AdminProgramRoleSaveRequest,
  AdminRoleChangeRequest,
  AdminRolePermissionMatrixResponse,
  AdminRolePermissionUpdateRequest,
  AdminRoleResponse,
  AdminTermsAgreementResponse,
  ApiResponseAccountDirectoryBulkDeleteResponse,
  ApiResponseAdminAccountResponse,
  ApiResponseAdminPermissionRequestNotificationResponse,
  ApiResponseBulkActionResponse,
  ApiResponseVoid,
  ApprovalResetRequest,
  BulkDecisionRequest,
  DeleteAdminParams,
  DeleteApplicationHistoryParams,
  DeleteSchoolParams,
  ExternalIdentifierResponse,
  ExternalIdentifierUpsertRequest,
  FilledDocumentResponse,
  GetApplicationEnrollmentSummaryParams,
  GetLectureAttendanceParams,
  IndividualMemberDetailResponse,
  InstructorDetailResponse,
  InstructorMemberDetailResponse,
  InstructorRoleBulkReviewRequest,
  InstructorRoleRequestDetailResponse,
  InstructorRoleReviewRequest,
  InstructorRoleWorkflowResponse,
  ListAdminApprovalRequestsParams,
  ListAdminsParams,
  ListAllCmsMembersAndAdminsParams,
  ListAssignmentSubmissionsParams,
  ListCommentsParams,
  ListInstructorRoleRequestsParams,
  ListLectureReportsParams,
  ListMemberAdminProgramsParams,
  ListMemberApplicationsParams,
  ListMemberCommentsParams,
  ListMemberProgramHistoryParams,
  ListMembersParams,
  ListPermissionChangeLogsParams,
  ListPermissionsParams,
  ListProgramEnrollmentHistoryParams,
  ListProgramRoles1Params,
  ListProgramRolesParams,
  ListSchoolsParams,
  MemberAssignmentSubmissionResponse,
  MemberConsentRecordResponse,
  MemberDetailResponse,
  MemberEnrollmentSummaryResponse,
  MemberLectureAttendanceResponse,
  MemberLectureReportResponse,
  MemberProgramRoleSaveRequest,
  MemberWorkflowResponse,
  PageResponse,
  PageResponseAccountDirectoryItemResponse,
  PageResponseAdminAccountListItemResponse,
  PageResponseAdminPermissionChangeLogResponse,
  PageResponseAdminProgramAssignmentResponse,
  PageResponseInstructorRoleRequestListItemResponse,
  PageResponseMemberAdminProgramResponse,
  PageResponseMemberApplicationHistoryResponse,
  PageResponseMemberProgramHistoryResponse,
  PageResponseSchoolOrganizationListItemResponse,
  PageResponseSchoolOrganizationProgramEnrollmentHistoryItemResponse,
  PreRegisterConflictResolveRequest,
  PreRegisterConflictResponse,
  ProgramRoleOptionResponse,
  RawPrivacyAvailableActionsResponse,
  SchoolAffiliatedTeacherResponse,
  SchoolMemberDetailResponse,
  SchoolOrganizationListItemResponse,
  SchoolOrganizationUpsertRequest,
  SchoolProgramEnrollmentHistoryBulkDeleteRequest,
  TeacherEmploymentStatusUpdateRequest,
  TeacherMemberDetailResponse,
  UserResponse
} from './schemas';

import { customInstance } from '../../orval-mutator';


type SecondParameter<T extends (...args: never) => unknown> = Parameters<T>[1];


  export const getJAKoreaCMSBackendAPIMembersSubset = () => {
/**
 * ### 이 API가 하는 일
 * - 프로그램 조회
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 화면 직접 호출보다는 운영/진단 또는 내부 처리에서 사용합니다.
 * - 호출 방식: `GET /api/admin/users/{memberId}/program-roles`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: Swagger 요청 폼 또는 화면 필터/선택값
 * - 응답 사용 위치: 응답 본문을 화면 상태와 조회 캐시에 반영
 * - 프론트 조회 키: 화면별 조회 키 정책에 따름
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 준비 상태 정보 없음
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 스테이징 기본 검증 대상
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: ADMIN_READ 권한 필요
 * - 접근 범위: 관리자 CMS 권한 범위
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: 기본 마스킹 응답
 * - 감사로그 저장: 필수 아님
 *
 * ### 상태값/화면 배지 기준
 * - 조회 API는 응답 원본 status/code 값을 화면 배지 라벨과 분리해서 보관합니다. 라벨은 프론트 표시용, 원본 값은 후속 API 호출 조건으로 사용합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Stage327 회원관리/대시보드 피그마 및 프론트 핸드오프 반영
 * @deprecated
 * @summary 프로그램 조회
 */
const listProgramRoles = (
    memberId: number,
    params?: ListProgramRolesParams,
 options?: SecondParameter<typeof customInstance<PageResponseMemberAdminProgramResponse>>,) => {
      return customInstance<PageResponseMemberAdminProgramResponse>(
      {url: `/api/admin/users/${memberId}/program-roles`, method: 'GET',
        params
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - PUT /api/admin/users/{memberId}/program-roles 구현 API
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 화면 직접 호출보다는 운영/진단 또는 내부 처리에서 사용합니다.
 * - 호출 방식: `PUT /api/admin/users/{memberId}/program-roles`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: Swagger 요청 폼 또는 화면 필터/선택값
 * - 응답 사용 위치: 응답 본문을 화면 상태와 조회 캐시에 반영
 * - 프론트 조회 키: 화면별 조회 키 정책에 따름
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 준비 상태 정보 없음
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 스테이징 기본 검증 대상
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: ADMIN_WRITE 권한 필요
 * - 접근 범위: 관리자 CMS 권한 범위
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: 기본 마스킹 응답
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Stage327 회원관리/대시보드 피그마 및 프론트 핸드오프 반영
 * @deprecated
 * @summary PUT /api/admin/users/{memberId}/program-roles 구현 API
 */
const saveProgramRoles = (
    memberId: number,
    memberProgramRoleSaveRequest: MemberProgramRoleSaveRequest,
 options?: SecondParameter<typeof customInstance<PageResponseMemberAdminProgramResponse>>,) => {
      return customInstance<PageResponseMemberAdminProgramResponse>(
      {url: `/api/admin/users/${memberId}/program-roles`, method: 'PUT',
      headers: {'Content-Type': 'application/json', },
      data: memberProgramRoleSaveRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 화면/운영에서 사용하는 구현 API
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 회원 권한/관리자 권한 (`null`)
 * - 프론트 담당 영역: 관리자-permissions (`admin-permissions`)
 * - 호출 방식: `GET /api/admin/admin-roles/{roleCode}/permissions`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 필터/페이지네이션/선택 행에서 요청값 전달
 * - 응답 사용 위치: 조회 캐시 및 화면 목록·상세 상태 갱신
 * - 프론트 조회 키: `get_admin_admin-roles_roleCode_permissions`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 라우트 준비 완료
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 기본 스모크 검증 대상
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 admin-permissions API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: ADMIN_PERMISSION_READ 권한 필요
 * - 접근 범위: 관리자 CMS 권한 범위
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: 개인정보 없음
 * - 감사로그 저장: 필수 아님
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 조회 API는 응답 원본 status/code 값을 화면 배지 라벨과 분리해서 보관합니다. 라벨은 프론트 표시용, 원본 값은 후속 API 호출 조건으로 사용합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: v2.8.0 P1 폼/런타임 및 마스터 워크플로우 기준 구현 완료
 * @summary 화면/운영에서 사용하는 구현 API
 */
const getRolePermissions = (
    roleCode: string,
 options?: SecondParameter<typeof customInstance<AdminRolePermissionMatrixResponse>>,) => {
      return customInstance<AdminRolePermissionMatrixResponse>(
      {url: `/api/admin/admin-roles/${roleCode}/permissions`, method: 'GET'
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 화면/운영에서 사용하는 구현 API
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 회원 권한/관리자 권한 (`null`)
 * - 프론트 담당 영역: 관리자-permissions (`admin-permissions`)
 * - 호출 방식: `PUT /api/admin/admin-roles/{roleCode}/permissions`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 폼 상태 or 선택 행 action payload
 * - 응답 사용 위치: 변경 결과 then 관련 조회 키 갱신
 * - 프론트 조회 키: `put_admin_admin-roles_roleCode_permissions`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 라우트 준비 완료
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 기본 스모크 검증 대상
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 admin-permissions API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: ADMIN_PERMISSION_WRITE 권한 필요
 * - 접근 범위: 마스터 관리자만 가능
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: 개인정보 없음
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: v2.8.0 P1 폼/런타임 및 마스터 워크플로우 기준 구현 완료
 * @summary 화면/운영에서 사용하는 구현 API
 */
const updateRolePermissions = (
    roleCode: string,
    adminRolePermissionUpdateRequest: AdminRolePermissionUpdateRequest,
 options?: SecondParameter<typeof customInstance<ApiResponseVoid>>,) => {
      return customInstance<ApiResponseVoid>(
      {url: `/api/admin/admin-roles/${roleCode}/permissions`, method: 'PUT',
      headers: {'Content-Type': 'application/json', },
      data: adminRolePermissionUpdateRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - CMS 관리자 프로그램 담당 역할 목록 조회
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 화면 직접 호출보다는 운영/진단 또는 내부 처리에서 사용합니다.
 * - 호출 방식: `GET /api/admin/admin-accounts/{adminAccountId}/program-roles`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: Swagger 요청 폼 또는 화면 필터/선택값
 * - 응답 사용 위치: 응답 본문을 화면 상태와 조회 캐시에 반영
 * - 프론트 조회 키: 화면별 조회 키 정책에 따름
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 준비 상태 정보 없음
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 스테이징 기본 검증 대상
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: ADMIN_READ 권한 필요
 * - 접근 범위: 관리자 CMS 권한 범위
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: 기본 마스킹 응답
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 조회 API는 응답 원본 status/code 값을 화면 배지 라벨과 분리해서 보관합니다. 라벨은 프론트 표시용, 원본 값은 후속 API 호출 조건으로 사용합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: 회원 memberId/e-mail 브리지가 아닌 admin_account.id canonical 경계
 * @summary CMS 관리자 프로그램 담당 역할 목록 조회
 */
const listProgramRoles1 = (
    adminAccountId: number,
    params?: ListProgramRoles1Params,
 options?: SecondParameter<typeof customInstance<PageResponseAdminProgramAssignmentResponse>>,) => {
      return customInstance<PageResponseAdminProgramAssignmentResponse>(
      {url: `/api/admin/admin-accounts/${adminAccountId}/program-roles`, method: 'GET',
        params
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - CMS 관리자 프로그램 담당 역할 일괄 저장
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 화면 직접 호출보다는 운영/진단 또는 내부 처리에서 사용합니다.
 * - 호출 방식: `PUT /api/admin/admin-accounts/{adminAccountId}/program-roles`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: Swagger 요청 폼 또는 화면 필터/선택값
 * - 응답 사용 위치: 응답 본문을 화면 상태와 조회 캐시에 반영
 * - 프론트 조회 키: 화면별 조회 키 정책에 따름
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 준비 상태 정보 없음
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 스테이징 기본 검증 대상
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: ADMIN_WRITE 권한 필요
 * - 접근 범위: 관리자 CMS 권한 범위
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: 기본 마스킹 응답
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: admin_account.id canonical write boundary
 * @summary CMS 관리자 프로그램 담당 역할 일괄 저장
 */
const saveProgramRoles1 = (
    adminAccountId: number,
    adminProgramRoleSaveRequest: AdminProgramRoleSaveRequest,
 options?: SecondParameter<typeof customInstance<PageResponseAdminProgramAssignmentResponse>>,) => {
      return customInstance<PageResponseAdminProgramAssignmentResponse>(
      {url: `/api/admin/admin-accounts/${adminAccountId}/program-roles`, method: 'PUT',
      headers: {'Content-Type': 'application/json', },
      data: adminProgramRoleSaveRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - POST /api/admin/users/{memberId}/privacy/unmask
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 회원 관리 (`null`)
 * - 프론트 담당 영역: members (`members`)
 * - 호출 방식: `POST /api/admin/users/{memberId}/privacy/unmask`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 폼 상태 or 선택 행 action payload
 * - 응답 사용 위치: 변경 결과 then 관련 조회 키 갱신
 * - 프론트 조회 키: `post_admin_members_memberId_privacy_unmask`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 라우트 준비 완료
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 기본 스모크 검증 대상
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 members API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 개인정보/파일 API는 마스킹 응답과 원문 접근을 구분합니다. 원문 조회, export, 민감파일 다운로드는 감사로그가 저장되어야 성공으로 취급합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary POST /api/admin/users/{memberId}/privacy/unmask
 */
const unmaskMemberPrivacy = (
    memberId: number,
    adminPrivacyUnmaskRequest: AdminPrivacyUnmaskRequest,
 options?: SecondParameter<typeof customInstance<MemberDetailResponse>>,) => {
      return customInstance<MemberDetailResponse>(
      {url: `/api/admin/users/${memberId}/privacy/unmask`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: adminPrivacyUnmaskRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 강사 회원 역할 상세 원문 조회
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 회원 관리 (`null`)
 * - 프론트 담당 영역: members (`members`)
 * - 호출 방식: `POST /api/admin/users/{memberId}/instructor/privacy/unmask`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 역할별 등록 폼 또는 선택 회원 ID
 * - 응답 사용 위치: mutation 성공 후 회원 목록·상세 query invalidate
 * - 프론트 조회 키: `post_api_admin_users_memberId_instructor_privacy_unmask`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 라우트 준비 완료
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: MEMBER_HANDOFF_E2E
 * - 목데이터 대체: 회원 역할별 mock/localStorage 데이터를 canonical API 상태로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: PRIVACY_RAW_READ 권한 필요
 * - 접근 범위: 관리자 CMS 권한 범위
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: PERSONAL_DATA 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 개인정보/파일 API는 마스킹 응답과 원문 접근을 구분합니다. 원문 조회, export, 민감파일 다운로드는 감사로그가 저장되어야 성공으로 취급합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: FE 회원 관리 핸드오프 2026-07-23 역할별 canonical 계약
 * @summary 강사 회원 역할 상세 원문 조회
 */
const unmaskInstructorMemberPrivacy = (
    memberId: number,
    adminPrivacyUnmaskRequest: AdminPrivacyUnmaskRequest,
 options?: SecondParameter<typeof customInstance<InstructorMemberDetailResponse>>,) => {
      return customInstance<InstructorMemberDetailResponse>(
      {url: `/api/admin/users/${memberId}/instructor/privacy/unmask`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: adminPrivacyUnmaskRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 강사 개인정보 원문 조회
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 화면 직접 호출보다는 운영/진단 또는 내부 처리에서 사용합니다.
 * - 호출 방식: `POST /api/admin/users/{memberId}/instructor-profile/privacy/unmask`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: Swagger 요청 폼 또는 화면 필터/선택값
 * - 응답 사용 위치: 응답 본문을 화면 상태와 조회 캐시에 반영
 * - 프론트 조회 키: 화면별 조회 키 정책에 따름
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 준비 상태 정보 없음
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 스테이징 기본 검증 대상
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: PRIVACY_RAW_READ 권한 필요
 * - 접근 범위: 관리자 CMS 권한 범위
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: RAW_PRIVACY 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 개인정보/파일 API는 마스킹 응답과 원문 접근을 구분합니다. 원문 조회, export, 민감파일 다운로드는 감사로그가 저장되어야 성공으로 취급합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Stage008 instructor raw privacy unmask contract
 * @summary 강사 개인정보 원문 조회
 */
const unmaskInstructorPrivacy = (
    memberId: number,
    adminPrivacyUnmaskRequest: AdminPrivacyUnmaskRequest,
 options?: SecondParameter<typeof customInstance<InstructorDetailResponse>>,) => {
      return customInstance<InstructorDetailResponse>(
      {url: `/api/admin/users/${memberId}/instructor-profile/privacy/unmask`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: adminPrivacyUnmaskRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 개인 회원 역할 상세 원문 조회
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 회원 관리 (`null`)
 * - 프론트 담당 영역: members (`members`)
 * - 호출 방식: `POST /api/admin/users/{memberId}/individual/privacy/unmask`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 역할별 등록 폼 또는 선택 회원 ID
 * - 응답 사용 위치: mutation 성공 후 회원 목록·상세 query invalidate
 * - 프론트 조회 키: `post_api_admin_users_memberId_individual_privacy_unmask`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 라우트 준비 완료
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: MEMBER_HANDOFF_E2E
 * - 목데이터 대체: 회원 역할별 mock/localStorage 데이터를 canonical API 상태로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: PRIVACY_RAW_READ 권한 필요
 * - 접근 범위: 관리자 CMS 권한 범위
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: PERSONAL_DATA 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 개인정보/파일 API는 마스킹 응답과 원문 접근을 구분합니다. 원문 조회, export, 민감파일 다운로드는 감사로그가 저장되어야 성공으로 취급합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: FE 회원 관리 핸드오프 2026-07-23 역할별 canonical 계약
 * @summary 개인 회원 역할 상세 원문 조회
 */
const unmaskIndividualMemberPrivacy = (
    memberId: number,
    adminPrivacyUnmaskRequest: AdminPrivacyUnmaskRequest,
 options?: SecondParameter<typeof customInstance<IndividualMemberDetailResponse>>,) => {
      return customInstance<IndividualMemberDetailResponse>(
      {url: `/api/admin/users/${memberId}/individual/privacy/unmask`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: adminPrivacyUnmaskRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - POST /api/admin/users/{memberId}/delete
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 회원 관리 (`null`)
 * - 프론트 담당 영역: members (`members`)
 * - 호출 방식: `POST /api/admin/users/{memberId}/delete`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 폼 상태 or 선택 행 action payload
 * - 응답 사용 위치: 변경 결과 then 관련 조회 키 갱신
 * - 프론트 조회 키: `post_admin_members_memberId_delete`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 라우트 준비 완료
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 기본 스모크 검증 대상
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 members API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary POST /api/admin/users/{memberId}/delete
 */
const deleteAndAnonymize = (
    memberId: number,
    adminMemberDeleteRequest: AdminMemberDeleteRequest,
 options?: SecondParameter<typeof customInstance<MemberWorkflowResponse>>,) => {
      return customInstance<MemberWorkflowResponse>(
      {url: `/api/admin/users/${memberId}/delete`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: adminMemberDeleteRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 회원 동의서 작성본 원문 조회
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 화면 직접 호출보다는 운영/진단 또는 내부 처리에서 사용합니다.
 * - 호출 방식: `POST /api/admin/users/{memberId}/consent-records/{consentType}/filled-document`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: Swagger 요청 폼 또는 화면 필터/선택값
 * - 응답 사용 위치: 응답 본문을 화면 상태와 조회 캐시에 반영
 * - 프론트 조회 키: 화면별 조회 키 정책에 따름
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 준비 상태 정보 없음
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 스테이징 기본 검증 대상
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: MEMBER_READ+DYNAMIC_PRIVACY_PERMISSION 권한 필요
 * - 접근 범위: 관리자 CMS 권한 범위
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: 개인정보 원문 접근 가능 API
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: WritingFormDraft/paymentBasicInfo exact snapshot reveal; payment requires PRIVACY_ACCOUNT_READ, others PRIVACY_RAW_READ, reason audited
 * @summary 회원 동의서 작성본 원문 조회
 */
const consentFilledDocument = (
    memberId: number,
    consentType: string,
    adminPrivacyUnmaskRequest: AdminPrivacyUnmaskRequest,
 options?: SecondParameter<typeof customInstance<FilledDocumentResponse>>,) => {
      return customInstance<FilledDocumentResponse>(
      {url: `/api/admin/users/${memberId}/consent-records/${consentType}/filled-document`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: adminPrivacyUnmaskRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 회원/강사 조회
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 회원 관리 (`null`)
 * - 프론트 담당 영역: members (`members`)
 * - 호출 방식: `GET /api/admin/users/{memberId}/comments`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 선택 회원 ID 및 선택 화면Code
 * - 응답 사용 위치: 상세 화면 관리자 코멘트 영역 렌더링
 * - 프론트 조회 키: `get_admin_members_memberId_comments`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 라우트 준비 완료
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 기본 스모크 검증 대상
 * - 목데이터 대체: 학교/회원 상세 관리자 코멘트 mock 표시를 API 상태로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 조회 API는 응답 원본 status/code 값을 화면 배지 라벨과 분리해서 보관합니다. 라벨은 프론트 표시용, 원본 값은 후속 API 호출 조건으로 사용합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary 회원/강사 조회
 */
const listMemberComments = (
    memberId: number,
    params?: ListMemberCommentsParams,
 options?: SecondParameter<typeof customInstance<AdminCommentResponse[]>>,) => {
      return customInstance<AdminCommentResponse[]>(
      {url: `/api/admin/users/${memberId}/comments`, method: 'GET',
        params
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - POST /api/admin/users/{memberId}/comments
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 회원 관리 (`null`)
 * - 프론트 담당 영역: members (`members`)
 * - 호출 방식: `POST /api/admin/users/{memberId}/comments`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 코멘트 입력 폼
 * - 응답 사용 위치: 등록 후 상세 코멘트 query invalidate
 * - 프론트 조회 키: `post_admin_members_memberId_comments`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 라우트 준비 완료
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 기본 스모크 검증 대상
 * - 목데이터 대체: 학교/회원 상세 관리자 코멘트 mock 표시를 API 상태로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary POST /api/admin/users/{memberId}/comments
 */
const createMemberComment = (
    memberId: number,
    adminMemberCommentCreateRequest: AdminMemberCommentCreateRequest,
 options?: SecondParameter<typeof customInstance<AdminCommentResponse>>,) => {
      return customInstance<AdminCommentResponse>(
      {url: `/api/admin/users/${memberId}/comments`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: adminMemberCommentCreateRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 학교·기관 회원 사전 등록
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 회원 관리 (`null`)
 * - 프론트 담당 영역: members (`members`)
 * - 호출 방식: `POST /api/admin/users/pre-register/school`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 역할별 등록 폼 또는 선택 회원 ID
 * - 응답 사용 위치: mutation 성공 후 회원 목록·상세 query invalidate
 * - 프론트 조회 키: `post_api_admin_users_pre_register_school`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 라우트 준비 완료
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: MEMBER_HANDOFF_E2E
 * - 목데이터 대체: 회원 역할별 mock/localStorage 데이터를 canonical API 상태로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: MEMBER_WRITE 권한 필요
 * - 접근 범위: 관리자 CMS 권한 범위
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: PERSONAL_DATA 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: FE 회원 관리 핸드오프 2026-07-23 역할별 canonical 계약
 * @deprecated
 * @summary 학교·기관 회원 사전 등록
 */
const preRegisterSchool = (
    adminPreRegisterSchoolRequest: AdminPreRegisterSchoolRequest,
 options?: SecondParameter<typeof customInstance<SchoolOrganizationListItemResponse>>,) => {
      return customInstance<SchoolOrganizationListItemResponse>(
      {url: `/api/admin/users/pre-register/school`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: adminPreRegisterSchoolRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 강사 회원 사전 등록
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 회원 관리 (`null`)
 * - 프론트 담당 영역: members (`members`)
 * - 호출 방식: `POST /api/admin/users/pre-register/instructor`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 역할별 등록 폼 또는 선택 회원 ID
 * - 응답 사용 위치: mutation 성공 후 회원 목록·상세 query invalidate
 * - 프론트 조회 키: `post_api_admin_users_pre_register_instructor`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 라우트 준비 완료
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: MEMBER_HANDOFF_E2E
 * - 목데이터 대체: 회원 역할별 mock/localStorage 데이터를 canonical API 상태로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: MEMBER_WRITE 권한 필요
 * - 접근 범위: 관리자 CMS 권한 범위
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: PERSONAL_DATA 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: FE 회원 관리 핸드오프 2026-07-23 역할별 canonical 계약
 * @summary 강사 회원 사전 등록
 */
const preRegisterInstructor = (
    adminPreRegisterInstructorRequest: AdminPreRegisterInstructorRequest,
 options?: SecondParameter<typeof customInstance<MemberWorkflowResponse>>,) => {
      return customInstance<MemberWorkflowResponse>(
      {url: `/api/admin/users/pre-register/instructor`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: adminPreRegisterInstructorRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 개인 회원 사전 등록
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 회원 관리 (`null`)
 * - 프론트 담당 영역: members (`members`)
 * - 호출 방식: `POST /api/admin/users/pre-register/individual`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 역할별 등록 폼 또는 선택 회원 ID
 * - 응답 사용 위치: mutation 성공 후 회원 목록·상세 query invalidate
 * - 프론트 조회 키: `post_api_admin_users_pre_register_individual`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 라우트 준비 완료
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: MEMBER_HANDOFF_E2E
 * - 목데이터 대체: 회원 역할별 mock/localStorage 데이터를 canonical API 상태로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: MEMBER_WRITE 권한 필요
 * - 접근 범위: 관리자 CMS 권한 범위
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: PERSONAL_DATA 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: FE 회원 관리 핸드오프 2026-07-23 역할별 canonical 계약
 * @summary 개인 회원 사전 등록
 */
const preRegisterIndividual = (
    adminPreRegisterIndividualRequest: AdminPreRegisterIndividualRequest,
 options?: SecondParameter<typeof customInstance<MemberWorkflowResponse>>,) => {
      return customInstance<MemberWorkflowResponse>(
      {url: `/api/admin/users/pre-register/individual`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: adminPreRegisterIndividualRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - POST /api/admin/users/pre-register-conflicts/{conflictId}/resolve
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 회원 관리 (`null`)
 * - 프론트 담당 영역: members (`members`)
 * - 호출 방식: `POST /api/admin/users/pre-register-conflicts/{conflictId}/resolve`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 폼 상태 or 선택 행 action payload
 * - 응답 사용 위치: 변경 결과 then 관련 조회 키 갱신
 * - 프론트 조회 키: `post_admin_members_pre-register-conflicts_conflictId_resolve`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 라우트 준비 완료
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 기본 스모크 검증 대상
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 members API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary POST /api/admin/users/pre-register-conflicts/{conflictId}/resolve
 */
const resolvePreRegisterConflict = (
    conflictId: number,
    preRegisterConflictResolveRequest: PreRegisterConflictResolveRequest,
 options?: SecondParameter<typeof customInstance<MemberWorkflowResponse>>,) => {
      return customInstance<MemberWorkflowResponse>(
      {url: `/api/admin/users/pre-register-conflicts/${conflictId}/resolve`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: preRegisterConflictResolveRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - POST /api/admin/users/bulk-delete
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 화면 직접 호출보다는 운영/진단 또는 내부 처리에서 사용합니다.
 * - 호출 방식: `POST /api/admin/users/bulk-delete`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: Swagger 요청 폼 또는 화면 필터/선택값
 * - 응답 사용 위치: 응답 본문을 화면 상태와 조회 캐시에 반영
 * - 프론트 조회 키: 화면별 조회 키 정책에 따름
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 준비 상태 정보 없음
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 스테이징 기본 검증 대상
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: MEMBER_WRITE 권한 필요
 * - 접근 범위: GLOBAL 범위 정책
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: PII_ANONYMIZATION 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Frontend bulk member anonymization handoff
 * @summary POST /api/admin/users/bulk-delete
 */
const bulkDeleteAndAnonymize = (
    bulkDecisionRequest: BulkDecisionRequest,
 options?: SecondParameter<typeof customInstance<ApiResponseBulkActionResponse>>,) => {
      return customInstance<ApiResponseBulkActionResponse>(
      {url: `/api/admin/users/bulk-delete`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: bulkDecisionRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - CMS 학교 organization 목록 조회
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 화면 직접 호출보다는 운영/진단 또는 내부 처리에서 사용합니다.
 * - 호출 방식: `GET /api/admin/organizations/schools`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: Swagger 요청 폼 또는 화면 필터/선택값
 * - 응답 사용 위치: 응답 본문을 화면 상태와 조회 캐시에 반영
 * - 프론트 조회 키: 화면별 조회 키 정책에 따름
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 준비 상태 정보 없음
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 스테이징 기본 검증 대상
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: MEMBER_READ 권한 필요
 * - 접근 범위: CMS_SCHOOL_ORGANIZATION 범위 정책
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: NONE 개인정보 정책
 * - 감사로그 저장: 필수 아님
 *
 * ### 상태값/화면 배지 기준
 * - 조회 API는 응답 원본 status/code 값을 화면 배지 라벨과 분리해서 보관합니다. 라벨은 프론트 표시용, 원본 값은 후속 API 호출 조건으로 사용합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: 학교는 member가 아니라 organizationId canonical resource; CMS admin role과 member role 분리
 * @summary CMS 학교 organization 목록 조회
 */
const listSchools = (
    params?: ListSchoolsParams,
 options?: SecondParameter<typeof customInstance<PageResponseSchoolOrganizationListItemResponse>>,) => {
      return customInstance<PageResponseSchoolOrganizationListItemResponse>(
      {url: `/api/admin/organizations/schools`, method: 'GET',
        params
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - CMS 학교 organization 등록
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 화면 직접 호출보다는 운영/진단 또는 내부 처리에서 사용합니다.
 * - 호출 방식: `POST /api/admin/organizations/schools`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: Swagger 요청 폼 또는 화면 필터/선택값
 * - 응답 사용 위치: 응답 본문을 화면 상태와 조회 캐시에 반영
 * - 프론트 조회 키: 화면별 조회 키 정책에 따름
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 준비 상태 정보 없음
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 스테이징 기본 검증 대상
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: MEMBER_WRITE 권한 필요
 * - 접근 범위: CMS_SCHOOL_ORGANIZATION 범위 정책
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: NONE 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: 학교는 member가 아니며 organization만 생성; synthetic SCHOOL member 금지
 * @summary CMS 학교 organization 등록
 */
const createSchool = (
    schoolOrganizationUpsertRequest: SchoolOrganizationUpsertRequest,
 options?: SecondParameter<typeof customInstance<SchoolOrganizationListItemResponse>>,) => {
      return customInstance<SchoolOrganizationListItemResponse>(
      {url: `/api/admin/organizations/schools`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: schoolOrganizationUpsertRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 학교 프로젝트 수강 이력 일괄 삭제
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 화면 직접 호출보다는 운영/진단 또는 내부 처리에서 사용합니다.
 * - 호출 방식: `POST /api/admin/organizations/schools/{organizationId}/program-enrollment-history/bulk-delete`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: Swagger 요청 폼 또는 화면 필터/선택값
 * - 응답 사용 위치: 응답 본문을 화면 상태와 조회 캐시에 반영
 * - 프론트 조회 키: 화면별 조회 키 정책에 따름
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 준비 상태 정보 없음
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 스테이징 기본 검증 대상
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: MEMBER_WRITE 권한 필요
 * - 접근 범위: 관리자 CMS 권한 범위
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: PERSONAL_DATA 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: 2026-08-25 프론트 member-management handoff cross-check
 * @summary 학교 프로젝트 수강 이력 일괄 삭제
 */
const bulkDeleteProgramEnrollmentHistory = (
    organizationId: number,
    schoolProgramEnrollmentHistoryBulkDeleteRequest: SchoolProgramEnrollmentHistoryBulkDeleteRequest,
 options?: SecondParameter<typeof customInstance<ApiResponseBulkActionResponse>>,) => {
      return customInstance<ApiResponseBulkActionResponse>(
      {url: `/api/admin/organizations/schools/${organizationId}/program-enrollment-history/bulk-delete`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: schoolProgramEnrollmentHistoryBulkDeleteRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - CMS 학교 organization 일괄 삭제
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 화면 직접 호출보다는 운영/진단 또는 내부 처리에서 사용합니다.
 * - 호출 방식: `POST /api/admin/organizations/schools/bulk-delete`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: Swagger 요청 폼 또는 화면 필터/선택값
 * - 응답 사용 위치: 응답 본문을 화면 상태와 조회 캐시에 반영
 * - 프론트 조회 키: 화면별 조회 키 정책에 따름
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 준비 상태 정보 없음
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 스테이징 기본 검증 대상
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: MEMBER_WRITE 권한 필요
 * - 접근 범위: CMS_SCHOOL_ORGANIZATION 범위 정책
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: NONE 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: organizationId max100 partial success; single delete guard reused
 * @summary CMS 학교 organization 일괄 삭제
 */
const bulkDeleteSchools = (
    bulkDecisionRequest: BulkDecisionRequest,
 options?: SecondParameter<typeof customInstance<ApiResponseBulkActionResponse>>,) => {
      return customInstance<ApiResponseBulkActionResponse>(
      {url: `/api/admin/organizations/schools/bulk-delete`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: bulkDecisionRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 전체 회원·관리자 혼합 일괄 삭제
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 화면 직접 호출보다는 운영/진단 또는 내부 처리에서 사용합니다.
 * - 호출 방식: `POST /api/admin/members/all/bulk-delete`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: Swagger 요청 폼 또는 화면 필터/선택값
 * - 응답 사용 위치: 응답 본문을 화면 상태와 조회 캐시에 반영
 * - 프론트 조회 키: 화면별 조회 키 정책에 따름
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 준비 상태 정보 없음
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 스테이징 기본 검증 대상
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: ADMIN_WRITE+MEMBER_WRITE 권한 필요
 * - 접근 범위: 관리자 CMS 권한 범위
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: ANONYMIZE 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: typed MEMBER/ADMIN_ACCOUNT targets preserve canonical id namespaces
 * @summary 전체 회원·관리자 혼합 일괄 삭제
 */
const bulkDelete1 = (
    accountDirectoryBulkDeleteRequest: AccountDirectoryBulkDeleteRequest,
 options?: SecondParameter<typeof customInstance<ApiResponseAccountDirectoryBulkDeleteResponse>>,) => {
      return customInstance<ApiResponseAccountDirectoryBulkDeleteResponse>(
      {url: `/api/admin/members/all/bulk-delete`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: accountDirectoryBulkDeleteRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 강사 권한 신청 재검토 대기 전환
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 회원 권한/관리자 권한 (`null`)
 * - 프론트 담당 영역: members-permission (`members-permission`)
 * - 호출 방식: `POST /api/admin/instructor-role-requests/{requestId}/reset-pending`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: authenticated API action
 * - 응답 사용 위치: 강사 권한 신청 재검토 대기 전환
 * - 프론트 조회 키: `post_api_admin_instructor_role_requests_requestId_reset_pending`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 라우트 준비 완료
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: MEMBER_PERMISSION_RESET_SMOKE
 * - 목데이터 대체: none
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: INSTRUCTOR_APPROVE 권한 필요
 * - 접근 범위: GLOBAL 범위 정책
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: NOT_APPLICABLE 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: 2026-07-23 member remaining gap closure
 * @summary 강사 권한 신청 재검토 대기 전환
 */
const resetPending = (
    requestId: number,
    approvalResetRequest: ApprovalResetRequest,
 options?: SecondParameter<typeof customInstance<InstructorRoleWorkflowResponse>>,) => {
      return customInstance<InstructorRoleWorkflowResponse>(
      {url: `/api/admin/instructor-role-requests/${requestId}/reset-pending`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: approvalResetRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - POST /api/admin/instructor-role-requests/{requestId}/resend-notification 구현 API
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 화면 직접 호출보다는 운영/진단 또는 내부 처리에서 사용합니다.
 * - 호출 방식: `POST /api/admin/instructor-role-requests/{requestId}/resend-notification`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: Swagger 요청 폼 또는 화면 필터/선택값
 * - 응답 사용 위치: 응답 본문을 화면 상태와 조회 캐시에 반영
 * - 프론트 조회 키: 화면별 조회 키 정책에 따름
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 준비 상태 정보 없음
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 스테이징 기본 검증 대상
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: INSTRUCTOR_APPROVE 권한 필요
 * - 접근 범위: 관리자 CMS 권한 범위
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: 개인정보 없음
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Stage327 회원관리/대시보드 피그마 및 프론트 핸드오프 반영
 * @summary POST /api/admin/instructor-role-requests/{requestId}/resend-notification 구현 API
 */
const resendNotification = (
    requestId: number,
 options?: SecondParameter<typeof customInstance<InstructorRoleWorkflowResponse>>,) => {
      return customInstance<InstructorRoleWorkflowResponse>(
      {url: `/api/admin/instructor-role-requests/${requestId}/resend-notification`, method: 'POST'
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 강사 권한 신청 반려
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 회원 권한/관리자 권한 (`null`)
 * - 프론트 담당 영역: 관리자-permissions (`admin-permissions`)
 * - 호출 방식: `POST /api/admin/instructor-role-requests/{requestId}/reject`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 폼 상태 or 선택 행 action payload
 * - 응답 사용 위치: 변경 결과 then 관련 조회 키 갱신
 * - 프론트 조회 키: `post_admin_instructor-role-requests_requestId_reject`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 라우트 준비 완료
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 기본 스모크 검증 대상
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 admin-permissions API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: INSTRUCTOR_APPROVE 권한 필요
 * - 접근 범위: 관리자 CMS 권한 범위
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: 기본 마스킹 응답
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Stage317 member permission rejection workflow canonical route.
 * @summary 강사 권한 신청 반려
 */
const reject2 = (
    requestId: number,
    instructorRoleReviewRequest: InstructorRoleReviewRequest,
 options?: SecondParameter<typeof customInstance<InstructorRoleWorkflowResponse>>,) => {
      return customInstance<InstructorRoleWorkflowResponse>(
      {url: `/api/admin/instructor-role-requests/${requestId}/reject`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: instructorRoleReviewRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 강사 권한 신청 상세 원문 조회
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 화면 직접 호출보다는 운영/진단 또는 내부 처리에서 사용합니다.
 * - 호출 방식: `POST /api/admin/instructor-role-requests/{requestId}/privacy/unmask`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: Swagger 요청 폼 또는 화면 필터/선택값
 * - 응답 사용 위치: 응답 본문을 화면 상태와 조회 캐시에 반영
 * - 프론트 조회 키: 화면별 조회 키 정책에 따름
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 준비 상태 정보 없음
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 스테이징 기본 검증 대상
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: PRIVACY_RAW_READ 권한 필요
 * - 접근 범위: 관리자 CMS 권한 범위
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNMASK_REASON_REQUIRED 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 개인정보/파일 API는 마스킹 응답과 원문 접근을 구분합니다. 원문 조회, export, 민감파일 다운로드는 감사로그가 저장되어야 성공으로 취급합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: 신청 당시 PII/정산 원문 사유 기반 조회
 * @summary 강사 권한 신청 상세 원문 조회
 */
const unmask = (
    requestId: number,
    adminPrivacyUnmaskRequest: AdminPrivacyUnmaskRequest,
 options?: SecondParameter<typeof customInstance<InstructorRoleRequestDetailResponse>>,) => {
      return customInstance<InstructorRoleRequestDetailResponse>(
      {url: `/api/admin/instructor-role-requests/${requestId}/privacy/unmask`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: adminPrivacyUnmaskRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 강사 권한 신청 승인
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 회원 권한/관리자 권한 (`null`)
 * - 프론트 담당 영역: 관리자-permissions (`admin-permissions`)
 * - 호출 방식: `POST /api/admin/instructor-role-requests/{requestId}/approve`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 폼 상태 or 선택 행 action payload
 * - 응답 사용 위치: 변경 결과 then 관련 조회 키 갱신
 * - 프론트 조회 키: `post_admin_instructor-role-requests_requestId_approve`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 라우트 준비 완료
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 기본 스모크 검증 대상
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 admin-permissions API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: INSTRUCTOR_APPROVE 권한 필요
 * - 접근 범위: 관리자 CMS 권한 범위
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: 기본 마스킹 응답
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Stage317 member permission approval workflow canonical route.
 * @summary 강사 권한 신청 승인
 */
const approve1 = (
    requestId: number,
    instructorRoleReviewRequest: InstructorRoleReviewRequest,
 options?: SecondParameter<typeof customInstance<InstructorRoleWorkflowResponse>>,) => {
      return customInstance<InstructorRoleWorkflowResponse>(
      {url: `/api/admin/instructor-role-requests/${requestId}/approve`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: instructorRoleReviewRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - POST /api/admin/instructor-role-requests/bulk-reject
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 화면 직접 호출보다는 운영/진단 또는 내부 처리에서 사용합니다.
 * - 호출 방식: `POST /api/admin/instructor-role-requests/bulk-reject`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: Swagger 요청 폼 또는 화면 필터/선택값
 * - 응답 사용 위치: 응답 본문을 화면 상태와 조회 캐시에 반영
 * - 프론트 조회 키: 화면별 조회 키 정책에 따름
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 준비 상태 정보 없음
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 스테이징 기본 검증 대상
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: INSTRUCTOR_APPROVE 권한 필요
 * - 접근 범위: GLOBAL 범위 정책
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: 기본 마스킹 응답
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Frontend bulk API handoff implemented with max 100 and partial-success contract
 * @summary POST /api/admin/instructor-role-requests/bulk-reject
 */
const bulkReject = (
    instructorRoleBulkReviewRequest: InstructorRoleBulkReviewRequest,
 options?: SecondParameter<typeof customInstance<ApiResponseBulkActionResponse>>,) => {
      return customInstance<ApiResponseBulkActionResponse>(
      {url: `/api/admin/instructor-role-requests/bulk-reject`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: instructorRoleBulkReviewRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - POST /api/admin/instructor-role-requests/bulk-approve
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 화면 직접 호출보다는 운영/진단 또는 내부 처리에서 사용합니다.
 * - 호출 방식: `POST /api/admin/instructor-role-requests/bulk-approve`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: Swagger 요청 폼 또는 화면 필터/선택값
 * - 응답 사용 위치: 응답 본문을 화면 상태와 조회 캐시에 반영
 * - 프론트 조회 키: 화면별 조회 키 정책에 따름
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 준비 상태 정보 없음
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 스테이징 기본 검증 대상
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: INSTRUCTOR_APPROVE 권한 필요
 * - 접근 범위: GLOBAL 범위 정책
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: 기본 마스킹 응답
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Frontend bulk API handoff implemented with max 100 and partial-success contract
 * @summary POST /api/admin/instructor-role-requests/bulk-approve
 */
const bulkApprove = (
    instructorRoleBulkReviewRequest: InstructorRoleBulkReviewRequest,
 options?: SecondParameter<typeof customInstance<ApiResponseBulkActionResponse>>,) => {
      return customInstance<ApiResponseBulkActionResponse>(
      {url: `/api/admin/instructor-role-requests/bulk-approve`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: instructorRoleBulkReviewRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 관리자 승인 반려 건 재검토 대기 전환
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 회원 권한/관리자 권한 (`null`)
 * - 프론트 담당 영역: members-permission (`members-permission`)
 * - 호출 방식: `POST /api/admin/admin-approval-requests/{adminAccountId}/reset-pending`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: authenticated API action
 * - 응답 사용 위치: 관리자 승인 반려 건 재검토 대기 전환
 * - 프론트 조회 키: `post_api_admin_admin_approval_requests_adminId_reset_pending`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 라우트 준비 완료
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: MEMBER_PERMISSION_RESET_SMOKE
 * - 목데이터 대체: none
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: ADMIN_WRITE 권한 필요
 * - 접근 범위: GLOBAL 범위 정책
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: NOT_APPLICABLE 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: 2026-07-23 member remaining gap closure
 * @summary 관리자 승인 반려 건 재검토 대기 전환
 */
const resetAdminApprovalToPending = (
    adminAccountId: number,
    approvalResetRequest: ApprovalResetRequest,
 options?: SecondParameter<typeof customInstance<ApiResponseAdminAccountResponse>>,) => {
      return customInstance<ApiResponseAdminAccountResponse>(
      {url: `/api/admin/admin-approval-requests/${adminAccountId}/reset-pending`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: approvalResetRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 관리자 승인 알림 재발송
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 회원 권한/관리자 권한 (`null`)
 * - 프론트 담당 영역: 관리자-permissions (`admin-permissions`)
 * - 호출 방식: `POST /api/admin/admin-approval-requests/{adminAccountId}/resend-notification`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 역할별 등록 폼 또는 선택 회원 ID
 * - 응답 사용 위치: mutation 성공 후 회원 목록·상세 query invalidate
 * - 프론트 조회 키: `post_api_admin_admin_approval_requests_adminId_resend_notification`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 라우트 준비 완료
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: MEMBER_HANDOFF_E2E
 * - 목데이터 대체: 회원 역할별 mock/localStorage 데이터를 canonical API 상태로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: ADMIN_WRITE 권한 필요
 * - 접근 범위: 관리자 CMS 권한 범위
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: 기본 마스킹 응답
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: FE 회원 관리 핸드오프 2026-07-23 역할별 canonical 계약
 * @summary 관리자 승인 알림 재발송
 */
const resendAdminApprovalNotification = (
    adminAccountId: number,
 options?: SecondParameter<typeof customInstance<ApiResponseAdminPermissionRequestNotificationResponse>>,) => {
      return customInstance<ApiResponseAdminPermissionRequestNotificationResponse>(
      {url: `/api/admin/admin-approval-requests/${adminAccountId}/resend-notification`, method: 'POST'
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 관리자 셀프 가입 반려
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 화면 직접 호출보다는 운영/진단 또는 내부 처리에서 사용합니다.
 * - 호출 방식: `POST /api/admin/admin-approval-requests/{adminAccountId}/reject`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: Swagger 요청 폼 또는 화면 필터/선택값
 * - 응답 사용 위치: 응답 본문을 화면 상태와 조회 캐시에 반영
 * - 프론트 조회 키: 화면별 조회 키 정책에 따름
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 준비 상태 정보 없음
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 스테이징 기본 검증 대상
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: ADMIN_WRITE 권한 필요
 * - 접근 범위: 마스터 관리자만 가능
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: ADMIN_HIGH_RISK 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: PENDING_VERIFICATION 관리자 계정을 REJECTED_VERIFICATION로 전환
 * @summary 관리자 셀프 가입 반려
 */
const rejectAdminApprovalRequest = (
    adminAccountId: number,
    adminAccountApprovalDecisionRequest: AdminAccountApprovalDecisionRequest,
 options?: SecondParameter<typeof customInstance<ApiResponseAdminAccountResponse>>,) => {
      return customInstance<ApiResponseAdminAccountResponse>(
      {url: `/api/admin/admin-approval-requests/${adminAccountId}/reject`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: adminAccountApprovalDecisionRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 관리자 셀프 가입 승인
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 화면 직접 호출보다는 운영/진단 또는 내부 처리에서 사용합니다.
 * - 호출 방식: `POST /api/admin/admin-approval-requests/{adminAccountId}/approve`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: Swagger 요청 폼 또는 화면 필터/선택값
 * - 응답 사용 위치: 응답 본문을 화면 상태와 조회 캐시에 반영
 * - 프론트 조회 키: 화면별 조회 키 정책에 따름
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 준비 상태 정보 없음
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 스테이징 기본 검증 대상
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: ADMIN_WRITE 권한 필요
 * - 접근 범위: 마스터 관리자만 가능
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: ADMIN_HIGH_RISK 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: PENDING_VERIFICATION 관리자 계정을 ACTIVE로 전환
 * @summary 관리자 셀프 가입 승인
 */
const approveAdminApprovalRequest = (
    adminAccountId: number,
    adminAccountApprovalDecisionRequest: AdminAccountApprovalDecisionRequest,
 options?: SecondParameter<typeof customInstance<ApiResponseAdminAccountResponse>>,) => {
      return customInstance<ApiResponseAdminAccountResponse>(
      {url: `/api/admin/admin-approval-requests/${adminAccountId}/approve`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: adminAccountApprovalDecisionRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - POST /api/admin/admin-approval-requests/bulk-reject
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 화면 직접 호출보다는 운영/진단 또는 내부 처리에서 사용합니다.
 * - 호출 방식: `POST /api/admin/admin-approval-requests/bulk-reject`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: Swagger 요청 폼 또는 화면 필터/선택값
 * - 응답 사용 위치: 응답 본문을 화면 상태와 조회 캐시에 반영
 * - 프론트 조회 키: 화면별 조회 키 정책에 따름
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 준비 상태 정보 없음
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 스테이징 기본 검증 대상
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: ADMIN_WRITE 권한 필요
 * - 접근 범위: GLOBAL 범위 정책
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: NONE 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Frontend bulk API handoff implemented with max 100 and partial-success contract
 * @summary POST /api/admin/admin-approval-requests/bulk-reject
 */
const bulkRejectAdminApprovalRequests = (
    adminApprovalBulkDecisionRequest: AdminApprovalBulkDecisionRequest,
 options?: SecondParameter<typeof customInstance<ApiResponseBulkActionResponse>>,) => {
      return customInstance<ApiResponseBulkActionResponse>(
      {url: `/api/admin/admin-approval-requests/bulk-reject`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: adminApprovalBulkDecisionRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - POST /api/admin/admin-approval-requests/bulk-approve
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 화면 직접 호출보다는 운영/진단 또는 내부 처리에서 사용합니다.
 * - 호출 방식: `POST /api/admin/admin-approval-requests/bulk-approve`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: Swagger 요청 폼 또는 화면 필터/선택값
 * - 응답 사용 위치: 응답 본문을 화면 상태와 조회 캐시에 반영
 * - 프론트 조회 키: 화면별 조회 키 정책에 따름
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 준비 상태 정보 없음
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 스테이징 기본 검증 대상
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: ADMIN_WRITE 권한 필요
 * - 접근 범위: GLOBAL 범위 정책
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: NONE 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Frontend bulk API handoff implemented with max 100 and partial-success contract
 * @summary POST /api/admin/admin-approval-requests/bulk-approve
 */
const bulkApproveAdminApprovalRequests = (
    adminApprovalBulkDecisionRequest: AdminApprovalBulkDecisionRequest,
 options?: SecondParameter<typeof customInstance<ApiResponseBulkActionResponse>>,) => {
      return customInstance<ApiResponseBulkActionResponse>(
      {url: `/api/admin/admin-approval-requests/bulk-approve`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: adminApprovalBulkDecisionRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 관리자 목록 조회
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 회원 권한/관리자 권한 (`null`)
 * - 프론트 담당 영역: 관리자-permissions (`admin-permissions`)
 * - 호출 방식: `GET /api/admin/admin-accounts`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 필터/페이지네이션/선택 행에서 요청값 전달
 * - 응답 사용 위치: 조회 캐시 및 화면 목록·상세 상태 갱신
 * - 프론트 조회 키: `get_admin_admin-accounts`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 라우트 준비 완료
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 기본 스모크 검증 대상
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 admin-permissions API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: ADMIN_READ 권한 필요
 * - 접근 범위: 관리자 CMS 권한 범위
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: 기본 마스킹 응답
 * - 감사로그 저장: 필수 아님
 *
 * ### 상태값/화면 배지 기준
 * - 조회 API는 응답 원본 status/code 값을 화면 배지 라벨과 분리해서 보관합니다. 라벨은 프론트 표시용, 원본 값은 후속 API 호출 조건으로 사용합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: v2.8.0 P1 폼/런타임 및 마스터 워크플로우 기준 구현 완료
 * @summary 관리자 목록 조회
 */
const listAdmins = (
    params?: ListAdminsParams,
 options?: SecondParameter<typeof customInstance<PageResponseAdminAccountListItemResponse>>,) => {
      return customInstance<PageResponseAdminAccountListItemResponse>(
      {url: `/api/admin/admin-accounts`, method: 'GET',
        params
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 관리자 생성
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 회원 권한/관리자 권한 (`null`)
 * - 프론트 담당 영역: 관리자-permissions (`admin-permissions`)
 * - 호출 방식: `POST /api/admin/admin-accounts`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 폼 상태 or 선택 행 action payload
 * - 응답 사용 위치: 변경 결과 then 관련 조회 키 갱신
 * - 프론트 조회 키: `post_admin_admin-accounts`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 라우트 준비 완료
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 기본 스모크 검증 대상
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 admin-permissions API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: ADMIN_WRITE 권한 필요
 * - 접근 범위: 마스터 관리자만 가능
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: 기본 마스킹 응답
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: v2.8.0 P1 폼/런타임 및 마스터 워크플로우 기준 구현 완료
 * @summary 관리자 생성
 */
const createAdmin = (
    adminAccountCreateRequest: AdminAccountCreateRequest,
 options?: SecondParameter<typeof customInstance<ApiResponseAdminAccountResponse>>,) => {
      return customInstance<ApiResponseAdminAccountResponse>(
      {url: `/api/admin/admin-accounts`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: adminAccountCreateRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 마스터 관리자 검증 후 관리자 계정 활성화 또는 반려
 * - API 분류: 시스템 진단/운영 검증 API
 * - 사용하는 화면: 회원 권한/관리자 권한 (`null`)
 * - 프론트 담당 영역: permission (`permission`)
 * - 호출 방식: `POST /api/admin/admin-accounts/{adminAccountId}/verify`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: path/요청 본문 from operator action or secure workflow
 * - 응답 사용 위치: operation result / audit trail / download policy
 * - 프론트 조회 키: `post_api_admin_admin_accounts_adminId_verify`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 라우트 준비 완료
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: SECURITY_SMOKE
 * - 목데이터 대체: No localStorage persistence; use API response/cache and invalidate on mutation.
 * - 이 API는 일반 사용자 화면용이 아니라 관리자/QA/운영자가 시스템 상태를 확인할 때 사용하는 API입니다. 일반 화면 개발 시에는 직접 호출하지 않습니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: ADMIN_WRITE 권한 필요
 * - 접근 범위: 마스터 관리자만 가능
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: ADMIN_HIGH_RISK 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: admin accounts are created as PENDING_VERIFICATION and cannot login until MASTER verification
 * @summary 마스터 관리자 검증 후 관리자 계정 활성화 또는 반려
 */
const verifyAdmin = (
    adminAccountId: number,
    adminAccountVerificationRequest: AdminAccountVerificationRequest,
 options?: SecondParameter<typeof customInstance<ApiResponseAdminAccountResponse>>,) => {
      return customInstance<ApiResponseAdminAccountResponse>(
      {url: `/api/admin/admin-accounts/${adminAccountId}/verify`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: adminAccountVerificationRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - CMS 관리자 담당 프로그램 이력 일괄 삭제
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 화면 직접 호출보다는 운영/진단 또는 내부 처리에서 사용합니다.
 * - 호출 방식: `POST /api/admin/admin-accounts/{adminAccountId}/program-roles/bulk-delete`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: Swagger 요청 폼 또는 화면 필터/선택값
 * - 응답 사용 위치: 응답 본문을 화면 상태와 조회 캐시에 반영
 * - 프론트 조회 키: 화면별 조회 키 정책에 따름
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 준비 상태 정보 없음
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 스테이징 기본 검증 대상
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: ADMIN_WRITE 권한 필요
 * - 접근 범위: 관리자 CMS 권한 범위
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: ADMIN_CMS 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: 2026-08-25 프론트 member-management handoff cross-check
 * @summary CMS 관리자 담당 프로그램 이력 일괄 삭제
 */
const bulkDeleteProgramRoles = (
    adminAccountId: number,
    adminProgramRoleBulkDeleteRequest: AdminProgramRoleBulkDeleteRequest,
 options?: SecondParameter<typeof customInstance<ApiResponseBulkActionResponse>>,) => {
      return customInstance<ApiResponseBulkActionResponse>(
      {url: `/api/admin/admin-accounts/${adminAccountId}/program-roles/bulk-delete`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: adminProgramRoleBulkDeleteRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 관리자 개인정보 원문 조회
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 화면 직접 호출보다는 운영/진단 또는 내부 처리에서 사용합니다.
 * - 호출 방식: `POST /api/admin/admin-accounts/{adminAccountId}/privacy/unmask`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: Swagger 요청 폼 또는 화면 필터/선택값
 * - 응답 사용 위치: 응답 본문을 화면 상태와 조회 캐시에 반영
 * - 프론트 조회 키: 화면별 조회 키 정책에 따름
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 준비 상태 정보 없음
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 스테이징 기본 검증 대상
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: PRIVACY_RAW_READ 권한 필요
 * - 접근 범위: ADMIN_ACCOUNT_SCOPE 범위 정책
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNMASKED_AUDITED 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 개인정보/파일 API는 마스킹 응답과 원문 접근을 구분합니다. 원문 조회, export, 민감파일 다운로드는 감사로그가 저장되어야 성공으로 취급합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: V32 adminAccountId canonical privacy/comment contract
 * @summary 관리자 개인정보 원문 조회
 */
const unmask1 = (
    adminAccountId: number,
    adminPrivacyUnmaskRequest: AdminPrivacyUnmaskRequest,
 options?: SecondParameter<typeof customInstance<AdminAccountPrivacyResponse>>,) => {
      return customInstance<AdminAccountPrivacyResponse>(
      {url: `/api/admin/admin-accounts/${adminAccountId}/privacy/unmask`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: adminPrivacyUnmaskRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 관리자 비밀번호 초기화
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 화면 직접 호출보다는 운영/진단 또는 내부 처리에서 사용합니다.
 * - 호출 방식: `POST /api/admin/admin-accounts/{adminAccountId}/password-reset`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: Swagger 요청 폼 또는 화면 필터/선택값
 * - 응답 사용 위치: 응답 본문을 화면 상태와 조회 캐시에 반영
 * - 프론트 조회 키: 화면별 조회 키 정책에 따름
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 준비 상태 정보 없음
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 스테이징 기본 검증 대상
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: ADMIN_WRITE 권한 필요
 * - 접근 범위: 마스터 관리자만 가능
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: ADMIN_HIGH_RISK 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: 관리자 비밀번호를 정책에 맞는 새 임시 비밀번호로 초기화하고 기존 토큰을 무효화합니다.
 * @summary 관리자 비밀번호 초기화
 */
const resetAdminPassword = (
    adminAccountId: number,
    adminAccountPasswordResetRequest: AdminAccountPasswordResetRequest,
 options?: SecondParameter<typeof customInstance<ApiResponseAdminAccountResponse>>,) => {
      return customInstance<ApiResponseAdminAccountResponse>(
      {url: `/api/admin/admin-accounts/${adminAccountId}/password-reset`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: adminAccountPasswordResetRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 관리자 상세 코멘트 목록
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 화면 직접 호출보다는 운영/진단 또는 내부 처리에서 사용합니다.
 * - 호출 방식: `GET /api/admin/admin-accounts/{adminAccountId}/comments`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: Swagger 요청 폼 또는 화면 필터/선택값
 * - 응답 사용 위치: 응답 본문을 화면 상태와 조회 캐시에 반영
 * - 프론트 조회 키: 화면별 조회 키 정책에 따름
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 준비 상태 정보 없음
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 스테이징 기본 검증 대상
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: MEMBER_COMMENT_READ 권한 필요
 * - 접근 범위: ADMIN_ACCOUNT_SCOPE 범위 정책
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: INTERNAL_ADMIN_COMMENT 개인정보 정책
 * - 감사로그 저장: 필수 아님
 *
 * ### 상태값/화면 배지 기준
 * - 조회 API는 응답 원본 status/code 값을 화면 배지 라벨과 분리해서 보관합니다. 라벨은 프론트 표시용, 원본 값은 후속 API 호출 조건으로 사용합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: V32 adminAccountId canonical privacy/comment contract
 * @summary 관리자 상세 코멘트 목록
 */
const listComments = (
    adminAccountId: number,
    params?: ListCommentsParams,
 options?: SecondParameter<typeof customInstance<AdminCommentResponse[]>>,) => {
      return customInstance<AdminCommentResponse[]>(
      {url: `/api/admin/admin-accounts/${adminAccountId}/comments`, method: 'GET',
        params
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 관리자 상세 코멘트 등록
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 화면 직접 호출보다는 운영/진단 또는 내부 처리에서 사용합니다.
 * - 호출 방식: `POST /api/admin/admin-accounts/{adminAccountId}/comments`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: Swagger 요청 폼 또는 화면 필터/선택값
 * - 응답 사용 위치: 응답 본문을 화면 상태와 조회 캐시에 반영
 * - 프론트 조회 키: 화면별 조회 키 정책에 따름
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 준비 상태 정보 없음
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 스테이징 기본 검증 대상
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: MEMBER_COMMENT_WRITE 권한 필요
 * - 접근 범위: ADMIN_ACCOUNT_SCOPE 범위 정책
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: INTERNAL_ADMIN_COMMENT 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: V32 adminAccountId canonical privacy/comment contract
 * @summary 관리자 상세 코멘트 등록
 */
const createComment = (
    adminAccountId: number,
    adminMemberCommentCreateRequest: AdminMemberCommentCreateRequest,
 options?: SecondParameter<typeof customInstance<AdminCommentResponse>>,) => {
      return customInstance<AdminCommentResponse>(
      {url: `/api/admin/admin-accounts/${adminAccountId}/comments`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: adminMemberCommentCreateRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 관리자 계정 일괄 삭제
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 화면 직접 호출보다는 운영/진단 또는 내부 처리에서 사용합니다.
 * - 호출 방식: `POST /api/admin/admin-accounts/bulk-delete`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: Swagger 요청 폼 또는 화면 필터/선택값
 * - 응답 사용 위치: 응답 본문을 화면 상태와 조회 캐시에 반영
 * - 프론트 조회 키: 화면별 조회 키 정책에 따름
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 준비 상태 정보 없음
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 스테이징 기본 검증 대상
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: ADMIN_WRITE 권한 필요
 * - 접근 범위: 마스터 관리자만 가능
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: PII_ANONYMIZATION 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: 2026-08-12 CMS 관리자 회원 수정 요청 반영
 * @summary 관리자 계정 일괄 삭제
 */
const bulkDeleteAdmins = (
    bulkDecisionRequest: BulkDecisionRequest,
 options?: SecondParameter<typeof customInstance<ApiResponseBulkActionResponse>>,) => {
      return customInstance<ApiResponseBulkActionResponse>(
      {url: `/api/admin/admin-accounts/bulk-delete`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: bulkDecisionRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 회원/강사 부분 수정
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 화면 직접 호출보다는 운영/진단 또는 내부 처리에서 사용합니다.
 * - 호출 방식: `PATCH /api/admin/users/{memberId}`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: Swagger 요청 폼 또는 화면 필터/선택값
 * - 응답 사용 위치: 응답 본문을 화면 상태와 조회 캐시에 반영
 * - 프론트 조회 키: 화면별 조회 키 정책에 따름
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 준비 상태 정보 없음
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 스테이징 기본 검증 대상
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary 회원/강사 부분 수정
 */
const updateMemberBasicInfo = (
    memberId: number,
    adminMemberBasicInfoUpdateRequest: AdminMemberBasicInfoUpdateRequest,
 options?: SecondParameter<typeof customInstance<UserResponse>>,) => {
      return customInstance<UserResponse>(
      {url: `/api/admin/users/${memberId}`, method: 'PATCH',
      headers: {'Content-Type': 'application/json', },
      data: adminMemberBasicInfoUpdateRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 회원/강사 부분 수정
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 회원 관리 (`null`)
 * - 프론트 담당 영역: members (`members`)
 * - 호출 방식: `PATCH /api/admin/users/{memberId}/external-identifiers/{provider}`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 폼 상태 or 선택 행 action payload
 * - 응답 사용 위치: 변경 결과 then 관련 조회 키 갱신
 * - 프론트 조회 키: `patch_admin_members_memberId_external-identifiers_provider`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 라우트 준비 완료
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 기본 스모크 검증 대상
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 members API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary 회원/강사 부분 수정
 */
const upsertExternalIdentifier = (
    memberId: number,
    provider: string,
    externalIdentifierUpsertRequest: ExternalIdentifierUpsertRequest,
 options?: SecondParameter<typeof customInstance<ExternalIdentifierResponse>>,) => {
      return customInstance<ExternalIdentifierResponse>(
      {url: `/api/admin/users/${memberId}/external-identifiers/${provider}`, method: 'PATCH',
      headers: {'Content-Type': 'application/json', },
      data: externalIdentifierUpsertRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 회원/강사 삭제
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 화면 직접 호출보다는 운영/진단 또는 내부 처리에서 사용합니다.
 * - 호출 방식: `DELETE /api/admin/users/{memberId}/comments/{commentId}`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: Swagger 요청 폼 또는 화면 필터/선택값
 * - 응답 사용 위치: 응답 본문을 화면 상태와 조회 캐시에 반영
 * - 프론트 조회 키: 화면별 조회 키 정책에 따름
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 준비 상태 정보 없음
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 스테이징 기본 검증 대상
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: MEMBER_COMMENT_WRITE 권한 필요
 * - 접근 범위: 관리자 CMS 권한 범위
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: 기본 마스킹 응답
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Stage327 회원관리/대시보드 피그마 및 프론트 핸드오프 반영
 * @summary 회원/강사 삭제
 */
const deleteMemberComment = (
    memberId: number,
    commentId: number,
 options?: SecondParameter<typeof customInstance<unknown>>,) => {
      return customInstance<unknown>(
      {url: `/api/admin/users/${memberId}/comments/${commentId}`, method: 'DELETE'
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 회원/강사 부분 수정
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 화면 직접 호출보다는 운영/진단 또는 내부 처리에서 사용합니다.
 * - 호출 방식: `PATCH /api/admin/users/{memberId}/comments/{commentId}`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: Swagger 요청 폼 또는 화면 필터/선택값
 * - 응답 사용 위치: 응답 본문을 화면 상태와 조회 캐시에 반영
 * - 프론트 조회 키: 화면별 조회 키 정책에 따름
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 준비 상태 정보 없음
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 스테이징 기본 검증 대상
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: MEMBER_COMMENT_WRITE 권한 필요
 * - 접근 범위: 관리자 CMS 권한 범위
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: 기본 마스킹 응답
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Stage327 회원관리/대시보드 피그마 및 프론트 핸드오프 반영
 * @summary 회원/강사 부분 수정
 */
const updateMemberComment = (
    memberId: number,
    commentId: number,
    adminCommentUpdateRequest: AdminCommentUpdateRequest,
 options?: SecondParameter<typeof customInstance<AdminCommentResponse>>,) => {
      return customInstance<AdminCommentResponse>(
      {url: `/api/admin/users/${memberId}/comments/${commentId}`, method: 'PATCH',
      headers: {'Content-Type': 'application/json', },
      data: adminCommentUpdateRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 회원/강사 부분 수정
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 화면 직접 호출보다는 운영/진단 또는 내부 처리에서 사용합니다.
 * - 호출 방식: `PATCH /api/admin/users/{memberId}/affiliated-teachers/{teacherMemberId}/employment-status`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: Swagger 요청 폼 또는 화면 필터/선택값
 * - 응답 사용 위치: 응답 본문을 화면 상태와 조회 캐시에 반영
 * - 프론트 조회 키: 화면별 조회 키 정책에 따름
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 준비 상태 정보 없음
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 스테이징 기본 검증 대상
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: MEMBER_WRITE 권한 필요
 * - 접근 범위: 관리자 CMS 권한 범위
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: 기본 마스킹 응답
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Stage327 회원관리/대시보드 피그마 및 프론트 핸드오프 반영
 * @summary 회원/강사 부분 수정
 */
const updateAffiliatedTeacherEmploymentStatus = (
    memberId: number,
    teacherMemberId: number,
    teacherEmploymentStatusUpdateRequest: TeacherEmploymentStatusUpdateRequest,
 options?: SecondParameter<typeof customInstance<SchoolAffiliatedTeacherResponse>>,) => {
      return customInstance<SchoolAffiliatedTeacherResponse>(
      {url: `/api/admin/users/${memberId}/affiliated-teachers/${teacherMemberId}/employment-status`, method: 'PATCH',
      headers: {'Content-Type': 'application/json', },
      data: teacherEmploymentStatusUpdateRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - CMS 학교 organization 상세 조회
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 화면 직접 호출보다는 운영/진단 또는 내부 처리에서 사용합니다.
 * - 호출 방식: `GET /api/admin/organizations/schools/{organizationId}`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: Swagger 요청 폼 또는 화면 필터/선택값
 * - 응답 사용 위치: 응답 본문을 화면 상태와 조회 캐시에 반영
 * - 프론트 조회 키: 화면별 조회 키 정책에 따름
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 준비 상태 정보 없음
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 스테이징 기본 검증 대상
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: MEMBER_READ 권한 필요
 * - 접근 범위: CMS_SCHOOL_ORGANIZATION 범위 정책
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: NONE 개인정보 정책
 * - 감사로그 저장: 필수 아님
 *
 * ### 상태값/화면 배지 기준
 * - 조회 API는 응답 원본 status/code 값을 화면 배지 라벨과 분리해서 보관합니다. 라벨은 프론트 표시용, 원본 값은 후속 API 호출 조건으로 사용합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: 학교 상세 canonical 식별자는 memberId가 아니라 organizationId; synthetic SCHOOL member 신규 생성 금지
 * @summary CMS 학교 organization 상세 조회
 */
const getSchool = (
    organizationId: number,
 options?: SecondParameter<typeof customInstance<SchoolOrganizationListItemResponse>>,) => {
      return customInstance<SchoolOrganizationListItemResponse>(
      {url: `/api/admin/organizations/schools/${organizationId}`, method: 'GET'
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - CMS 학교 organization 삭제
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 화면 직접 호출보다는 운영/진단 또는 내부 처리에서 사용합니다.
 * - 호출 방식: `DELETE /api/admin/organizations/schools/{organizationId}`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: Swagger 요청 폼 또는 화면 필터/선택값
 * - 응답 사용 위치: 응답 본문을 화면 상태와 조회 캐시에 반영
 * - 프론트 조회 키: 화면별 조회 키 정책에 따름
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 준비 상태 정보 없음
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 스테이징 기본 검증 대상
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: MEMBER_WRITE 권한 필요
 * - 접근 범위: CMS_SCHOOL_ORGANIZATION 범위 정책
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: NONE 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Figma 학교 목록 삭제; active teacher/business history guard 후 soft-delete
 * @summary CMS 학교 organization 삭제
 */
const deleteSchool = (
    organizationId: number,
    params?: DeleteSchoolParams,
 options?: SecondParameter<typeof customInstance<ApiResponseVoid>>,) => {
      return customInstance<ApiResponseVoid>(
      {url: `/api/admin/organizations/schools/${organizationId}`, method: 'DELETE',
        params
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - CMS 학교 organization 기본정보 수정
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 화면 직접 호출보다는 운영/진단 또는 내부 처리에서 사용합니다.
 * - 호출 방식: `PATCH /api/admin/organizations/schools/{organizationId}`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: Swagger 요청 폼 또는 화면 필터/선택값
 * - 응답 사용 위치: 응답 본문을 화면 상태와 조회 캐시에 반영
 * - 프론트 조회 키: 화면별 조회 키 정책에 따름
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 준비 상태 정보 없음
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 스테이징 기본 검증 대상
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: MEMBER_WRITE 권한 필요
 * - 접근 범위: CMS_SCHOOL_ORGANIZATION 범위 정책
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: NONE 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: organizationId canonical write; member/teacher identity를 수정하지 않음
 * @summary CMS 학교 organization 기본정보 수정
 */
const updateSchool = (
    organizationId: number,
    schoolOrganizationUpsertRequest: SchoolOrganizationUpsertRequest,
 options?: SecondParameter<typeof customInstance<SchoolOrganizationListItemResponse>>,) => {
      return customInstance<SchoolOrganizationListItemResponse>(
      {url: `/api/admin/organizations/schools/${organizationId}`, method: 'PATCH',
      headers: {'Content-Type': 'application/json', },
      data: schoolOrganizationUpsertRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - CMS 학교 소속 교사 재직 상태 변경
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 화면 직접 호출보다는 운영/진단 또는 내부 처리에서 사용합니다.
 * - 호출 방식: `PATCH /api/admin/organizations/schools/{organizationId}/teachers/{teacherMemberId}/employment-status`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: Swagger 요청 폼 또는 화면 필터/선택값
 * - 응답 사용 위치: 응답 본문을 화면 상태와 조회 캐시에 반영
 * - 프론트 조회 키: 화면별 조회 키 정책에 따름
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 준비 상태 정보 없음
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 스테이징 기본 검증 대상
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: MEMBER_WRITE 권한 필요
 * - 접근 범위: CMS_SCHOOL_ORGANIZATION 범위 정책
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: 기본 마스킹 응답
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: 학교는 member가 아니라 organizationId canonical resource; CMS admin role과 member role 분리
 * @summary CMS 학교 소속 교사 재직 상태 변경
 */
const updateTeacherEmploymentStatus = (
    organizationId: number,
    teacherMemberId: number,
    teacherEmploymentStatusUpdateRequest: TeacherEmploymentStatusUpdateRequest,
 options?: SecondParameter<typeof customInstance<SchoolAffiliatedTeacherResponse>>,) => {
      return customInstance<SchoolAffiliatedTeacherResponse>(
      {url: `/api/admin/organizations/schools/${organizationId}/teachers/${teacherMemberId}/employment-status`, method: 'PATCH',
      headers: {'Content-Type': 'application/json', },
      data: teacherEmploymentStatusUpdateRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 관리자 사용 상태 변경
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 화면 직접 호출보다는 운영/진단 또는 내부 처리에서 사용합니다.
 * - 호출 방식: `PATCH /api/admin/admin-accounts/{adminAccountId}/status`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: Swagger 요청 폼 또는 화면 필터/선택값
 * - 응답 사용 위치: 응답 본문을 화면 상태와 조회 캐시에 반영
 * - 프론트 조회 키: 화면별 조회 키 정책에 따름
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 준비 상태 정보 없음
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 스테이징 기본 검증 대상
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: ADMIN_WRITE 권한 필요
 * - 접근 범위: 마스터 관리자만 가능
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: ADMIN_HIGH_RISK 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: 관리자 상세 화면에서 ACTIVE/SUSPENDED 상태를 변경합니다.
 * @summary 관리자 사용 상태 변경
 */
const changeAdminStatus = (
    adminAccountId: number,
    adminAccountStatusChangeRequest: AdminAccountStatusChangeRequest,
 options?: SecondParameter<typeof customInstance<ApiResponseAdminAccountResponse>>,) => {
      return customInstance<ApiResponseAdminAccountResponse>(
      {url: `/api/admin/admin-accounts/${adminAccountId}/status`, method: 'PATCH',
      headers: {'Content-Type': 'application/json', },
      data: adminAccountStatusChangeRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 관리자 role 변경
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 회원 권한/관리자 권한 (`null`)
 * - 프론트 담당 영역: 관리자-permissions (`admin-permissions`)
 * - 호출 방식: `PATCH /api/admin/admin-accounts/{adminAccountId}/role`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 폼 상태 or 선택 행 action payload
 * - 응답 사용 위치: 변경 결과 then 관련 조회 키 갱신
 * - 프론트 조회 키: `patch_admin_admin-accounts_adminId_role`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 라우트 준비 완료
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 기본 스모크 검증 대상
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 admin-permissions API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: ADMIN_WRITE 권한 필요
 * - 접근 범위: 마스터 관리자만 가능
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: 개인정보 없음
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: v2.8.0 P1 폼/런타임 및 마스터 워크플로우 기준 구현 완료
 * @summary 관리자 role 변경
 */
const changeAdminRole = (
    adminAccountId: number,
    adminRoleChangeRequest: AdminRoleChangeRequest,
 options?: SecondParameter<typeof customInstance<ApiResponseAdminAccountResponse>>,) => {
      return customInstance<ApiResponseAdminAccountResponse>(
      {url: `/api/admin/admin-accounts/${adminAccountId}/role`, method: 'PATCH',
      headers: {'Content-Type': 'application/json', },
      data: adminRoleChangeRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 관리자 상세 코멘트 삭제
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 화면 직접 호출보다는 운영/진단 또는 내부 처리에서 사용합니다.
 * - 호출 방식: `DELETE /api/admin/admin-accounts/{adminAccountId}/comments/{commentId}`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: Swagger 요청 폼 또는 화면 필터/선택값
 * - 응답 사용 위치: 응답 본문을 화면 상태와 조회 캐시에 반영
 * - 프론트 조회 키: 화면별 조회 키 정책에 따름
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 준비 상태 정보 없음
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 스테이징 기본 검증 대상
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: MEMBER_COMMENT_WRITE 권한 필요
 * - 접근 범위: ADMIN_ACCOUNT_SCOPE 범위 정책
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: INTERNAL_ADMIN_COMMENT 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: V32 adminAccountId canonical privacy/comment contract
 * @summary 관리자 상세 코멘트 삭제
 */
const deleteComment = (
    adminAccountId: number,
    commentId: number,
 options?: SecondParameter<typeof customInstance<unknown>>,) => {
      return customInstance<unknown>(
      {url: `/api/admin/admin-accounts/${adminAccountId}/comments/${commentId}`, method: 'DELETE'
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 관리자 상세 코멘트 수정
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 화면 직접 호출보다는 운영/진단 또는 내부 처리에서 사용합니다.
 * - 호출 방식: `PATCH /api/admin/admin-accounts/{adminAccountId}/comments/{commentId}`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: Swagger 요청 폼 또는 화면 필터/선택값
 * - 응답 사용 위치: 응답 본문을 화면 상태와 조회 캐시에 반영
 * - 프론트 조회 키: 화면별 조회 키 정책에 따름
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 준비 상태 정보 없음
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 스테이징 기본 검증 대상
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: MEMBER_COMMENT_WRITE 권한 필요
 * - 접근 범위: ADMIN_ACCOUNT_SCOPE 범위 정책
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: INTERNAL_ADMIN_COMMENT 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: V32 adminAccountId canonical privacy/comment contract
 * @summary 관리자 상세 코멘트 수정
 */
const updateComment = (
    adminAccountId: number,
    commentId: number,
    adminCommentUpdateRequest: AdminCommentUpdateRequest,
 options?: SecondParameter<typeof customInstance<AdminCommentResponse>>,) => {
      return customInstance<AdminCommentResponse>(
      {url: `/api/admin/admin-accounts/${adminAccountId}/comments/${commentId}`, method: 'PATCH',
      headers: {'Content-Type': 'application/json', },
      data: adminCommentUpdateRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 관리자 기본 정보 수정
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 화면 직접 호출보다는 운영/진단 또는 내부 처리에서 사용합니다.
 * - 호출 방식: `PATCH /api/admin/admin-accounts/{adminAccountId}/basic-info`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: Swagger 요청 폼 또는 화면 필터/선택값
 * - 응답 사용 위치: 응답 본문을 화면 상태와 조회 캐시에 반영
 * - 프론트 조회 키: 화면별 조회 키 정책에 따름
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 준비 상태 정보 없음
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 스테이징 기본 검증 대상
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: ADMIN_WRITE 권한 필요
 * - 접근 범위: 마스터 관리자만 가능
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: PERSONAL_DATA 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: 관리자 상세 화면에서 이름, 연락처, 성별, 생년월일을 수정합니다.
 * @summary 관리자 기본 정보 수정
 */
const updateAdminBasicInfo = (
    adminAccountId: number,
    adminAccountBasicInfoUpdateRequest: AdminAccountBasicInfoUpdateRequest,
 options?: SecondParameter<typeof customInstance<ApiResponseAdminAccountResponse>>,) => {
      return customInstance<ApiResponseAdminAccountResponse>(
      {url: `/api/admin/admin-accounts/${adminAccountId}/basic-info`, method: 'PATCH',
      headers: {'Content-Type': 'application/json', },
      data: adminAccountBasicInfoUpdateRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 회원/강사 조회
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 회원 관리 (`null`)
 * - 프론트 담당 영역: members (`members`)
 * - 호출 방식: `GET /api/admin/users`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 필터/페이지네이션/선택 행에서 요청값 전달
 * - 응답 사용 위치: 조회 캐시 및 화면 목록·상세 상태 갱신
 * - 프론트 조회 키: `get_admin_members`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 라우트 준비 완료
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 기본 스모크 검증 대상
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 members API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 조회 API는 응답 원본 status/code 값을 화면 배지 라벨과 분리해서 보관합니다. 라벨은 프론트 표시용, 원본 값은 후속 API 호출 조건으로 사용합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary 회원/강사 조회
 */
const listMembers = (
    params?: ListMembersParams,
 options?: SecondParameter<typeof customInstance<PageResponse>>,) => {
      return customInstance<PageResponse>(
      {url: `/api/admin/users`, method: 'GET',
        params
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 교사 회원 역할 상세 조회
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 회원 관리 (`null`)
 * - 프론트 담당 영역: members (`members`)
 * - 호출 방식: `GET /api/admin/users/{memberId}/teacher`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 선택 교사 memberId
 * - 응답 사용 위치: 교사 기본정보·소속 학교·재직 상태·동의 이력 렌더링
 * - 프론트 조회 키: `get_api_admin_users_memberId_teacher`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 라우트 준비 완료
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: MEMBER_TEACHER_DIRECTORY_E2E
 * - 목데이터 대체: 교사 상세 mock/localStorage 데이터를 canonical 역할 상세 API로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: MEMBER_READ 권한 필요
 * - 접근 범위: 관리자 CMS 권한 범위
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: PERSONAL_DATA 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 조회 API는 응답 원본 status/code 값을 화면 배지 라벨과 분리해서 보관합니다. 라벨은 프론트 표시용, 원본 값은 후속 API 호출 조건으로 사용합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: 회원포탈 교사 가입 후 CMS 학교(교사) 목록/상세 조회 분리 및 401 오분류 수정
 * @summary 교사 회원 역할 상세 조회
 */
const getTeacherMemberDetail = (
    memberId: number,
 options?: SecondParameter<typeof customInstance<TeacherMemberDetailResponse>>,) => {
      return customInstance<TeacherMemberDetailResponse>(
      {url: `/api/admin/users/${memberId}/teacher`, method: 'GET'
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 학교·기관 회원 상세 조회
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 회원 관리 (`null`)
 * - 프론트 담당 영역: members (`members`)
 * - 호출 방식: `GET /api/admin/users/{memberId}/school`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 선택 회원 ID 및 상세 탭
 * - 응답 사용 위치: 역할별 상세 화면 렌더링
 * - 프론트 조회 키: `get_api_admin_users_memberId_school`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 라우트 준비 완료
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: MEMBER_HANDOFF_E2E
 * - 목데이터 대체: 회원 역할별 mock/localStorage 데이터를 canonical API 상태로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: MEMBER_READ 권한 필요
 * - 접근 범위: 관리자 CMS 권한 범위
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: PERSONAL_DATA 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 조회 API는 응답 원본 status/code 값을 화면 배지 라벨과 분리해서 보관합니다. 라벨은 프론트 표시용, 원본 값은 후속 API 호출 조건으로 사용합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: FE 회원 관리 핸드오프 2026-07-23 역할별 canonical 계약
 * @deprecated
 * @summary 학교·기관 회원 상세 조회
 */
const getSchoolMemberDetail = (
    memberId: number,
 options?: SecondParameter<typeof customInstance<SchoolMemberDetailResponse>>,) => {
      return customInstance<SchoolMemberDetailResponse>(
      {url: `/api/admin/users/${memberId}/school`, method: 'GET'
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 프로그램 조회
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 화면 직접 호출보다는 운영/진단 또는 내부 처리에서 사용합니다.
 * - 호출 방식: `GET /api/admin/users/{memberId}/program-history`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: Swagger 요청 폼 또는 화면 필터/선택값
 * - 응답 사용 위치: 응답 본문을 화면 상태와 조회 캐시에 반영
 * - 프론트 조회 키: 화면별 조회 키 정책에 따름
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 준비 상태 정보 없음
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 스테이징 기본 검증 대상
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 조회 API는 응답 원본 status/code 값을 화면 배지 라벨과 분리해서 보관합니다. 라벨은 프론트 표시용, 원본 값은 후속 API 호출 조건으로 사용합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary 프로그램 조회
 */
const listMemberProgramHistory = (
    memberId: number,
    params?: ListMemberProgramHistoryParams,
 options?: SecondParameter<typeof customInstance<PageResponseMemberProgramHistoryResponse>>,) => {
      return customInstance<PageResponseMemberProgramHistoryResponse>(
      {url: `/api/admin/users/${memberId}/program-history`, method: 'GET',
        params
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 회원 개인정보 availableActions 조회
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 화면 직접 호출보다는 운영/진단 또는 내부 처리에서 사용합니다.
 * - 호출 방식: `GET /api/admin/users/{memberId}/privacy/available-actions`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: Swagger 요청 폼 또는 화면 필터/선택값
 * - 응답 사용 위치: 응답 본문을 화면 상태와 조회 캐시에 반영
 * - 프론트 조회 키: 화면별 조회 키 정책에 따름
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 준비 상태 정보 없음
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 스테이징 기본 검증 대상
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: MEMBER_READ 권한 필요
 * - 접근 범위: 관리자 CMS 권한 범위
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: RAW_PRIVACY_POLICY 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 개인정보/파일 API는 마스킹 응답과 원문 접근을 구분합니다. 원문 조회, export, 민감파일 다운로드는 감사로그가 저장되어야 성공으로 취급합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Stage008 member raw privacy action contract
 * @summary 회원 개인정보 availableActions 조회
 */
const getMemberPrivacyAvailableActions = (
    memberId: number,
 options?: SecondParameter<typeof customInstance<RawPrivacyAvailableActionsResponse>>,) => {
      return customInstance<RawPrivacyAvailableActionsResponse>(
      {url: `/api/admin/users/${memberId}/privacy/available-actions`, method: 'GET'
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 강사 회원 상세 조회
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 회원 관리 (`null`)
 * - 프론트 담당 영역: members (`members`)
 * - 호출 방식: `GET /api/admin/users/{memberId}/instructor`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 선택 회원 ID 및 상세 탭
 * - 응답 사용 위치: 역할별 상세 화면 렌더링
 * - 프론트 조회 키: `get_api_admin_users_memberId_instructor`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 라우트 준비 완료
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: MEMBER_HANDOFF_E2E
 * - 목데이터 대체: 회원 역할별 mock/localStorage 데이터를 canonical API 상태로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: INSTRUCTOR_READ 권한 필요
 * - 접근 범위: 관리자 CMS 권한 범위
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: PERSONAL_DATA 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 조회 API는 응답 원본 status/code 값을 화면 배지 라벨과 분리해서 보관합니다. 라벨은 프론트 표시용, 원본 값은 후속 API 호출 조건으로 사용합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: FE 회원 관리 핸드오프 2026-07-23 역할별 canonical 계약
 * @summary 강사 회원 상세 조회
 */
const getInstructorMemberDetail = (
    memberId: number,
 options?: SecondParameter<typeof customInstance<InstructorMemberDetailResponse>>,) => {
      return customInstance<InstructorMemberDetailResponse>(
      {url: `/api/admin/users/${memberId}/instructor`, method: 'GET'
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 회원/강사 조회
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 회원 관리 (`null`)
 * - 프론트 담당 영역: members (`members`)
 * - 호출 방식: `GET /api/admin/users/{memberId}/instructor-profile`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 필터/페이지네이션/선택 행에서 요청값 전달
 * - 응답 사용 위치: 조회 캐시 및 화면 목록·상세 상태 갱신
 * - 프론트 조회 키: `get_admin_members_memberId_instructor-profile`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 라우트 준비 완료
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 기본 스모크 검증 대상
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 members API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 개인정보/파일 API는 마스킹 응답과 원문 접근을 구분합니다. 원문 조회, export, 민감파일 다운로드는 감사로그가 저장되어야 성공으로 취급합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary 회원/강사 조회
 */
const getInstructorDetail = (
    memberId: number,
 options?: SecondParameter<typeof customInstance<InstructorDetailResponse>>,) => {
      return customInstance<InstructorDetailResponse>(
      {url: `/api/admin/users/${memberId}/instructor-profile`, method: 'GET'
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 강사 개인정보 availableActions 조회
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 화면 직접 호출보다는 운영/진단 또는 내부 처리에서 사용합니다.
 * - 호출 방식: `GET /api/admin/users/{memberId}/instructor-profile/privacy/available-actions`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: Swagger 요청 폼 또는 화면 필터/선택값
 * - 응답 사용 위치: 응답 본문을 화면 상태와 조회 캐시에 반영
 * - 프론트 조회 키: 화면별 조회 키 정책에 따름
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 준비 상태 정보 없음
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 스테이징 기본 검증 대상
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: INSTRUCTOR_READ 권한 필요
 * - 접근 범위: 관리자 CMS 권한 범위
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: RAW_PRIVACY_POLICY 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 개인정보/파일 API는 마스킹 응답과 원문 접근을 구분합니다. 원문 조회, export, 민감파일 다운로드는 감사로그가 저장되어야 성공으로 취급합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Stage008 instructor raw privacy action contract
 * @summary 강사 개인정보 availableActions 조회
 */
const getInstructorPrivacyAvailableActions = (
    memberId: number,
 options?: SecondParameter<typeof customInstance<RawPrivacyAvailableActionsResponse>>,) => {
      return customInstance<RawPrivacyAvailableActionsResponse>(
      {url: `/api/admin/users/${memberId}/instructor-profile/privacy/available-actions`, method: 'GET'
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 개인 회원 상세 조회
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 회원 관리 (`null`)
 * - 프론트 담당 영역: members (`members`)
 * - 호출 방식: `GET /api/admin/users/{memberId}/individual`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 선택 회원 ID 및 상세 탭
 * - 응답 사용 위치: 역할별 상세 화면 렌더링
 * - 프론트 조회 키: `get_api_admin_users_memberId_individual`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 라우트 준비 완료
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: MEMBER_HANDOFF_E2E
 * - 목데이터 대체: 회원 역할별 mock/localStorage 데이터를 canonical API 상태로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: MEMBER_READ 권한 필요
 * - 접근 범위: 관리자 CMS 권한 범위
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: PERSONAL_DATA 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 조회 API는 응답 원본 status/code 값을 화면 배지 라벨과 분리해서 보관합니다. 라벨은 프론트 표시용, 원본 값은 후속 API 호출 조건으로 사용합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: FE 회원 관리 핸드오프 2026-07-23 역할별 canonical 계약
 * @summary 개인 회원 상세 조회
 */
const getIndividualMemberDetail = (
    memberId: number,
 options?: SecondParameter<typeof customInstance<IndividualMemberDetailResponse>>,) => {
      return customInstance<IndividualMemberDetailResponse>(
      {url: `/api/admin/users/${memberId}/individual`, method: 'GET'
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 회원/강사 조회
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 회원 관리 (`null`)
 * - 프론트 담당 영역: members (`members`)
 * - 호출 방식: `GET /api/admin/users/{memberId}/external-identifiers`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 필터/페이지네이션/선택 행에서 요청값 전달
 * - 응답 사용 위치: 조회 캐시 및 화면 목록·상세 상태 갱신
 * - 프론트 조회 키: `get_admin_members_memberId_external-identifiers`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 라우트 준비 완료
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 기본 스모크 검증 대상
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 members API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 조회 API는 응답 원본 status/code 값을 화면 배지 라벨과 분리해서 보관합니다. 라벨은 프론트 표시용, 원본 값은 후속 API 호출 조건으로 사용합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary 회원/강사 조회
 */
const externalIdentifiers = (
    memberId: number,
 options?: SecondParameter<typeof customInstance<ExternalIdentifierResponse[]>>,) => {
      return customInstance<ExternalIdentifierResponse[]>(
      {url: `/api/admin/users/${memberId}/external-identifiers`, method: 'GET'
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 회원/강사 조회
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 회원 관리 (`null`)
 * - 프론트 담당 영역: members (`members`)
 * - 호출 방식: `GET /api/admin/users/{memberId}/consent-records`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 필터/페이지네이션/선택 행에서 요청값 전달
 * - 응답 사용 위치: 조회 캐시 및 화면 목록·상세 상태 갱신
 * - 프론트 조회 키: `get_admin_members_memberId_consent-records`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 라우트 준비 완료
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 기본 스모크 검증 대상
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 members API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 조회 API는 응답 원본 status/code 값을 화면 배지 라벨과 분리해서 보관합니다. 라벨은 프론트 표시용, 원본 값은 후속 API 호출 조건으로 사용합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary 회원/강사 조회
 */
const consentRecords = (
    memberId: number,
 options?: SecondParameter<typeof customInstance<MemberConsentRecordResponse[]>>,) => {
      return customInstance<MemberConsentRecordResponse[]>(
      {url: `/api/admin/users/${memberId}/consent-records`, method: 'GET'
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 회원/강사 조회
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 화면 직접 호출보다는 운영/진단 또는 내부 처리에서 사용합니다.
 * - 호출 방식: `GET /api/admin/users/{memberId}/applications`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: Swagger 요청 폼 또는 화면 필터/선택값
 * - 응답 사용 위치: 응답 본문을 화면 상태와 조회 캐시에 반영
 * - 프론트 조회 키: 화면별 조회 키 정책에 따름
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 준비 상태 정보 없음
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 스테이징 기본 검증 대상
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 신청/배정 상태는 `SUBMITTED`, `WAITING_REVIEW`, `APPROVED`, `REJECTED`, `AUTO_REJECTED`, `WAITING_ASSIGNMENT`, `ASSIGNED`, `CANCELLED`를 기준으로 버튼 노출과 상태 배지를 분기합니다.
 * - 동시 승인/배정 충돌은 409로 처리되며, 프론트는 목록과 상세를 재조회해 최신 상태를 보여줍니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary 회원/강사 조회
 */
const listMemberApplications = (
    memberId: number,
    params?: ListMemberApplicationsParams,
 options?: SecondParameter<typeof customInstance<PageResponseMemberApplicationHistoryResponse>>,) => {
      return customInstance<PageResponseMemberApplicationHistoryResponse>(
      {url: `/api/admin/users/${memberId}/applications`, method: 'GET',
        params
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 회원/강사 조회
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 화면 직접 호출보다는 운영/진단 또는 내부 처리에서 사용합니다.
 * - 호출 방식: `GET /api/admin/users/{memberId}/applications/{applicationId}/lecture-reports`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: Swagger 요청 폼 또는 화면 필터/선택값
 * - 응답 사용 위치: 응답 본문을 화면 상태와 조회 캐시에 반영
 * - 프론트 조회 키: 화면별 조회 키 정책에 따름
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 준비 상태 정보 없음
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 스테이징 기본 검증 대상
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: MEMBER_READ 권한 필요
 * - 접근 범위: 관리자 CMS 권한 범위
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: 기본 마스킹 응답
 * - 감사로그 저장: 필수 아님
 *
 * ### 상태값/화면 배지 기준
 * - 신청/배정 상태는 `SUBMITTED`, `WAITING_REVIEW`, `APPROVED`, `REJECTED`, `AUTO_REJECTED`, `WAITING_ASSIGNMENT`, `ASSIGNED`, `CANCELLED`를 기준으로 버튼 노출과 상태 배지를 분기합니다.
 * - 동시 승인/배정 충돌은 409로 처리되며, 프론트는 목록과 상세를 재조회해 최신 상태를 보여줍니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Stage327 회원관리/대시보드 피그마 및 프론트 핸드오프 반영
 * @summary 회원/강사 조회
 */
const listLectureReports = (
    memberId: number,
    applicationId: number,
    params?: ListLectureReportsParams,
 options?: SecondParameter<typeof customInstance<MemberLectureReportResponse[]>>,) => {
      return customInstance<MemberLectureReportResponse[]>(
      {url: `/api/admin/users/${memberId}/applications/${applicationId}/lecture-reports`, method: 'GET',
        params
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 회원 신청 건 회차별 출석 조회
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 화면 직접 호출보다는 운영/진단 또는 내부 처리에서 사용합니다.
 * - 호출 방식: `GET /api/admin/users/{memberId}/applications/{applicationId}/lecture-attendance`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: Swagger 요청 폼 또는 화면 필터/선택값
 * - 응답 사용 위치: 응답 본문을 화면 상태와 조회 캐시에 반영
 * - 프론트 조회 키: 화면별 조회 키 정책에 따름
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 준비 상태 정보 없음
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 스테이징 기본 검증 대상
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: MEMBER_READ 권한 필요
 * - 접근 범위: 관리자 CMS 권한 범위
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: PERSONAL_DATA 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 신청/배정 상태는 `SUBMITTED`, `WAITING_REVIEW`, `APPROVED`, `REJECTED`, `AUTO_REJECTED`, `WAITING_ASSIGNMENT`, `ASSIGNED`, `CANCELLED`를 기준으로 버튼 노출과 상태 배지를 분기합니다.
 * - 동시 승인/배정 충돌은 409로 처리되며, 프론트는 목록과 상세를 재조회해 최신 상태를 보여줍니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: 2026-08-25 프론트 member-management handoff cross-check
 * @summary 회원 신청 건 회차별 출석 조회
 */
const getLectureAttendance = (
    memberId: number,
    applicationId: number,
    params?: GetLectureAttendanceParams,
 options?: SecondParameter<typeof customInstance<MemberLectureAttendanceResponse>>,) => {
      return customInstance<MemberLectureAttendanceResponse>(
      {url: `/api/admin/users/${memberId}/applications/${applicationId}/lecture-attendance`, method: 'GET',
        params
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 회원/강사 조회
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 화면 직접 호출보다는 운영/진단 또는 내부 처리에서 사용합니다.
 * - 호출 방식: `GET /api/admin/users/{memberId}/applications/{applicationId}/enrollment-summary`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: Swagger 요청 폼 또는 화면 필터/선택값
 * - 응답 사용 위치: 응답 본문을 화면 상태와 조회 캐시에 반영
 * - 프론트 조회 키: 화면별 조회 키 정책에 따름
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 준비 상태 정보 없음
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 스테이징 기본 검증 대상
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: MEMBER_READ 권한 필요
 * - 접근 범위: 관리자 CMS 권한 범위
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: 기본 마스킹 응답
 * - 감사로그 저장: 필수 아님
 *
 * ### 상태값/화면 배지 기준
 * - 신청/배정 상태는 `SUBMITTED`, `WAITING_REVIEW`, `APPROVED`, `REJECTED`, `AUTO_REJECTED`, `WAITING_ASSIGNMENT`, `ASSIGNED`, `CANCELLED`를 기준으로 버튼 노출과 상태 배지를 분기합니다.
 * - 동시 승인/배정 충돌은 409로 처리되며, 프론트는 목록과 상세를 재조회해 최신 상태를 보여줍니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Stage327 회원관리/대시보드 피그마 및 프론트 핸드오프 반영
 * @summary 회원/강사 조회
 */
const getApplicationEnrollmentSummary = (
    memberId: number,
    applicationId: number,
    params?: GetApplicationEnrollmentSummaryParams,
 options?: SecondParameter<typeof customInstance<MemberEnrollmentSummaryResponse>>,) => {
      return customInstance<MemberEnrollmentSummaryResponse>(
      {url: `/api/admin/users/${memberId}/applications/${applicationId}/enrollment-summary`, method: 'GET',
        params
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 회원/강사 조회
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 화면 직접 호출보다는 운영/진단 또는 내부 처리에서 사용합니다.
 * - 호출 방식: `GET /api/admin/users/{memberId}/applications/{applicationId}/assignment-submissions`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: Swagger 요청 폼 또는 화면 필터/선택값
 * - 응답 사용 위치: 응답 본문을 화면 상태와 조회 캐시에 반영
 * - 프론트 조회 키: 화면별 조회 키 정책에 따름
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 준비 상태 정보 없음
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 스테이징 기본 검증 대상
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: MEMBER_READ 권한 필요
 * - 접근 범위: 관리자 CMS 권한 범위
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: 기본 마스킹 응답
 * - 감사로그 저장: 필수 아님
 *
 * ### 상태값/화면 배지 기준
 * - 신청/배정 상태는 `SUBMITTED`, `WAITING_REVIEW`, `APPROVED`, `REJECTED`, `AUTO_REJECTED`, `WAITING_ASSIGNMENT`, `ASSIGNED`, `CANCELLED`를 기준으로 버튼 노출과 상태 배지를 분기합니다.
 * - 동시 승인/배정 충돌은 409로 처리되며, 프론트는 목록과 상세를 재조회해 최신 상태를 보여줍니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Stage327 회원관리/대시보드 피그마 및 프론트 핸드오프 반영
 * @summary 회원/강사 조회
 */
const listAssignmentSubmissions = (
    memberId: number,
    applicationId: number,
    params?: ListAssignmentSubmissionsParams,
 options?: SecondParameter<typeof customInstance<MemberAssignmentSubmissionResponse[]>>,) => {
      return customInstance<MemberAssignmentSubmissionResponse[]>(
      {url: `/api/admin/users/${memberId}/applications/${applicationId}/assignment-submissions`, method: 'GET',
        params
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 회원/강사 조회
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 회원 관리 (`null`)
 * - 프론트 담당 영역: members (`members`)
 * - 호출 방식: `GET /api/admin/users/{memberId}/affiliated-teachers`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 선택 학교 회원 ID
 * - 응답 사용 위치: 학교 상세 소속 교사 테이블 렌더링
 * - 프론트 조회 키: `get_admin_members_memberId_affiliated-teachers`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 라우트 준비 완료
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 기본 스모크 검증 대상
 * - 목데이터 대체: 학교 상세 소속 교사 mock 테이블을 API 상태로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 조회 API는 응답 원본 status/code 값을 화면 배지 라벨과 분리해서 보관합니다. 라벨은 프론트 표시용, 원본 값은 후속 API 호출 조건으로 사용합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary 회원/강사 조회
 */
const listAffiliatedTeachers = (
    memberId: number,
 options?: SecondParameter<typeof customInstance<SchoolAffiliatedTeacherResponse[]>>,) => {
      return customInstance<SchoolAffiliatedTeacherResponse[]>(
      {url: `/api/admin/users/${memberId}/affiliated-teachers`, method: 'GET'
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 프로그램 조회
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 화면 직접 호출보다는 운영/진단 또는 내부 처리에서 사용합니다.
 * - 호출 방식: `GET /api/admin/users/{memberId}/admin-programs`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: Swagger 요청 폼 또는 화면 필터/선택값
 * - 응답 사용 위치: 응답 본문을 화면 상태와 조회 캐시에 반영
 * - 프론트 조회 키: 화면별 조회 키 정책에 따름
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 준비 상태 정보 없음
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 스테이징 기본 검증 대상
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 조회 API는 응답 원본 status/code 값을 화면 배지 라벨과 분리해서 보관합니다. 라벨은 프론트 표시용, 원본 값은 후속 API 호출 조건으로 사용합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @deprecated
 * @summary 프로그램 조회
 */
const listMemberAdminPrograms = (
    memberId: number,
    params?: ListMemberAdminProgramsParams,
 options?: SecondParameter<typeof customInstance<PageResponseMemberAdminProgramResponse>>,) => {
      return customInstance<PageResponseMemberAdminProgramResponse>(
      {url: `/api/admin/users/${memberId}/admin-programs`, method: 'GET',
        params
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 프로그램 담당 역할 옵션 조회
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 회원 권한/관리자 권한 (`null`)
 * - 프론트 담당 영역: members-permission (`members-permission`)
 * - 호출 방식: `GET /api/admin/users/program-role-options`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: authenticated API action
 * - 응답 사용 위치: 프로그램 담당 역할 옵션 조회
 * - 프론트 조회 키: `get_api_admin_users_program_role_options`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 라우트 준비 완료
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: PROGRAM_ROLE_OPTIONS_SMOKE
 * - 목데이터 대체: none
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: ADMIN_READ 권한 필요
 * - 접근 범위: GLOBAL 범위 정책
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: NOT_APPLICABLE 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 조회 API는 응답 원본 status/code 값을 화면 배지 라벨과 분리해서 보관합니다. 라벨은 프론트 표시용, 원본 값은 후속 API 호출 조건으로 사용합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: 2026-07-23 member remaining gap closure
 * @deprecated
 * @summary 프로그램 담당 역할 옵션 조회
 */
const listProgramRoleOptions = (

 options?: SecondParameter<typeof customInstance<ProgramRoleOptionResponse[]>>,) => {
      return customInstance<ProgramRoleOptionResponse[]>(
      {url: `/api/admin/users/program-role-options`, method: 'GET'
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 회원/강사 조회
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 회원 관리 (`null`)
 * - 프론트 담당 영역: members (`members`)
 * - 호출 방식: `GET /api/admin/users/pre-register-conflicts`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 필터/페이지네이션/선택 행에서 요청값 전달
 * - 응답 사용 위치: 조회 캐시 및 화면 목록·상세 상태 갱신
 * - 프론트 조회 키: `get_admin_members_pre-register-conflicts`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 라우트 준비 완료
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 기본 스모크 검증 대상
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 members API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 조회 API는 응답 원본 status/code 값을 화면 배지 라벨과 분리해서 보관합니다. 라벨은 프론트 표시용, 원본 값은 후속 API 호출 조건으로 사용합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary 회원/강사 조회
 */
const listPreRegisterConflicts = (

 options?: SecondParameter<typeof customInstance<PreRegisterConflictResponse[]>>,) => {
      return customInstance<PreRegisterConflictResponse[]>(
      {url: `/api/admin/users/pre-register-conflicts`, method: 'GET'
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - CMS 학교 소속 교사 목록 조회
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 화면 직접 호출보다는 운영/진단 또는 내부 처리에서 사용합니다.
 * - 호출 방식: `GET /api/admin/organizations/schools/{organizationId}/teachers`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: Swagger 요청 폼 또는 화면 필터/선택값
 * - 응답 사용 위치: 응답 본문을 화면 상태와 조회 캐시에 반영
 * - 프론트 조회 키: 화면별 조회 키 정책에 따름
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 준비 상태 정보 없음
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 스테이징 기본 검증 대상
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: MEMBER_READ 권한 필요
 * - 접근 범위: CMS_SCHOOL_ORGANIZATION 범위 정책
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: 기본 마스킹 응답
 * - 감사로그 저장: 필수 아님
 *
 * ### 상태값/화면 배지 기준
 * - 조회 API는 응답 원본 status/code 값을 화면 배지 라벨과 분리해서 보관합니다. 라벨은 프론트 표시용, 원본 값은 후속 API 호출 조건으로 사용합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: 학교는 member가 아니라 organizationId canonical resource; CMS admin role과 member role 분리
 * @summary CMS 학교 소속 교사 목록 조회
 */
const listTeachers = (
    organizationId: number,
 options?: SecondParameter<typeof customInstance<SchoolAffiliatedTeacherResponse[]>>,) => {
      return customInstance<SchoolAffiliatedTeacherResponse[]>(
      {url: `/api/admin/organizations/schools/${organizationId}/teachers`, method: 'GET'
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 학교 프로젝트 수강 이력 조회
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 화면 직접 호출보다는 운영/진단 또는 내부 처리에서 사용합니다.
 * - 호출 방식: `GET /api/admin/organizations/schools/{organizationId}/program-enrollment-history`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: Swagger 요청 폼 또는 화면 필터/선택값
 * - 응답 사용 위치: 응답 본문을 화면 상태와 조회 캐시에 반영
 * - 프론트 조회 키: 화면별 조회 키 정책에 따름
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 준비 상태 정보 없음
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 스테이징 기본 검증 대상
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: MEMBER_READ 권한 필요
 * - 접근 범위: 관리자 CMS 권한 범위
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: 기본 마스킹 응답
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 조회 API는 응답 원본 status/code 값을 화면 배지 라벨과 분리해서 보관합니다. 라벨은 프론트 표시용, 원본 값은 후속 API 호출 조건으로 사용합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: 2026-08-25 프론트 member-management handoff cross-check
 * @summary 학교 프로젝트 수강 이력 조회
 */
const listProgramEnrollmentHistory = (
    organizationId: number,
    params?: ListProgramEnrollmentHistoryParams,
 options?: SecondParameter<typeof customInstance<PageResponseSchoolOrganizationProgramEnrollmentHistoryItemResponse>>,) => {
      return customInstance<PageResponseSchoolOrganizationProgramEnrollmentHistoryItemResponse>(
      {url: `/api/admin/organizations/schools/${organizationId}/program-enrollment-history`, method: 'GET',
        params
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 전체 회원·관리자 통합 목록 조회
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 화면 직접 호출보다는 운영/진단 또는 내부 처리에서 사용합니다.
 * - 호출 방식: `GET /api/admin/members/all`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: Swagger 요청 폼 또는 화면 필터/선택값
 * - 응답 사용 위치: 응답 본문을 화면 상태와 조회 캐시에 반영
 * - 프론트 조회 키: 화면별 조회 키 정책에 따름
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 준비 상태 정보 없음
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 스테이징 기본 검증 대상
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: ADMIN_READ+MEMBER_READ 권한 필요
 * - 접근 범위: 관리자 CMS 권한 범위
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: 기본 마스킹 응답
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 조회 API는 응답 원본 status/code 값을 화면 배지 라벨과 분리해서 보관합니다. 라벨은 프론트 표시용, 원본 값은 후속 API 호출 조건으로 사용합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: 2026-08-12 CMS 관리자 회원 수정 요청 반영
 * @summary 전체 회원·관리자 통합 목록 조회
 */
const listAllCmsMembersAndAdmins = (
    params?: ListAllCmsMembersAndAdminsParams,
 options?: SecondParameter<typeof customInstance<PageResponseAccountDirectoryItemResponse>>,) => {
      return customInstance<PageResponseAccountDirectoryItemResponse>(
      {url: `/api/admin/members/all`, method: 'GET',
        params
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 강사 권한 신청 목록 조회
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 회원 권한/관리자 권한 (`null`)
 * - 프론트 담당 영역: 관리자-permissions (`admin-permissions`)
 * - 호출 방식: `GET /api/admin/instructor-role-requests`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 필터/페이지네이션/선택 행에서 요청값 전달
 * - 응답 사용 위치: 조회 캐시 및 화면 목록·상세 상태 갱신
 * - 프론트 조회 키: `get_admin_instructor-role-requests`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 라우트 준비 완료
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 기본 스모크 검증 대상
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 admin-permissions API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: INSTRUCTOR_REQUEST_READ 권한 필요
 * - 접근 범위: 관리자 CMS 권한 범위
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: 기본 마스킹 응답
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 조회 API는 응답 원본 status/code 값을 화면 배지 라벨과 분리해서 보관합니다. 라벨은 프론트 표시용, 원본 값은 후속 API 호출 조건으로 사용합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Stage206 member permission 프론트 gap contract: /api/admin/instructor-role-requests,강사 권한 신청 목록 조회,ADMIN,INSTRUCTOR_REQUEST_READ,ADMIN_CMS
 * @summary 강사 권한 신청 목록 조회
 */
const listInstructorRoleRequests = (
    params?: ListInstructorRoleRequestsParams,
 options?: SecondParameter<typeof customInstance<PageResponseInstructorRoleRequestListItemResponse>>,) => {
      return customInstance<PageResponseInstructorRoleRequestListItemResponse>(
      {url: `/api/admin/instructor-role-requests`, method: 'GET',
        params
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 강사 권한 신청 상세 조회
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 화면 직접 호출보다는 운영/진단 또는 내부 처리에서 사용합니다.
 * - 호출 방식: `GET /api/admin/instructor-role-requests/{requestId}`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: Swagger 요청 폼 또는 화면 필터/선택값
 * - 응답 사용 위치: 응답 본문을 화면 상태와 조회 캐시에 반영
 * - 프론트 조회 키: 화면별 조회 키 정책에 따름
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 준비 상태 정보 없음
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 스테이징 기본 검증 대상
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: INSTRUCTOR_REQUEST_READ 권한 필요
 * - 접근 범위: 관리자 CMS 권한 범위
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: 기본 마스킹 응답
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 조회 API는 응답 원본 status/code 값을 화면 배지 라벨과 분리해서 보관합니다. 라벨은 프론트 표시용, 원본 값은 후속 API 호출 조건으로 사용합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Platform 신청 당시 구조화 profile/settlement/termsAgreements snapshot 상세
 * @summary 강사 권한 신청 상세 조회
 */
const getDetail = (
    requestId: number,
 options?: SecondParameter<typeof customInstance<InstructorRoleRequestDetailResponse>>,) => {
      return customInstance<InstructorRoleRequestDetailResponse>(
      {url: `/api/admin/instructor-role-requests/${requestId}`, method: 'GET'
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 화면/운영에서 사용하는 구현 API
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 회원 권한/관리자 권한 (`null`)
 * - 프론트 담당 영역: 관리자-permissions (`admin-permissions`)
 * - 호출 방식: `GET /api/admin/admin-roles`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 필터/페이지네이션/선택 행에서 요청값 전달
 * - 응답 사용 위치: 조회 캐시 및 화면 목록·상세 상태 갱신
 * - 프론트 조회 키: `get_admin_admin-roles`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 라우트 준비 완료
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 기본 스모크 검증 대상
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 admin-permissions API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: ADMIN_PERMISSION_READ 권한 필요
 * - 접근 범위: 관리자 CMS 권한 범위
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: 개인정보 없음
 * - 감사로그 저장: 필수 아님
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 조회 API는 응답 원본 status/code 값을 화면 배지 라벨과 분리해서 보관합니다. 라벨은 프론트 표시용, 원본 값은 후속 API 호출 조건으로 사용합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: v2.8.0 P1 폼/런타임 및 마스터 워크플로우 기준 구현 완료
 * @summary 화면/운영에서 사용하는 구현 API
 */
const listRoles = (

 options?: SecondParameter<typeof customInstance<AdminRoleResponse[]>>,) => {
      return customInstance<AdminRoleResponse[]>(
      {url: `/api/admin/admin-roles`, method: 'GET'
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 화면/운영에서 사용하는 구현 API
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 회원 권한/관리자 권한 (`null`)
 * - 프론트 담당 영역: 관리자-permissions (`admin-permissions`)
 * - 호출 방식: `GET /api/admin/admin-permissions`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 필터/페이지네이션/선택 행에서 요청값 전달
 * - 응답 사용 위치: 조회 캐시 및 화면 목록·상세 상태 갱신
 * - 프론트 조회 키: `get_admin_admin-permissions`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 라우트 준비 완료
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 기본 스모크 검증 대상
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 admin-permissions API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: ADMIN_PERMISSION_READ 권한 필요
 * - 접근 범위: 관리자 CMS 권한 범위
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: 개인정보 없음
 * - 감사로그 저장: 필수 아님
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 조회 API는 응답 원본 status/code 값을 화면 배지 라벨과 분리해서 보관합니다. 라벨은 프론트 표시용, 원본 값은 후속 API 호출 조건으로 사용합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: v2.8.0 P1 폼/런타임 및 마스터 워크플로우 기준 구현 완료
 * @summary 화면/운영에서 사용하는 구현 API
 */
const listPermissions = (
    params?: ListPermissionsParams,
 options?: SecondParameter<typeof customInstance<AdminPermissionResponse[]>>,) => {
      return customInstance<AdminPermissionResponse[]>(
      {url: `/api/admin/admin-permissions`, method: 'GET',
        params
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 화면/운영에서 사용하는 구현 API
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 후원사/교재/마스터데이터 (`null`)
 * - 프론트 담당 영역: master-data (`master-data`)
 * - 호출 방식: `GET /api/admin/admin-permission-change-logs`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 필터/페이지네이션/선택 행에서 요청값 전달
 * - 응답 사용 위치: 조회 캐시 및 화면 목록·상세 상태 갱신
 * - 프론트 조회 키: `get_admin_admin-permission-change-logs`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: STAGING_VERIFY_REQUIRED
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: DATA_MANAGEMENT_QA_SIGNOFF
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 master-data API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 관리자 CMS 권한 범위
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: 개인정보 없음
 * - 감사로그 저장: 필수 아님
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 조회 API는 응답 원본 status/code 값을 화면 배지 라벨과 분리해서 보관합니다. 라벨은 프론트 표시용, 원본 값은 후속 API 호출 조건으로 사용합니다.
 * ### Swagger에서 확인할 때
 * - 감사/로그 API는 필터 기간, 대상 id, 관리자 권한에 따른 응답 차이를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: v2.8.0 P1 폼/런타임 및 마스터 워크플로우 기준 구현 완료
 * @summary 화면/운영에서 사용하는 구현 API
 */
const listPermissionChangeLogs = (
    params?: ListPermissionChangeLogsParams,
 options?: SecondParameter<typeof customInstance<PageResponseAdminPermissionChangeLogResponse>>,) => {
      return customInstance<PageResponseAdminPermissionChangeLogResponse>(
      {url: `/api/admin/admin-permission-change-logs`, method: 'GET',
        params
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 관리자 셀프 가입 승인 대기 목록 조회
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 화면 직접 호출보다는 운영/진단 또는 내부 처리에서 사용합니다.
 * - 호출 방식: `GET /api/admin/admin-approval-requests`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: Swagger 요청 폼 또는 화면 필터/선택값
 * - 응답 사용 위치: 응답 본문을 화면 상태와 조회 캐시에 반영
 * - 프론트 조회 키: 화면별 조회 키 정책에 따름
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 준비 상태 정보 없음
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 스테이징 기본 검증 대상
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: ADMIN_READ 권한 필요
 * - 접근 범위: 마스터 관리자만 가능
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: 기본 마스킹 응답
 * - 감사로그 저장: 필수 아님
 *
 * ### 상태값/화면 배지 기준
 * - 조회 API는 응답 원본 status/code 값을 화면 배지 라벨과 분리해서 보관합니다. 라벨은 프론트 표시용, 원본 값은 후속 API 호출 조건으로 사용합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: 피그마 02-2 회원 관리 > 권한 승인(관리자) 화면 전용 API
 * @summary 관리자 셀프 가입 승인 대기 목록 조회
 */
const listAdminApprovalRequests = (
    params?: ListAdminApprovalRequestsParams,
 options?: SecondParameter<typeof customInstance<PageResponseAdminAccountListItemResponse>>,) => {
      return customInstance<PageResponseAdminAccountListItemResponse>(
      {url: `/api/admin/admin-approval-requests`, method: 'GET',
        params
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 관리자 셀프 가입 승인 상세 조회
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 화면 직접 호출보다는 운영/진단 또는 내부 처리에서 사용합니다.
 * - 호출 방식: `GET /api/admin/admin-approval-requests/{adminAccountId}`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: Swagger 요청 폼 또는 화면 필터/선택값
 * - 응답 사용 위치: 응답 본문을 화면 상태와 조회 캐시에 반영
 * - 프론트 조회 키: 화면별 조회 키 정책에 따름
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 준비 상태 정보 없음
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 스테이징 기본 검증 대상
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: ADMIN_READ 권한 필요
 * - 접근 범위: 마스터 관리자만 가능
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: 기본 마스킹 응답
 * - 감사로그 저장: 필수 아님
 *
 * ### 상태값/화면 배지 기준
 * - 조회 API는 응답 원본 status/code 값을 화면 배지 라벨과 분리해서 보관합니다. 라벨은 프론트 표시용, 원본 값은 후속 API 호출 조건으로 사용합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: 승인/반려 모달 상세 데이터 제공
 * @summary 관리자 셀프 가입 승인 상세 조회
 */
const getAdminApprovalRequest = (
    adminAccountId: number,
 options?: SecondParameter<typeof customInstance<AdminAccountApprovalDetailResponse>>,) => {
      return customInstance<AdminAccountApprovalDetailResponse>(
      {url: `/api/admin/admin-approval-requests/${adminAccountId}`, method: 'GET'
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 관리자 계정 상세 조회
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 화면 직접 호출보다는 운영/진단 또는 내부 처리에서 사용합니다.
 * - 호출 방식: `GET /api/admin/admin-accounts/{adminAccountId}`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: Swagger 요청 폼 또는 화면 필터/선택값
 * - 응답 사용 위치: 응답 본문을 화면 상태와 조회 캐시에 반영
 * - 프론트 조회 키: 화면별 조회 키 정책에 따름
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 준비 상태 정보 없음
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 스테이징 기본 검증 대상
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: ADMIN_READ 권한 필요
 * - 접근 범위: 마스터 관리자만 가능
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: 기본 마스킹 응답
 * - 감사로그 저장: 필수 아님
 *
 * ### 상태값/화면 배지 기준
 * - 조회 API는 응답 원본 status/code 값을 화면 배지 라벨과 분리해서 보관합니다. 라벨은 프론트 표시용, 원본 값은 후속 API 호출 조건으로 사용합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: 관리자 상세 화면에서 기본 정보, 권한 유형, MFA 여부, 승인 정보, 소셜 연결 상태를 조회합니다.
 * @summary 관리자 계정 상세 조회
 */
const getAdminAccount = (
    adminAccountId: number,
 options?: SecondParameter<typeof customInstance<AdminAccountApprovalDetailResponse>>,) => {
      return customInstance<AdminAccountApprovalDetailResponse>(
      {url: `/api/admin/admin-accounts/${adminAccountId}`, method: 'GET'
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 관리자 계정 삭제
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 화면 직접 호출보다는 운영/진단 또는 내부 처리에서 사용합니다.
 * - 호출 방식: `DELETE /api/admin/admin-accounts/{adminAccountId}`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: Swagger 요청 폼 또는 화면 필터/선택값
 * - 응답 사용 위치: 응답 본문을 화면 상태와 조회 캐시에 반영
 * - 프론트 조회 키: 화면별 조회 키 정책에 따름
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 준비 상태 정보 없음
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 스테이징 기본 검증 대상
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: ADMIN_WRITE 권한 필요
 * - 접근 범위: 마스터 관리자만 가능
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: ADMIN_HIGH_RISK 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: MASTER 관리자가 관리자 계정을 soft delete 처리합니다. 자기 자신 삭제 불가, 소셜 연결 DISCONNECTED, tokenVersion 증가로 기존 토큰 무효화.
 * @summary 관리자 계정 삭제
 */
const deleteAdmin = (
    adminAccountId: number,
    params?: DeleteAdminParams,
 options?: SecondParameter<typeof customInstance<ApiResponseVoid>>,) => {
      return customInstance<ApiResponseVoid>(
      {url: `/api/admin/admin-accounts/${adminAccountId}`, method: 'DELETE',
        params
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 관리자 개인정보 원문 조회 가능 작업
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 화면 직접 호출보다는 운영/진단 또는 내부 처리에서 사용합니다.
 * - 호출 방식: `GET /api/admin/admin-accounts/{adminAccountId}/privacy/available-actions`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: Swagger 요청 폼 또는 화면 필터/선택값
 * - 응답 사용 위치: 응답 본문을 화면 상태와 조회 캐시에 반영
 * - 프론트 조회 키: 화면별 조회 키 정책에 따름
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 준비 상태 정보 없음
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 스테이징 기본 검증 대상
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: ADMIN_READ 권한 필요
 * - 접근 범위: ADMIN_ACCOUNT_SCOPE 범위 정책
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: 기본 마스킹 응답
 * - 감사로그 저장: 필수 아님
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 개인정보/파일 API는 마스킹 응답과 원문 접근을 구분합니다. 원문 조회, export, 민감파일 다운로드는 감사로그가 저장되어야 성공으로 취급합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: V32 adminAccountId canonical privacy/comment contract
 * @summary 관리자 개인정보 원문 조회 가능 작업
 */
const availableActions1 = (
    adminAccountId: number,
 options?: SecondParameter<typeof customInstance<RawPrivacyAvailableActionsResponse>>,) => {
      return customInstance<RawPrivacyAvailableActionsResponse>(
      {url: `/api/admin/admin-accounts/${adminAccountId}/privacy/available-actions`, method: 'GET'
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 관리자 등록 약관·동의 이력 조회
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 회원 권한/관리자 권한 (`null`)
 * - 프론트 담당 영역: 관리자-permissions (`admin-permissions`)
 * - 호출 방식: `GET /api/admin/admin-accounts/{adminAccountId}/consent-records`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 선택 관리자 ID
 * - 응답 사용 위치: 관리자 등록 약관 4종 및 동의 이력 표시
 * - 프론트 조회 키: `get_api_admin_admin_accounts_adminId_consent_records`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 라우트 준비 완료
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: CMS_ADMIN_MEMBER_HANDOFF_E2E
 * - 목데이터 대체: 관리자 등록 약관 mock/localStorage 데이터를 canonical API 상태로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: ADMIN_READ 권한 필요
 * - 접근 범위: ADMIN_ACCOUNT_SCOPE 범위 정책
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: CONSENT_METADATA_MASKED 개인정보 정책
 * - 감사로그 저장: 필수 아님
 *
 * ### 상태값/화면 배지 기준
 * - 조회 API는 응답 원본 status/code 값을 화면 배지 라벨과 분리해서 보관합니다. 라벨은 프론트 표시용, 원본 값은 후속 API 호출 조건으로 사용합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: CMS members handoff 2026-07-31 admin terms round-trip contract
 * @summary 관리자 등록 약관·동의 이력 조회
 */
const consentRecords1 = (
    adminAccountId: number,
 options?: SecondParameter<typeof customInstance<AdminTermsAgreementResponse[]>>,) => {
      return customInstance<AdminTermsAgreementResponse[]>(
      {url: `/api/admin/admin-accounts/${adminAccountId}/consent-records`, method: 'GET'
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - CMS 관리자 프로그램 담당 역할 옵션
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 화면 직접 호출보다는 운영/진단 또는 내부 처리에서 사용합니다.
 * - 호출 방식: `GET /api/admin/admin-accounts/program-role-options`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: Swagger 요청 폼 또는 화면 필터/선택값
 * - 응답 사용 위치: 응답 본문을 화면 상태와 조회 캐시에 반영
 * - 프론트 조회 키: 화면별 조회 키 정책에 따름
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 준비 상태 정보 없음
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 스테이징 기본 검증 대상
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: ADMIN_READ 권한 필요
 * - 접근 범위: 관리자 CMS 권한 범위
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: PUBLIC_METADATA 개인정보 정책
 * - 감사로그 저장: 필수 아님
 *
 * ### 상태값/화면 배지 기준
 * - 조회 API는 응답 원본 status/code 값을 화면 배지 라벨과 분리해서 보관합니다. 라벨은 프론트 표시용, 원본 값은 후속 API 호출 조건으로 사용합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: 회원 API namespace와 분리된 관리자 역할 옵션
 * @summary CMS 관리자 프로그램 담당 역할 옵션
 */
const listProgramRoleOptions1 = (

 options?: SecondParameter<typeof customInstance<AdminProgramRoleOptionResponse[]>>,) => {
      return customInstance<AdminProgramRoleOptionResponse[]>(
      {url: `/api/admin/admin-accounts/program-role-options`, method: 'GET'
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 프로그램 삭제
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 화면 직접 호출보다는 운영/진단 또는 내부 처리에서 사용합니다.
 * - 호출 방식: `DELETE /api/admin/users/{memberId}/program-history/{participantId}`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: Swagger 요청 폼 또는 화면 필터/선택값
 * - 응답 사용 위치: 응답 본문을 화면 상태와 조회 캐시에 반영
 * - 프론트 조회 키: 화면별 조회 키 정책에 따름
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 준비 상태 정보 없음
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 스테이징 기본 검증 대상
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: MEMBER_WRITE 권한 필요
 * - 접근 범위: 관리자 CMS 권한 범위
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: 기본 마스킹 응답
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 신청/배정 상태는 `SUBMITTED`, `WAITING_REVIEW`, `APPROVED`, `REJECTED`, `AUTO_REJECTED`, `WAITING_ASSIGNMENT`, `ASSIGNED`, `CANCELLED`를 기준으로 버튼 노출과 상태 배지를 분기합니다.
 * - 동시 승인/배정 충돌은 409로 처리되며, 프론트는 목록과 상세를 재조회해 최신 상태를 보여줍니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Stage327 회원관리/대시보드 피그마 및 프론트 핸드오프 반영
 * @summary 프로그램 삭제
 */
const deleteProgramHistory = (
    memberId: number,
    participantId: number,
 options?: SecondParameter<typeof customInstance<unknown>>,) => {
      return customInstance<unknown>(
      {url: `/api/admin/users/${memberId}/program-history/${participantId}`, method: 'DELETE'
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 회원/강사 삭제
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 화면 직접 호출보다는 운영/진단 또는 내부 처리에서 사용합니다.
 * - 호출 방식: `DELETE /api/admin/users/{memberId}/applications/{applicationId}`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: Swagger 요청 폼 또는 화면 필터/선택값
 * - 응답 사용 위치: 응답 본문을 화면 상태와 조회 캐시에 반영
 * - 프론트 조회 키: 화면별 조회 키 정책에 따름
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 준비 상태 정보 없음
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 스테이징 기본 검증 대상
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: MEMBER_WRITE 권한 필요
 * - 접근 범위: 관리자 CMS 권한 범위
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: 기본 마스킹 응답
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 신청/배정 상태는 `SUBMITTED`, `WAITING_REVIEW`, `APPROVED`, `REJECTED`, `AUTO_REJECTED`, `WAITING_ASSIGNMENT`, `ASSIGNED`, `CANCELLED`를 기준으로 버튼 노출과 상태 배지를 분기합니다.
 * - 동시 승인/배정 충돌은 409로 처리되며, 프론트는 목록과 상세를 재조회해 최신 상태를 보여줍니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Stage327 회원관리/대시보드 피그마 및 프론트 핸드오프 반영
 * @summary 회원/강사 삭제
 */
const deleteApplicationHistory = (
    memberId: number,
    applicationId: number,
    params?: DeleteApplicationHistoryParams,
 options?: SecondParameter<typeof customInstance<unknown>>,) => {
      return customInstance<unknown>(
      {url: `/api/admin/users/${memberId}/applications/${applicationId}`, method: 'DELETE',
        params
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 프로그램 삭제
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 화면 직접 호출보다는 운영/진단 또는 내부 처리에서 사용합니다.
 * - 호출 방식: `DELETE /api/admin/users/{memberId}/admin-programs/{programId}`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: Swagger 요청 폼 또는 화면 필터/선택값
 * - 응답 사용 위치: 응답 본문을 화면 상태와 조회 캐시에 반영
 * - 프론트 조회 키: 화면별 조회 키 정책에 따름
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 준비 상태 정보 없음
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 스테이징 기본 검증 대상
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: ADMIN_WRITE 권한 필요
 * - 접근 범위: 관리자 CMS 권한 범위
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: 기본 마스킹 응답
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Stage327 회원관리/대시보드 피그마 및 프론트 핸드오프 반영
 * @deprecated
 * @summary 프로그램 삭제
 */
const deleteAdminProgram = (
    memberId: number,
    programId: number,
 options?: SecondParameter<typeof customInstance<unknown>>,) => {
      return customInstance<unknown>(
      {url: `/api/admin/users/${memberId}/admin-programs/${programId}`, method: 'DELETE'
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - CMS 관리자 프로그램 담당 역할 해제
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 화면 직접 호출보다는 운영/진단 또는 내부 처리에서 사용합니다.
 * - 호출 방식: `DELETE /api/admin/admin-accounts/{adminAccountId}/program-roles/{programId}`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: Swagger 요청 폼 또는 화면 필터/선택값
 * - 응답 사용 위치: 응답 본문을 화면 상태와 조회 캐시에 반영
 * - 프론트 조회 키: 화면별 조회 키 정책에 따름
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 준비 상태 정보 없음
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 스테이징 기본 검증 대상
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: ADMIN_WRITE 권한 필요
 * - 접근 범위: 관리자 CMS 권한 범위
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: 기본 마스킹 응답
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: admin_account.id canonical write boundary
 * @summary CMS 관리자 프로그램 담당 역할 해제
 */
const deleteProgramRole = (
    adminAccountId: number,
    programId: number,
 options?: SecondParameter<typeof customInstance<unknown>>,) => {
      return customInstance<unknown>(
      {url: `/api/admin/admin-accounts/${adminAccountId}/program-roles/${programId}`, method: 'DELETE'
    },
      options);
    }

return {listProgramRoles,saveProgramRoles,getRolePermissions,updateRolePermissions,listProgramRoles1,saveProgramRoles1,unmaskMemberPrivacy,unmaskInstructorMemberPrivacy,unmaskInstructorPrivacy,unmaskIndividualMemberPrivacy,deleteAndAnonymize,consentFilledDocument,listMemberComments,createMemberComment,preRegisterSchool,preRegisterInstructor,preRegisterIndividual,resolvePreRegisterConflict,bulkDeleteAndAnonymize,listSchools,createSchool,bulkDeleteProgramEnrollmentHistory,bulkDeleteSchools,bulkDelete1,resetPending,resendNotification,reject2,unmask,approve1,bulkReject,bulkApprove,resetAdminApprovalToPending,resendAdminApprovalNotification,rejectAdminApprovalRequest,approveAdminApprovalRequest,bulkRejectAdminApprovalRequests,bulkApproveAdminApprovalRequests,listAdmins,createAdmin,verifyAdmin,bulkDeleteProgramRoles,unmask1,resetAdminPassword,listComments,createComment,bulkDeleteAdmins,updateMemberBasicInfo,upsertExternalIdentifier,deleteMemberComment,updateMemberComment,updateAffiliatedTeacherEmploymentStatus,getSchool,deleteSchool,updateSchool,updateTeacherEmploymentStatus,changeAdminStatus,changeAdminRole,deleteComment,updateComment,updateAdminBasicInfo,listMembers,getTeacherMemberDetail,getSchoolMemberDetail,listMemberProgramHistory,getMemberPrivacyAvailableActions,getInstructorMemberDetail,getInstructorDetail,getInstructorPrivacyAvailableActions,getIndividualMemberDetail,externalIdentifiers,consentRecords,listMemberApplications,listLectureReports,getLectureAttendance,getApplicationEnrollmentSummary,listAssignmentSubmissions,listAffiliatedTeachers,listMemberAdminPrograms,listProgramRoleOptions,listPreRegisterConflicts,listTeachers,listProgramEnrollmentHistory,listAllCmsMembersAndAdmins,listInstructorRoleRequests,getDetail,listRoles,listPermissions,listPermissionChangeLogs,listAdminApprovalRequests,getAdminApprovalRequest,getAdminAccount,deleteAdmin,availableActions1,consentRecords1,listProgramRoleOptions1,deleteProgramHistory,deleteApplicationHistory,deleteAdminProgram,deleteProgramRole}};
export type ListProgramRolesResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['listProgramRoles']>>>
export type SaveProgramRolesResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['saveProgramRoles']>>>
export type GetRolePermissionsResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['getRolePermissions']>>>
export type UpdateRolePermissionsResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['updateRolePermissions']>>>
export type ListProgramRoles1Result = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['listProgramRoles1']>>>
export type SaveProgramRoles1Result = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['saveProgramRoles1']>>>
export type UnmaskMemberPrivacyResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['unmaskMemberPrivacy']>>>
export type UnmaskInstructorMemberPrivacyResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['unmaskInstructorMemberPrivacy']>>>
export type UnmaskInstructorPrivacyResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['unmaskInstructorPrivacy']>>>
export type UnmaskIndividualMemberPrivacyResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['unmaskIndividualMemberPrivacy']>>>
export type DeleteAndAnonymizeResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['deleteAndAnonymize']>>>
export type ConsentFilledDocumentResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['consentFilledDocument']>>>
export type ListMemberCommentsResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['listMemberComments']>>>
export type CreateMemberCommentResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['createMemberComment']>>>
export type PreRegisterSchoolResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['preRegisterSchool']>>>
export type PreRegisterInstructorResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['preRegisterInstructor']>>>
export type PreRegisterIndividualResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['preRegisterIndividual']>>>
export type ResolvePreRegisterConflictResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['resolvePreRegisterConflict']>>>
export type BulkDeleteAndAnonymizeResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['bulkDeleteAndAnonymize']>>>
export type ListSchoolsResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['listSchools']>>>
export type CreateSchoolResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['createSchool']>>>
export type BulkDeleteProgramEnrollmentHistoryResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['bulkDeleteProgramEnrollmentHistory']>>>
export type BulkDeleteSchoolsResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['bulkDeleteSchools']>>>
export type BulkDelete1Result = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['bulkDelete1']>>>
export type ResetPendingResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['resetPending']>>>
export type ResendNotificationResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['resendNotification']>>>
export type Reject2Result = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['reject2']>>>
export type UnmaskResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['unmask']>>>
export type Approve1Result = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['approve1']>>>
export type BulkRejectResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['bulkReject']>>>
export type BulkApproveResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['bulkApprove']>>>
export type ResetAdminApprovalToPendingResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['resetAdminApprovalToPending']>>>
export type ResendAdminApprovalNotificationResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['resendAdminApprovalNotification']>>>
export type RejectAdminApprovalRequestResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['rejectAdminApprovalRequest']>>>
export type ApproveAdminApprovalRequestResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['approveAdminApprovalRequest']>>>
export type BulkRejectAdminApprovalRequestsResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['bulkRejectAdminApprovalRequests']>>>
export type BulkApproveAdminApprovalRequestsResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['bulkApproveAdminApprovalRequests']>>>
export type ListAdminsResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['listAdmins']>>>
export type CreateAdminResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['createAdmin']>>>
export type VerifyAdminResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['verifyAdmin']>>>
export type BulkDeleteProgramRolesResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['bulkDeleteProgramRoles']>>>
export type Unmask1Result = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['unmask1']>>>
export type ResetAdminPasswordResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['resetAdminPassword']>>>
export type ListCommentsResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['listComments']>>>
export type CreateCommentResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['createComment']>>>
export type BulkDeleteAdminsResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['bulkDeleteAdmins']>>>
export type UpdateMemberBasicInfoResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['updateMemberBasicInfo']>>>
export type UpsertExternalIdentifierResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['upsertExternalIdentifier']>>>
export type DeleteMemberCommentResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['deleteMemberComment']>>>
export type UpdateMemberCommentResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['updateMemberComment']>>>
export type UpdateAffiliatedTeacherEmploymentStatusResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['updateAffiliatedTeacherEmploymentStatus']>>>
export type GetSchoolResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['getSchool']>>>
export type DeleteSchoolResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['deleteSchool']>>>
export type UpdateSchoolResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['updateSchool']>>>
export type UpdateTeacherEmploymentStatusResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['updateTeacherEmploymentStatus']>>>
export type ChangeAdminStatusResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['changeAdminStatus']>>>
export type ChangeAdminRoleResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['changeAdminRole']>>>
export type DeleteCommentResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['deleteComment']>>>
export type UpdateCommentResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['updateComment']>>>
export type UpdateAdminBasicInfoResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['updateAdminBasicInfo']>>>
export type ListMembersResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['listMembers']>>>
export type GetTeacherMemberDetailResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['getTeacherMemberDetail']>>>
export type GetSchoolMemberDetailResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['getSchoolMemberDetail']>>>
export type ListMemberProgramHistoryResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['listMemberProgramHistory']>>>
export type GetMemberPrivacyAvailableActionsResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['getMemberPrivacyAvailableActions']>>>
export type GetInstructorMemberDetailResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['getInstructorMemberDetail']>>>
export type GetInstructorDetailResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['getInstructorDetail']>>>
export type GetInstructorPrivacyAvailableActionsResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['getInstructorPrivacyAvailableActions']>>>
export type GetIndividualMemberDetailResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['getIndividualMemberDetail']>>>
export type ExternalIdentifiersResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['externalIdentifiers']>>>
export type ConsentRecordsResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['consentRecords']>>>
export type ListMemberApplicationsResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['listMemberApplications']>>>
export type ListLectureReportsResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['listLectureReports']>>>
export type GetLectureAttendanceResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['getLectureAttendance']>>>
export type GetApplicationEnrollmentSummaryResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['getApplicationEnrollmentSummary']>>>
export type ListAssignmentSubmissionsResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['listAssignmentSubmissions']>>>
export type ListAffiliatedTeachersResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['listAffiliatedTeachers']>>>
export type ListMemberAdminProgramsResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['listMemberAdminPrograms']>>>
export type ListProgramRoleOptionsResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['listProgramRoleOptions']>>>
export type ListPreRegisterConflictsResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['listPreRegisterConflicts']>>>
export type ListTeachersResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['listTeachers']>>>
export type ListProgramEnrollmentHistoryResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['listProgramEnrollmentHistory']>>>
export type ListAllCmsMembersAndAdminsResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['listAllCmsMembersAndAdmins']>>>
export type ListInstructorRoleRequestsResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['listInstructorRoleRequests']>>>
export type GetDetailResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['getDetail']>>>
export type ListRolesResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['listRoles']>>>
export type ListPermissionsResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['listPermissions']>>>
export type ListPermissionChangeLogsResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['listPermissionChangeLogs']>>>
export type ListAdminApprovalRequestsResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['listAdminApprovalRequests']>>>
export type GetAdminApprovalRequestResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['getAdminApprovalRequest']>>>
export type GetAdminAccountResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['getAdminAccount']>>>
export type DeleteAdminResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['deleteAdmin']>>>
export type AvailableActions1Result = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['availableActions1']>>>
export type ConsentRecords1Result = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['consentRecords1']>>>
export type ListProgramRoleOptions1Result = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['listProgramRoleOptions1']>>>
export type DeleteProgramHistoryResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['deleteProgramHistory']>>>
export type DeleteApplicationHistoryResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['deleteApplicationHistory']>>>
export type DeleteAdminProgramResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['deleteAdminProgram']>>>
export type DeleteProgramRoleResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIMembersSubset>['deleteProgramRole']>>>
