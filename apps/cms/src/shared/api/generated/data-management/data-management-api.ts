/**
 * Generated by orval v8.17.0 🍺
 * Do not edit manually.
 * JA Korea CMS Backend API — Data Management subset
 * Filtered for CMS data management Orval codegen.
 * OpenAPI spec version: v9
 */
import type {
  ApiResponseBulkActionResponse,
  ApiResponseDeleteResponse,
  ApiResponseDetailedProgramDeleteResponse,
  ApiResponseDetailedProgramResponse,
  ApiResponseMaterialKitResponse,
  ApiResponseMaterialKitVersionResponse,
  ApiResponseSponsorContactResponse,
  ApiResponseSponsorDeleteResponse,
  ApiResponseSponsorResponse,
  ApiResponseSponsorYearlyBusinessResponse,
  ApiResponseTextbookResponse,
  ApiResponseVoid,
  BulkIdsRequest,
  CalculateParams,
  CurrentKitCalculationParams,
  DetailedProgramRequest,
  DetailedProgramResponse,
  DetailedProgramsParams,
  KitsParams,
  MatchesParams,
  MaterialKitCalculationResponse,
  MaterialKitRequest,
  MaterialKitResponse,
  MaterialKitTargetCountRequest,
  MaterialKitVersionRequest,
  MaterialKitVersionResponse,
  PageResponseDetailedProgramResponse,
  PageResponseMaterialKitResponse,
  PageResponseSponsorProgramHistoryResponse,
  PageResponseTextbookResponse,
  ProgramHistoriesParams,
  SponsorContactRequest,
  SponsorContactResponse,
  SponsorDetailResponse,
  SponsorRequest,
  SponsorResponse,
  SponsorYearlyBusinessRequest,
  SponsorYearlyBusinessResponse,
  SponsorsParams,
  TextbookMatchResponse,
  TextbookRequest,
  TextbookResponse,
  TextbooksParams
} from './schemas';

import { customInstance } from '../../orval-mutator';


type SecondParameter<T extends (...args: never) => unknown> = Parameters<T>[1];


  export const getJAKoreaCMSBackendAPIDataManagementSubset = () => {
/**
 * ### 이 API가 하는 일
 * - 관리자 조회
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 후원사/교재/마스터데이터 (`null`)
 * - 프론트 담당 영역: master-data (`master-data`)
 * - 호출 방식: `GET /api/admin/textbooks`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 필터/페이지네이션/선택 행에서 요청값 전달
 * - 응답 사용 위치: 조회 캐시 및 화면 목록·상세 상태 갱신
 * - 프론트 조회 키: `get_admin_textbooks`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: STAGING_VERIFY_REQUIRED
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: DATA_MANAGEMENT_QA_SIGNOFF
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 master-data API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 조회 API는 응답 원본 status/code 값을 화면 배지 라벨과 분리해서 보관합니다. 라벨은 프론트 표시용, 원본 값은 후속 API 호출 조건으로 사용합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary 관리자 조회
 */
const textbooks = (
    params?: TextbooksParams,
 options?: SecondParameter<typeof customInstance<PageResponseTextbookResponse>>,) => {
      return customInstance<PageResponseTextbookResponse>(
      {url: `/api/admin/textbooks`, method: 'GET',
        params
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - POST /api/admin/textbooks
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 후원사/교재/마스터데이터 (`null`)
 * - 프론트 담당 영역: master-data (`master-data`)
 * - 호출 방식: `POST /api/admin/textbooks`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 폼 상태 or 선택 행 action payload
 * - 응답 사용 위치: 변경 결과 then 관련 조회 키 갱신
 * - 프론트 조회 키: `post_admin_textbooks`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: STAGING_VERIFY_REQUIRED
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: DATA_MANAGEMENT_QA_SIGNOFF
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 master-data API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary POST /api/admin/textbooks
 */
const create1 = (
    textbookRequest: TextbookRequest,
 options?: SecondParameter<typeof customInstance<ApiResponseTextbookResponse>>,) => {
      return customInstance<ApiResponseTextbookResponse>(
      {url: `/api/admin/textbooks`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: textbookRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - POST /api/admin/textbooks/bulk-delete
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 화면 직접 호출보다는 운영/진단 또는 내부 처리에서 사용합니다.
 * - 호출 방식: `POST /api/admin/textbooks/bulk-delete`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: Swagger 요청 폼 또는 화면 필터/선택값
 * - 응답 사용 위치: 응답 본문을 화면 상태와 조회 캐시에 반영
 * - 프론트 조회 키: 화면별 조회 키 정책에 따름
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 준비 상태 정보 없음
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 스테이징 기본 검증 대상
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: MASTER_DATA_WRITE 권한 필요
 * - 접근 범위: GLOBAL 범위 정책
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: NONE 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Frontend bulk API handoff implemented with max 100 and partial-success contract
 * @summary POST /api/admin/textbooks/bulk-delete
 */
const bulkDelete = (
    bulkIdsRequest: BulkIdsRequest,
 options?: SecondParameter<typeof customInstance<ApiResponseBulkActionResponse>>,) => {
      return customInstance<ApiResponseBulkActionResponse>(
      {url: `/api/admin/textbooks/bulk-delete`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: bulkIdsRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 관리자 조회
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 후원사/교재/마스터데이터 (`null`)
 * - 프론트 담당 영역: master-data (`master-data`)
 * - 호출 방식: `GET /api/admin/sponsors`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 필터/페이지네이션/선택 행에서 요청값 전달
 * - 응답 사용 위치: 조회 캐시 및 화면 목록·상세 상태 갱신
 * - 프론트 조회 키: `get_admin_sponsors`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: STAGING_VERIFY_REQUIRED
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: DATA_MANAGEMENT_QA_SIGNOFF
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 master-data API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 조회 API는 응답 원본 status/code 값을 화면 배지 라벨과 분리해서 보관합니다. 라벨은 프론트 표시용, 원본 값은 후속 API 호출 조건으로 사용합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary 관리자 조회
 */
const sponsors = (
    params?: SponsorsParams,
 options?: SecondParameter<typeof customInstance<SponsorResponse[]>>,) => {
      return customInstance<SponsorResponse[]>(
      {url: `/api/admin/sponsors`, method: 'GET',
        params
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - POST /api/admin/sponsors
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 후원사/교재/마스터데이터 (`null`)
 * - 프론트 담당 영역: master-data (`master-data`)
 * - 호출 방식: `POST /api/admin/sponsors`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 폼 상태 or 선택 행 action payload
 * - 응답 사용 위치: 변경 결과 then 관련 조회 키 갱신
 * - 프론트 조회 키: `post_admin_sponsors`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: STAGING_VERIFY_REQUIRED
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: DATA_MANAGEMENT_QA_SIGNOFF
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 master-data API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary POST /api/admin/sponsors
 */
const create4 = (
    sponsorRequest: SponsorRequest,
 options?: SecondParameter<typeof customInstance<ApiResponseSponsorResponse>>,) => {
      return customInstance<ApiResponseSponsorResponse>(
      {url: `/api/admin/sponsors`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: sponsorRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 관리자 조회
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 후원사/교재/마스터데이터 (`null`)
 * - 프론트 담당 영역: master-data (`master-data`)
 * - 호출 방식: `GET /api/admin/sponsors/{sponsorId}/yearly-businesses`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 필터/페이지네이션/선택 행에서 요청값 전달
 * - 응답 사용 위치: 조회 캐시 및 화면 목록·상세 상태 갱신
 * - 프론트 조회 키: `get_admin_sponsors_sponsorId_yearly-businesses`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: STAGING_VERIFY_REQUIRED
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: DATA_MANAGEMENT_QA_SIGNOFF
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 master-data API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 조회 API는 응답 원본 status/code 값을 화면 배지 라벨과 분리해서 보관합니다. 라벨은 프론트 표시용, 원본 값은 후속 API 호출 조건으로 사용합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary 관리자 조회
 */
const yearlyBusinesses = (
    sponsorId: number,
 options?: SecondParameter<typeof customInstance<SponsorYearlyBusinessResponse[]>>,) => {
      return customInstance<SponsorYearlyBusinessResponse[]>(
      {url: `/api/admin/sponsors/${sponsorId}/yearly-businesses`, method: 'GET'
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - POST /api/admin/sponsors/{sponsorId}/yearly-businesses
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 후원사/교재/마스터데이터 (`null`)
 * - 프론트 담당 영역: master-data (`master-data`)
 * - 호출 방식: `POST /api/admin/sponsors/{sponsorId}/yearly-businesses`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 폼 상태 or 선택 행 action payload
 * - 응답 사용 위치: 변경 결과 then 관련 조회 키 갱신
 * - 프론트 조회 키: `post_admin_sponsors_sponsorId_yearly-businesses`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: STAGING_VERIFY_REQUIRED
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: DATA_MANAGEMENT_QA_SIGNOFF
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 master-data API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary POST /api/admin/sponsors/{sponsorId}/yearly-businesses
 */
const addYearlyBusiness = (
    sponsorId: number,
    sponsorYearlyBusinessRequest: SponsorYearlyBusinessRequest,
 options?: SecondParameter<typeof customInstance<ApiResponseSponsorYearlyBusinessResponse>>,) => {
      return customInstance<ApiResponseSponsorYearlyBusinessResponse>(
      {url: `/api/admin/sponsors/${sponsorId}/yearly-businesses`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: sponsorYearlyBusinessRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - POST /api/admin/sponsors/{sponsorId}/end
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 후원사/교재/마스터데이터 (`null`)
 * - 프론트 담당 영역: master-data (`master-data`)
 * - 호출 방식: `POST /api/admin/sponsors/{sponsorId}/end`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 폼 상태 or 선택 행 action payload
 * - 응답 사용 위치: 변경 결과 then 관련 조회 키 갱신
 * - 프론트 조회 키: `post_admin_sponsors_sponsorId_end`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: STAGING_VERIFY_REQUIRED
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: DATA_MANAGEMENT_QA_SIGNOFF
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 master-data API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary POST /api/admin/sponsors/{sponsorId}/end
 */
const end = (
    sponsorId: number,
 options?: SecondParameter<typeof customInstance<ApiResponseSponsorResponse>>,) => {
      return customInstance<ApiResponseSponsorResponse>(
      {url: `/api/admin/sponsors/${sponsorId}/end`, method: 'POST'
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 관리자 조회
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 후원사/교재/마스터데이터 (`null`)
 * - 프론트 담당 영역: master-data (`master-data`)
 * - 호출 방식: `GET /api/admin/sponsors/{sponsorId}/contacts`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 필터/페이지네이션/선택 행에서 요청값 전달
 * - 응답 사용 위치: 조회 캐시 및 화면 목록·상세 상태 갱신
 * - 프론트 조회 키: `get_admin_sponsors_sponsorId_contacts`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: STAGING_VERIFY_REQUIRED
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: DATA_MANAGEMENT_QA_SIGNOFF
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 master-data API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 조회 API는 응답 원본 status/code 값을 화면 배지 라벨과 분리해서 보관합니다. 라벨은 프론트 표시용, 원본 값은 후속 API 호출 조건으로 사용합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary 관리자 조회
 */
const contacts = (
    sponsorId: number,
 options?: SecondParameter<typeof customInstance<SponsorContactResponse[]>>,) => {
      return customInstance<SponsorContactResponse[]>(
      {url: `/api/admin/sponsors/${sponsorId}/contacts`, method: 'GET'
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - POST /api/admin/sponsors/{sponsorId}/contacts
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 후원사/교재/마스터데이터 (`null`)
 * - 프론트 담당 영역: master-data (`master-data`)
 * - 호출 방식: `POST /api/admin/sponsors/{sponsorId}/contacts`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 폼 상태 or 선택 행 action payload
 * - 응답 사용 위치: 변경 결과 then 관련 조회 키 갱신
 * - 프론트 조회 키: `post_admin_sponsors_sponsorId_contacts`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: STAGING_VERIFY_REQUIRED
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: DATA_MANAGEMENT_QA_SIGNOFF
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 master-data API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary POST /api/admin/sponsors/{sponsorId}/contacts
 */
const addContact = (
    sponsorId: number,
    sponsorContactRequest: SponsorContactRequest,
 options?: SecondParameter<typeof customInstance<ApiResponseSponsorContactResponse>>,) => {
      return customInstance<ApiResponseSponsorContactResponse>(
      {url: `/api/admin/sponsors/${sponsorId}/contacts`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: sponsorContactRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - POST /api/admin/sponsors/contacts/bulk-delete
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 화면 직접 호출보다는 운영/진단 또는 내부 처리에서 사용합니다.
 * - 호출 방식: `POST /api/admin/sponsors/contacts/bulk-delete`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: Swagger 요청 폼 또는 화면 필터/선택값
 * - 응답 사용 위치: 응답 본문을 화면 상태와 조회 캐시에 반영
 * - 프론트 조회 키: 화면별 조회 키 정책에 따름
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 준비 상태 정보 없음
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 스테이징 기본 검증 대상
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: MASTER_DATA_WRITE 권한 필요
 * - 접근 범위: GLOBAL 범위 정책
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: NONE 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Frontend bulk API handoff implemented with max 100 and partial-success contract
 * @summary POST /api/admin/sponsors/contacts/bulk-delete
 */
const bulkDeleteContacts = (
    bulkIdsRequest: BulkIdsRequest,
 options?: SecondParameter<typeof customInstance<ApiResponseBulkActionResponse>>,) => {
      return customInstance<ApiResponseBulkActionResponse>(
      {url: `/api/admin/sponsors/contacts/bulk-delete`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: bulkIdsRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - POST /api/admin/sponsors/bulk-delete
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 화면 직접 호출보다는 운영/진단 또는 내부 처리에서 사용합니다.
 * - 호출 방식: `POST /api/admin/sponsors/bulk-delete`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: Swagger 요청 폼 또는 화면 필터/선택값
 * - 응답 사용 위치: 응답 본문을 화면 상태와 조회 캐시에 반영
 * - 프론트 조회 키: 화면별 조회 키 정책에 따름
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 준비 상태 정보 없음
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 스테이징 기본 검증 대상
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: MASTER_DATA_WRITE 권한 필요
 * - 접근 범위: GLOBAL 범위 정책
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: NONE 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Frontend bulk API handoff implemented with max 100 and partial-success contract
 * @summary POST /api/admin/sponsors/bulk-delete
 */
const bulkDeleteSponsors = (
    bulkIdsRequest: BulkIdsRequest,
 options?: SecondParameter<typeof customInstance<ApiResponseBulkActionResponse>>,) => {
      return customInstance<ApiResponseBulkActionResponse>(
      {url: `/api/admin/sponsors/bulk-delete`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: bulkIdsRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 관리자 조회
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 후원사/교재/마스터데이터 (`null`)
 * - 프론트 담당 영역: master-data (`master-data`)
 * - 호출 방식: `GET /api/admin/material-kits`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 필터/페이지네이션/선택 행에서 요청값 전달
 * - 응답 사용 위치: 조회 캐시 및 화면 목록·상세 상태 갱신
 * - 프론트 조회 키: `get_admin_material-kits`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: STAGING_VERIFY_REQUIRED
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: DATA_MANAGEMENT_QA_SIGNOFF
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 master-data API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 조회 API는 응답 원본 status/code 값을 화면 배지 라벨과 분리해서 보관합니다. 라벨은 프론트 표시용, 원본 값은 후속 API 호출 조건으로 사용합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary 관리자 조회
 */
const kits = (
    params?: KitsParams,
 options?: SecondParameter<typeof customInstance<PageResponseMaterialKitResponse>>,) => {
      return customInstance<PageResponseMaterialKitResponse>(
      {url: `/api/admin/material-kits`, method: 'GET',
        params
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - POST /api/admin/material-kits
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 후원사/교재/마스터데이터 (`null`)
 * - 프론트 담당 영역: master-data (`master-data`)
 * - 호출 방식: `POST /api/admin/material-kits`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 폼 상태 or 선택 행 action payload
 * - 응답 사용 위치: 변경 결과 then 관련 조회 키 갱신
 * - 프론트 조회 키: `post_admin_material-kits`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: STAGING_VERIFY_REQUIRED
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: DATA_MANAGEMENT_QA_SIGNOFF
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 master-data API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary POST /api/admin/material-kits
 */
const create6 = (
    materialKitRequest: MaterialKitRequest,
 options?: SecondParameter<typeof customInstance<ApiResponseMaterialKitResponse>>,) => {
      return customInstance<ApiResponseMaterialKitResponse>(
      {url: `/api/admin/material-kits`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: materialKitRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 관리자 조회
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 후원사/교재/마스터데이터 (`null`)
 * - 프론트 담당 영역: master-data (`master-data`)
 * - 호출 방식: `GET /api/admin/material-kits/{kitId}/versions`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 필터/페이지네이션/선택 행에서 요청값 전달
 * - 응답 사용 위치: 조회 캐시 및 화면 목록·상세 상태 갱신
 * - 프론트 조회 키: `get_admin_material-kits_kitId_versions`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: STAGING_VERIFY_REQUIRED
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: DATA_MANAGEMENT_QA_SIGNOFF
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 master-data API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 조회 API는 응답 원본 status/code 값을 화면 배지 라벨과 분리해서 보관합니다. 라벨은 프론트 표시용, 원본 값은 후속 API 호출 조건으로 사용합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary 관리자 조회
 */
const versions = (
    kitId: number,
 options?: SecondParameter<typeof customInstance<MaterialKitVersionResponse[]>>,) => {
      return customInstance<MaterialKitVersionResponse[]>(
      {url: `/api/admin/material-kits/${kitId}/versions`, method: 'GET'
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - POST /api/admin/material-kits/{kitId}/versions
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 후원사/교재/마스터데이터 (`null`)
 * - 프론트 담당 영역: master-data (`master-data`)
 * - 호출 방식: `POST /api/admin/material-kits/{kitId}/versions`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 폼 상태 or 선택 행 action payload
 * - 응답 사용 위치: 변경 결과 then 관련 조회 키 갱신
 * - 프론트 조회 키: `post_admin_material-kits_kitId_versions`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: STAGING_VERIFY_REQUIRED
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: DATA_MANAGEMENT_QA_SIGNOFF
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 master-data API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary POST /api/admin/material-kits/{kitId}/versions
 */
const createVersion = (
    kitId: number,
    materialKitVersionRequest: MaterialKitVersionRequest,
 options?: SecondParameter<typeof customInstance<ApiResponseMaterialKitVersionResponse>>,) => {
      return customInstance<ApiResponseMaterialKitVersionResponse>(
      {url: `/api/admin/material-kits/${kitId}/versions`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: materialKitVersionRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - POST /api/admin/material-kits/versions/{versionId}/target-counts
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 후원사/교재/마스터데이터 (`null`)
 * - 프론트 담당 영역: master-data (`master-data`)
 * - 호출 방식: `POST /api/admin/material-kits/versions/{versionId}/target-counts`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 폼 상태 or 선택 행 action payload
 * - 응답 사용 위치: 변경 결과 then 관련 조회 키 갱신
 * - 프론트 조회 키: `post_admin_material-kits_versions_versionId_target-counts`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: STAGING_VERIFY_REQUIRED
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: DATA_MANAGEMENT_QA_SIGNOFF
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 master-data API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary POST /api/admin/material-kits/versions/{versionId}/target-counts
 */
const addTargetCount = (
    versionId: number,
    materialKitTargetCountRequest: MaterialKitTargetCountRequest,
 options?: SecondParameter<typeof customInstance<ApiResponseVoid>>,) => {
      return customInstance<ApiResponseVoid>(
      {url: `/api/admin/material-kits/versions/${versionId}/target-counts`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: materialKitTargetCountRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 프로그램 조회
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 후원사/교재/마스터데이터 (`null`)
 * - 프론트 담당 영역: master-data (`master-data`)
 * - 호출 방식: `GET /api/admin/detailed-programs`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 필터/페이지네이션/선택 행에서 요청값 전달
 * - 응답 사용 위치: 조회 캐시 및 화면 목록·상세 상태 갱신
 * - 프론트 조회 키: `get_admin_detailed-programs`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: STAGING_VERIFY_REQUIRED
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: DATA_MANAGEMENT_QA_SIGNOFF
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 master-data API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 조회 API는 응답 원본 status/code 값을 화면 배지 라벨과 분리해서 보관합니다. 라벨은 프론트 표시용, 원본 값은 후속 API 호출 조건으로 사용합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary 프로그램 조회
 */
const detailedPrograms = (
    params?: DetailedProgramsParams,
 options?: SecondParameter<typeof customInstance<PageResponseDetailedProgramResponse>>,) => {
      return customInstance<PageResponseDetailedProgramResponse>(
      {url: `/api/admin/detailed-programs`, method: 'GET',
        params
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - POST /api/admin/detailed-programs
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 후원사/교재/마스터데이터 (`null`)
 * - 프론트 담당 영역: master-data (`master-data`)
 * - 호출 방식: `POST /api/admin/detailed-programs`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 폼 상태 or 선택 행 action payload
 * - 응답 사용 위치: 변경 결과 then 관련 조회 키 갱신
 * - 프론트 조회 키: `post_admin_detailed-programs`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: STAGING_VERIFY_REQUIRED
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: DATA_MANAGEMENT_QA_SIGNOFF
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 master-data API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary POST /api/admin/detailed-programs
 */
const create7 = (
    detailedProgramRequest: DetailedProgramRequest,
 options?: SecondParameter<typeof customInstance<ApiResponseDetailedProgramResponse>>,) => {
      return customInstance<ApiResponseDetailedProgramResponse>(
      {url: `/api/admin/detailed-programs`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: detailedProgramRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - POST /api/admin/detailed-programs/bulk-delete
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 화면 직접 호출보다는 운영/진단 또는 내부 처리에서 사용합니다.
 * - 호출 방식: `POST /api/admin/detailed-programs/bulk-delete`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: Swagger 요청 폼 또는 화면 필터/선택값
 * - 응답 사용 위치: 응답 본문을 화면 상태와 조회 캐시에 반영
 * - 프론트 조회 키: 화면별 조회 키 정책에 따름
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: 준비 상태 정보 없음
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: 스테이징 기본 검증 대상
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: MASTER_DATA_WRITE 권한 필요
 * - 접근 범위: GLOBAL 범위 정책
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: NONE 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Frontend bulk API handoff implemented with max 100 and partial-success contract
 * @summary POST /api/admin/detailed-programs/bulk-delete
 */
const bulkDelete3 = (
    bulkIdsRequest: BulkIdsRequest,
 options?: SecondParameter<typeof customInstance<ApiResponseBulkActionResponse>>,) => {
      return customInstance<ApiResponseBulkActionResponse>(
      {url: `/api/admin/detailed-programs/bulk-delete`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: bulkIdsRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 관리자 조회
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 후원사/교재/마스터데이터 (`null`)
 * - 프론트 담당 영역: master-data (`master-data`)
 * - 호출 방식: `GET /api/admin/textbooks/{textbookId}`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 필터/페이지네이션/선택 행에서 요청값 전달
 * - 응답 사용 위치: 조회 캐시 및 화면 목록·상세 상태 갱신
 * - 프론트 조회 키: `get_admin_textbooks_textbookId`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: STAGING_VERIFY_REQUIRED
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: DATA_MANAGEMENT_QA_SIGNOFF
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 master-data API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 조회 API는 응답 원본 status/code 값을 화면 배지 라벨과 분리해서 보관합니다. 라벨은 프론트 표시용, 원본 값은 후속 API 호출 조건으로 사용합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary 관리자 조회
 */
const textbook = (
    textbookId: string,
 options?: SecondParameter<typeof customInstance<TextbookResponse>>,) => {
      return customInstance<TextbookResponse>(
      {url: `/api/admin/textbooks/${textbookId}`, method: 'GET'
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 관리자 삭제
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 후원사/교재/마스터데이터 (`null`)
 * - 프론트 담당 영역: master-data (`master-data`)
 * - 호출 방식: `DELETE /api/admin/textbooks/{textbookId}`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 폼 상태 or 선택 행 action payload
 * - 응답 사용 위치: 변경 결과 then 관련 조회 키 갱신
 * - 프론트 조회 키: `delete_admin_textbooks_textbookId`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: STAGING_VERIFY_REQUIRED
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: DATA_MANAGEMENT_QA_SIGNOFF
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 master-data API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary 관리자 삭제
 */
const _delete = (
    textbookId: string,
 options?: SecondParameter<typeof customInstance<ApiResponseTextbookResponse>>,) => {
      return customInstance<ApiResponseTextbookResponse>(
      {url: `/api/admin/textbooks/${textbookId}`, method: 'DELETE'
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 관리자 부분 수정
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 후원사/교재/마스터데이터 (`null`)
 * - 프론트 담당 영역: master-data (`master-data`)
 * - 호출 방식: `PATCH /api/admin/textbooks/{textbookId}`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 폼 상태 or 선택 행 action payload
 * - 응답 사용 위치: 변경 결과 then 관련 조회 키 갱신
 * - 프론트 조회 키: `patch_admin_textbooks_textbookId`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: STAGING_VERIFY_REQUIRED
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: DATA_MANAGEMENT_QA_SIGNOFF
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 master-data API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary 관리자 부분 수정
 */
const update = (
    textbookId: string,
    textbookRequest: TextbookRequest,
 options?: SecondParameter<typeof customInstance<ApiResponseTextbookResponse>>,) => {
      return customInstance<ApiResponseTextbookResponse>(
      {url: `/api/admin/textbooks/${textbookId}`, method: 'PATCH',
      headers: {'Content-Type': 'application/json', },
      data: textbookRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 관리자 조회
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 후원사/교재/마스터데이터 (`null`)
 * - 프론트 담당 영역: master-data (`master-data`)
 * - 호출 방식: `GET /api/admin/sponsors/{sponsorId}`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 필터/페이지네이션/선택 행에서 요청값 전달
 * - 응답 사용 위치: 조회 캐시 및 화면 목록·상세 상태 갱신
 * - 프론트 조회 키: `get_admin_sponsors_sponsorId`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: STAGING_VERIFY_REQUIRED
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: DATA_MANAGEMENT_QA_SIGNOFF
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 master-data API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 조회 API는 응답 원본 status/code 값을 화면 배지 라벨과 분리해서 보관합니다. 라벨은 프론트 표시용, 원본 값은 후속 API 호출 조건으로 사용합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary 관리자 조회
 */
const sponsor = (
    sponsorId: number,
 options?: SecondParameter<typeof customInstance<SponsorDetailResponse>>,) => {
      return customInstance<SponsorDetailResponse>(
      {url: `/api/admin/sponsors/${sponsorId}`, method: 'GET'
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 관리자 삭제
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 후원사/교재/마스터데이터 (`null`)
 * - 프론트 담당 영역: master-data (`master-data`)
 * - 호출 방식: `DELETE /api/admin/sponsors/{sponsorId}`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 폼 상태 or 선택 행 action payload
 * - 응답 사용 위치: 변경 결과 then 관련 조회 키 갱신
 * - 프론트 조회 키: `delete_admin_sponsors_sponsorId`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: STAGING_VERIFY_REQUIRED
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: DATA_MANAGEMENT_QA_SIGNOFF
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 master-data API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary 관리자 삭제
 */
const delete2 = (
    sponsorId: number,
 options?: SecondParameter<typeof customInstance<ApiResponseSponsorDeleteResponse>>,) => {
      return customInstance<ApiResponseSponsorDeleteResponse>(
      {url: `/api/admin/sponsors/${sponsorId}`, method: 'DELETE'
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 관리자 부분 수정
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 후원사/교재/마스터데이터 (`null`)
 * - 프론트 담당 영역: master-data (`master-data`)
 * - 호출 방식: `PATCH /api/admin/sponsors/{sponsorId}`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 폼 상태 or 선택 행 action payload
 * - 응답 사용 위치: 변경 결과 then 관련 조회 키 갱신
 * - 프론트 조회 키: `patch_admin_sponsors_sponsorId`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: STAGING_VERIFY_REQUIRED
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: DATA_MANAGEMENT_QA_SIGNOFF
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 master-data API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary 관리자 부분 수정
 */
const update3 = (
    sponsorId: number,
    sponsorRequest: SponsorRequest,
 options?: SecondParameter<typeof customInstance<ApiResponseSponsorResponse>>,) => {
      return customInstance<ApiResponseSponsorResponse>(
      {url: `/api/admin/sponsors/${sponsorId}`, method: 'PATCH',
      headers: {'Content-Type': 'application/json', },
      data: sponsorRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 관리자 삭제
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 후원사/교재/마스터데이터 (`null`)
 * - 프론트 담당 영역: master-data (`master-data`)
 * - 호출 방식: `DELETE /api/admin/sponsors/yearly-businesses/{yearlyBusinessId}`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 폼 상태 or 선택 행 action payload
 * - 응답 사용 위치: 변경 결과 then 관련 조회 키 갱신
 * - 프론트 조회 키: `delete_admin_sponsor-yearly-businesses_yearlyBusinessId`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: STAGING_VERIFY_REQUIRED
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: DATA_MANAGEMENT_QA_SIGNOFF
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 master-data API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary 관리자 삭제
 */
const deleteYearlyBusiness = (
    yearlyBusinessId: number,
 options?: SecondParameter<typeof customInstance<ApiResponseDeleteResponse>>,) => {
      return customInstance<ApiResponseDeleteResponse>(
      {url: `/api/admin/sponsors/yearly-businesses/${yearlyBusinessId}`, method: 'DELETE'
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 관리자 부분 수정
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 후원사/교재/마스터데이터 (`null`)
 * - 프론트 담당 영역: master-data (`master-data`)
 * - 호출 방식: `PATCH /api/admin/sponsors/yearly-businesses/{yearlyBusinessId}`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 폼 상태 or 선택 행 action payload
 * - 응답 사용 위치: 변경 결과 then 관련 조회 키 갱신
 * - 프론트 조회 키: `patch_admin_sponsor-yearly-businesses_yearlyBusinessId`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: STAGING_VERIFY_REQUIRED
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: DATA_MANAGEMENT_QA_SIGNOFF
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 master-data API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary 관리자 부분 수정
 */
const updateYearlyBusiness = (
    yearlyBusinessId: number,
    sponsorYearlyBusinessRequest: SponsorYearlyBusinessRequest,
 options?: SecondParameter<typeof customInstance<ApiResponseSponsorYearlyBusinessResponse>>,) => {
      return customInstance<ApiResponseSponsorYearlyBusinessResponse>(
      {url: `/api/admin/sponsors/yearly-businesses/${yearlyBusinessId}`, method: 'PATCH',
      headers: {'Content-Type': 'application/json', },
      data: sponsorYearlyBusinessRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 관리자 삭제
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 후원사/교재/마스터데이터 (`null`)
 * - 프론트 담당 영역: master-data (`master-data`)
 * - 호출 방식: `DELETE /api/admin/sponsors/contacts/{contactId}`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 폼 상태 or 선택 행 action payload
 * - 응답 사용 위치: 변경 결과 then 관련 조회 키 갱신
 * - 프론트 조회 키: `delete_admin_sponsor-contacts_contactId`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: STAGING_VERIFY_REQUIRED
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: DATA_MANAGEMENT_QA_SIGNOFF
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 master-data API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary 관리자 삭제
 */
const deleteContact = (
    contactId: number,
 options?: SecondParameter<typeof customInstance<ApiResponseDeleteResponse>>,) => {
      return customInstance<ApiResponseDeleteResponse>(
      {url: `/api/admin/sponsors/contacts/${contactId}`, method: 'DELETE'
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 관리자 부분 수정
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 후원사/교재/마스터데이터 (`null`)
 * - 프론트 담당 영역: master-data (`master-data`)
 * - 호출 방식: `PATCH /api/admin/sponsors/contacts/{contactId}`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 폼 상태 or 선택 행 action payload
 * - 응답 사용 위치: 변경 결과 then 관련 조회 키 갱신
 * - 프론트 조회 키: `patch_admin_sponsor-contacts_contactId`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: STAGING_VERIFY_REQUIRED
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: DATA_MANAGEMENT_QA_SIGNOFF
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 master-data API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary 관리자 부분 수정
 */
const updateContact = (
    contactId: number,
    sponsorContactRequest: SponsorContactRequest,
 options?: SecondParameter<typeof customInstance<ApiResponseSponsorContactResponse>>,) => {
      return customInstance<ApiResponseSponsorContactResponse>(
      {url: `/api/admin/sponsors/contacts/${contactId}`, method: 'PATCH',
      headers: {'Content-Type': 'application/json', },
      data: sponsorContactRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 관리자 조회
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 후원사/교재/마스터데이터 (`null`)
 * - 프론트 담당 영역: master-data (`master-data`)
 * - 호출 방식: `GET /api/admin/material-kits/{kitId}`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 필터/페이지네이션/선택 행에서 요청값 전달
 * - 응답 사용 위치: 조회 캐시 및 화면 목록·상세 상태 갱신
 * - 프론트 조회 키: `get_admin_material-kits_kitId`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: STAGING_VERIFY_REQUIRED
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: DATA_MANAGEMENT_QA_SIGNOFF
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 master-data API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 조회 API는 응답 원본 status/code 값을 화면 배지 라벨과 분리해서 보관합니다. 라벨은 프론트 표시용, 원본 값은 후속 API 호출 조건으로 사용합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary 관리자 조회
 */
const kit = (
    kitId: number,
 options?: SecondParameter<typeof customInstance<MaterialKitResponse>>,) => {
      return customInstance<MaterialKitResponse>(
      {url: `/api/admin/material-kits/${kitId}`, method: 'GET'
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 관리자 삭제
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 후원사/교재/마스터데이터 (`null`)
 * - 프론트 담당 영역: master-data-material-kits (`master-data-material-kits`)
 * - 호출 방식: `DELETE /api/admin/material-kits/{kitId}`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 교재 관리 > 키트 수량 관리 모달에서 선택한 kitId
 * - 응답 사용 위치: 삭제/비활성화 성공 시 키트 목록 재조회, 참조 중 409 발생 시 삭제 불가 모달 표시
 * - 프론트 조회 키: `delete_admin_material_kits_kitId`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: STAGING_VERIFY_REQUIRED
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: DATA_MANAGEMENT_QA_SIGNOFF
 * - 목데이터 대체: 교재 키트 수량 관리 모달의 임시 삭제 상태를 키트 비활성화 API 결과로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary 관리자 삭제
 */
const delete3 = (
    kitId: number,
 options?: SecondParameter<typeof customInstance<ApiResponseMaterialKitResponse>>,) => {
      return customInstance<ApiResponseMaterialKitResponse>(
      {url: `/api/admin/material-kits/${kitId}`, method: 'DELETE'
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 관리자 부분 수정
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 후원사/교재/마스터데이터 (`null`)
 * - 프론트 담당 영역: master-data (`master-data`)
 * - 호출 방식: `PATCH /api/admin/material-kits/{kitId}`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 폼 상태 or 선택 행 action payload
 * - 응답 사용 위치: 변경 결과 then 관련 조회 키 갱신
 * - 프론트 조회 키: `patch_admin_material-kits_kitId`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: STAGING_VERIFY_REQUIRED
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: DATA_MANAGEMENT_QA_SIGNOFF
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 master-data API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary 관리자 부분 수정
 */
const update4 = (
    kitId: number,
    materialKitRequest: MaterialKitRequest,
 options?: SecondParameter<typeof customInstance<ApiResponseMaterialKitResponse>>,) => {
      return customInstance<ApiResponseMaterialKitResponse>(
      {url: `/api/admin/material-kits/${kitId}`, method: 'PATCH',
      headers: {'Content-Type': 'application/json', },
      data: materialKitRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 프로그램 조회
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 후원사/교재/마스터데이터 (`null`)
 * - 프론트 담당 영역: master-data (`master-data`)
 * - 호출 방식: `GET /api/admin/detailed-programs/{detailedProgramId}`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 필터/페이지네이션/선택 행에서 요청값 전달
 * - 응답 사용 위치: 조회 캐시 및 화면 목록·상세 상태 갱신
 * - 프론트 조회 키: `get_admin_detailed-programs_detailedProgramId`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: STAGING_VERIFY_REQUIRED
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: DATA_MANAGEMENT_QA_SIGNOFF
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 master-data API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 조회 API는 응답 원본 status/code 값을 화면 배지 라벨과 분리해서 보관합니다. 라벨은 프론트 표시용, 원본 값은 후속 API 호출 조건으로 사용합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary 프로그램 조회
 */
const detailedProgram = (
    detailedProgramId: number,
 options?: SecondParameter<typeof customInstance<DetailedProgramResponse>>,) => {
      return customInstance<DetailedProgramResponse>(
      {url: `/api/admin/detailed-programs/${detailedProgramId}`, method: 'GET'
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 프로그램 삭제
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 후원사/교재/마스터데이터 (`null`)
 * - 프론트 담당 영역: master-data (`master-data`)
 * - 호출 방식: `DELETE /api/admin/detailed-programs/{detailedProgramId}`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 폼 상태 or 선택 행 action payload
 * - 응답 사용 위치: 변경 결과 then 관련 조회 키 갱신
 * - 프론트 조회 키: `delete_admin_detailed-programs_detailedProgramId`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: STAGING_VERIFY_REQUIRED
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: DATA_MANAGEMENT_QA_SIGNOFF
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 master-data API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary 프로그램 삭제
 */
const delete4 = (
    detailedProgramId: number,
 options?: SecondParameter<typeof customInstance<ApiResponseDetailedProgramDeleteResponse>>,) => {
      return customInstance<ApiResponseDetailedProgramDeleteResponse>(
      {url: `/api/admin/detailed-programs/${detailedProgramId}`, method: 'DELETE'
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 프로그램 부분 수정
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 후원사/교재/마스터데이터 (`null`)
 * - 프론트 담당 영역: master-data (`master-data`)
 * - 호출 방식: `PATCH /api/admin/detailed-programs/{detailedProgramId}`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 폼 상태 or 선택 행 action payload
 * - 응답 사용 위치: 변경 결과 then 관련 조회 키 갱신
 * - 프론트 조회 키: `patch_admin_detailed-programs_detailedProgramId`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: STAGING_VERIFY_REQUIRED
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: DATA_MANAGEMENT_QA_SIGNOFF
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 master-data API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 변경 API는 성공 후 관련 목록/상세를 반드시 재조회합니다. 상태 충돌 또는 중복 요청은 409로 처리합니다.
 * ### Swagger에서 확인할 때
 * - 요청 전 목록/상세를 먼저 조회하고, 변경 요청 후 동일 목록/상세를 재조회해 상태값과 이력 반영 여부를 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary 프로그램 부분 수정
 */
const update5 = (
    detailedProgramId: number,
    detailedProgramRequest: DetailedProgramRequest,
 options?: SecondParameter<typeof customInstance<ApiResponseDetailedProgramResponse>>,) => {
      return customInstance<ApiResponseDetailedProgramResponse>(
      {url: `/api/admin/detailed-programs/${detailedProgramId}`, method: 'PATCH',
      headers: {'Content-Type': 'application/json', },
      data: detailedProgramRequest
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 관리자 조회
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 후원사/교재/마스터데이터 (`null`)
 * - 프론트 담당 영역: master-data (`master-data`)
 * - 호출 방식: `GET /api/admin/textbooks/matches`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 필터/페이지네이션/선택 행에서 요청값 전달
 * - 응답 사용 위치: 조회 캐시 및 화면 목록·상세 상태 갱신
 * - 프론트 조회 키: `get_admin_textbooks_matching`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: STAGING_VERIFY_REQUIRED
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: DATA_MANAGEMENT_QA_SIGNOFF
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 master-data API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 조회 API는 응답 원본 status/code 값을 화면 배지 라벨과 분리해서 보관합니다. 라벨은 프론트 표시용, 원본 값은 후속 API 호출 조건으로 사용합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary 관리자 조회
 */
const matches = (
    params?: MatchesParams,
 options?: SecondParameter<typeof customInstance<TextbookMatchResponse[]>>,) => {
      return customInstance<TextbookMatchResponse[]>(
      {url: `/api/admin/textbooks/matches`, method: 'GET',
        params
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 프로그램 조회
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 후원사/교재/마스터데이터 (`null`)
 * - 프론트 담당 영역: master-data-sponsors (`master-data-sponsors`)
 * - 호출 방식: `GET /api/admin/sponsors/{sponsorId}/program-histories`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 후원사 상세 선택 sponsorId와 프로그램명/년도/진행현황/교육대상/담당자 필터
 * - 응답 사용 위치: 후원사 상세 프로그램 진행 이력 테이블과 필터 결과, 페이지 상태를 갱신
 * - 프론트 조회 키: `get_sponsors_sponsorId_program_histories`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: STAGING_VERIFY_REQUIRED
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: DATA_MANAGEMENT_QA_SIGNOFF
 * - 목데이터 대체: 후원사 상세 프로그램 진행 이력의 클라이언트 전체조회/필터 상태를 서버 페이지·필터 응답으로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 조회 API는 응답 원본 status/code 값을 화면 배지 라벨과 분리해서 보관합니다. 라벨은 프론트 표시용, 원본 값은 후속 API 호출 조건으로 사용합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary 프로그램 조회
 */
const programHistories = (
    sponsorId: number,
    params?: ProgramHistoriesParams,
 options?: SecondParameter<typeof customInstance<PageResponseSponsorProgramHistoryResponse>>,) => {
      return customInstance<PageResponseSponsorProgramHistoryResponse>(
      {url: `/api/admin/sponsors/${sponsorId}/program-histories`, method: 'GET',
        params
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 관리자 조회
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 후원사/교재/마스터데이터 (`null`)
 * - 프론트 담당 영역: master-data (`master-data`)
 * - 호출 방식: `GET /api/admin/material-kits/{kitId}/versions/current/calculate`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 필터/페이지네이션/선택 행에서 요청값 전달
 * - 응답 사용 위치: 조회 캐시 및 화면 목록·상세 상태 갱신
 * - 프론트 조회 키: `get_admin_material-kits_kitId_versions_current_calculate`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: STAGING_VERIFY_REQUIRED
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: DATA_MANAGEMENT_QA_SIGNOFF
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 master-data API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 조회 API는 응답 원본 status/code 값을 화면 배지 라벨과 분리해서 보관합니다. 라벨은 프론트 표시용, 원본 값은 후속 API 호출 조건으로 사용합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary 관리자 조회
 */
const currentKitCalculation = (
    kitId: number,
    params: CurrentKitCalculationParams,
 options?: SecondParameter<typeof customInstance<MaterialKitCalculationResponse>>,) => {
      return customInstance<MaterialKitCalculationResponse>(
      {url: `/api/admin/material-kits/${kitId}/versions/current/calculate`, method: 'GET',
        params
    },
      options);
    }

/**
 * ### 이 API가 하는 일
 * - 관리자 조회
 * - API 분류: 내부 처리 또는 보조 API
 * - 사용하는 화면: 후원사/교재/마스터데이터 (`null`)
 * - 프론트 담당 영역: master-data (`master-data`)
 * - 호출 방식: `GET /api/admin/material-kits/{kitId}/calculate`
 *
 * ### 화면/프론트 사용 기준
 * - 요청값 출처: 필터/페이지네이션/선택 행에서 요청값 전달
 * - 응답 사용 위치: 조회 캐시 및 화면 목록·상세 상태 갱신
 * - 프론트 조회 키: `get_admin_material-kits_kitId_calculate`
 * - 구현 상태: 구현 완료
 * - 로컬/스테이징 준비도: STAGING_VERIFY_REQUIRED
 * - 외부 연동 확인: 외부 연동 대기 없음
 * - 스테이징 점검 기준: DATA_MANAGEMENT_QA_SIGNOFF
 * - 목데이터 대체: 임시 목데이터/localStorage 상태를 master-data API 상태/캐시로 대체합니다.
 * - 화면에서는 이 API 응답을 기준으로 목록, 상세, 상태 배지, 버튼 노출 여부를 갱신합니다.
 *
 * ### 권한/보안
 * - 호출 가능 계정: 관리자 계정
 * - 필요 권한: 별도 세부 권한 없음
 * - 접근 범위: 별도 접근 범위 제한 없음
 * - 인증 API가 아니라면 Swagger 우측 상단 Authorize에 관리자 또는 회원 Bearer 토큰을 입력한 뒤 호출합니다.
 *
 * ### 개인정보/감사 정책
 * - 개인정보 노출 기준: UNKNOWN 개인정보 정책
 * - 감사로그 저장: 필수
 * - 개인정보 원문 조회, 민감파일 다운로드, export 계열 요청은 감사로그 저장에 실패하면 요청도 차단됩니다.
 *
 * ### 상태값/화면 배지 기준
 * - 조회 API는 응답 원본 status/code 값을 화면 배지 라벨과 분리해서 보관합니다. 라벨은 프론트 표시용, 원본 값은 후속 API 호출 조건으로 사용합니다.
 * ### Swagger에서 확인할 때
 * - 목록 조회는 page/size/status/date/search 필터를 바꿔가며 응답이 화면 필터와 일치하는지 확인합니다.
 * - 로컬 더미 데이터는 `local` profile에서만 사용합니다. 운영/스테이징 데이터와 혼동하지 않습니다.
 * - 인증이 필요한 API는 먼저 로그인/MFA API로 토큰을 받은 뒤 Authorize에 입력합니다.
 *
 * ### 프론트 구현 참고
 * - 성공 응답은 화면 상태 또는 조회 캐시에 반영하고, 실패 응답은 error.code 기준으로 알림/팝업을 분기합니다.
 * - 목록 API는 페이지/필터/검색어/정렬 조건을 조회 키에 포함해 캐시 충돌을 피합니다.
 * - 생성/수정/삭제 API 성공 후에는 관련 목록과 상세 조회를 다시 불러옵니다.
 * - 날짜, 금액, 상태 배지는 백엔드 원본 값과 화면 정의서의 라벨 매핑을 기준으로 표시합니다.
 * - 검토 메모: Auto-synced from implemented controller route
 * @summary 관리자 조회
 */
const calculate = (
    kitId: number,
    params: CalculateParams,
 options?: SecondParameter<typeof customInstance<MaterialKitCalculationResponse>>,) => {
      return customInstance<MaterialKitCalculationResponse>(
      {url: `/api/admin/material-kits/${kitId}/calculate`, method: 'GET',
        params
    },
      options);
    }

return {textbooks,create1,bulkDelete,sponsors,create4,yearlyBusinesses,addYearlyBusiness,end,contacts,addContact,bulkDeleteContacts,bulkDeleteSponsors,kits,create6,versions,createVersion,addTargetCount,detailedPrograms,create7,bulkDelete3,textbook,_delete,update,sponsor,delete2,update3,deleteYearlyBusiness,updateYearlyBusiness,deleteContact,updateContact,kit,delete3,update4,detailedProgram,delete4,update5,matches,programHistories,currentKitCalculation,calculate}};
export type TextbooksResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIDataManagementSubset>['textbooks']>>>
export type Create1Result = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIDataManagementSubset>['create1']>>>
export type BulkDeleteResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIDataManagementSubset>['bulkDelete']>>>
export type SponsorsResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIDataManagementSubset>['sponsors']>>>
export type Create4Result = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIDataManagementSubset>['create4']>>>
export type YearlyBusinessesResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIDataManagementSubset>['yearlyBusinesses']>>>
export type AddYearlyBusinessResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIDataManagementSubset>['addYearlyBusiness']>>>
export type EndResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIDataManagementSubset>['end']>>>
export type ContactsResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIDataManagementSubset>['contacts']>>>
export type AddContactResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIDataManagementSubset>['addContact']>>>
export type BulkDeleteContactsResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIDataManagementSubset>['bulkDeleteContacts']>>>
export type BulkDeleteSponsorsResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIDataManagementSubset>['bulkDeleteSponsors']>>>
export type KitsResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIDataManagementSubset>['kits']>>>
export type Create6Result = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIDataManagementSubset>['create6']>>>
export type VersionsResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIDataManagementSubset>['versions']>>>
export type CreateVersionResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIDataManagementSubset>['createVersion']>>>
export type AddTargetCountResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIDataManagementSubset>['addTargetCount']>>>
export type DetailedProgramsResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIDataManagementSubset>['detailedPrograms']>>>
export type Create7Result = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIDataManagementSubset>['create7']>>>
export type BulkDelete3Result = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIDataManagementSubset>['bulkDelete3']>>>
export type TextbookResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIDataManagementSubset>['textbook']>>>
export type _DeleteResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIDataManagementSubset>['_delete']>>>
export type UpdateResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIDataManagementSubset>['update']>>>
export type SponsorResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIDataManagementSubset>['sponsor']>>>
export type Delete2Result = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIDataManagementSubset>['delete2']>>>
export type Update3Result = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIDataManagementSubset>['update3']>>>
export type DeleteYearlyBusinessResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIDataManagementSubset>['deleteYearlyBusiness']>>>
export type UpdateYearlyBusinessResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIDataManagementSubset>['updateYearlyBusiness']>>>
export type DeleteContactResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIDataManagementSubset>['deleteContact']>>>
export type UpdateContactResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIDataManagementSubset>['updateContact']>>>
export type KitResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIDataManagementSubset>['kit']>>>
export type Delete3Result = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIDataManagementSubset>['delete3']>>>
export type Update4Result = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIDataManagementSubset>['update4']>>>
export type DetailedProgramResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIDataManagementSubset>['detailedProgram']>>>
export type Delete4Result = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIDataManagementSubset>['delete4']>>>
export type Update5Result = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIDataManagementSubset>['update5']>>>
export type MatchesResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIDataManagementSubset>['matches']>>>
export type ProgramHistoriesResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIDataManagementSubset>['programHistories']>>>
export type CurrentKitCalculationResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIDataManagementSubset>['currentKitCalculation']>>>
export type CalculateResult = NonNullable<Awaited<ReturnType<ReturnType<typeof getJAKoreaCMSBackendAPIDataManagementSubset>['calculate']>>>
