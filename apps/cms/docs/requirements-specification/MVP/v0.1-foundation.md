# MVP v0.1 - Foundation (인증/역할/기본 구조)

> 요구사항 참조: §1 시스템 구성, §2 역할 및 권한, FR-B01, FR-B02

---

## 목표
- 서비스 전반의 **역할 체계/상태값/핵심 데이터 모델**을 요구사항에 맞게 구현
- 로그인/회원가입 흐름을 요구사항 §2에 맞게 정렬

---

## Phase 0.1.1 - 역할/권한 체계 재정의

### 요구사항 매핑
| 요구사항 | 구현 |
|---------|------|
| §2.1 프론트 사용자 역할 | `INDIVIDUAL` / `SCHOOL` / `INSTRUCTOR` |
| §2.2 관리자 권한 정의 | `MASTER` / `ADMIN` / `GENERAL` |
| §백오피스 관리자 역할 | `OWNER(담당자)` / `PARTNER(파트너)` / `ASSISTANT(보조)` |

### 구현 항목

#### 1) 타입 정의 (`src/types/user.ts`)
```typescript
// 프론트 사용자 역할 (§2.1)
export type UserRole = 'INDIVIDUAL' | 'SCHOOL' | 'INSTRUCTOR' | 'ADMIN'

// 관리자 권한 레벨 (§2.2)
export type AdminLevel = 'MASTER' | 'ADMIN' | 'GENERAL'

// 프로그램 단위 역할 (§백오피스 권한 구조)
export type ProgramRole = 'OWNER' | 'PARTNER' | 'ASSISTANT' | null

// 사용자 인터페이스
export interface User {
  id: string
  email: string
  name: string
  phone: string
  role: UserRole

  // 관리자 전용
  adminLevel?: AdminLevel

  // 프로그램별 역할 (관리자용)
  programRoles?: Record<string, ProgramRole>

  // 개인(참여자) 전용 (§2.1)
  // - 프로그램 신청(개인)
  // - 신청내역/진행상황 확인
  // - 일정 확인, 과제 제출
  // - 수료증/활동 확인서 발급

  // 학교 전용 (§2.1)
  schoolInfo?: {
    schoolName: string
    address: string
    // 학생명단 업로드
    // 학교단위 수료증 다운로드
    // 강사 대기실, 급식 가능 여부 등
  }

  // 강사 전용 (§2.1)
  instructorInfo?: {
    bankName: string
    accountNumber: string
    accountHolder: string
    isBusinessIncome: boolean // 사업소득자 여부 (3.3% vs 8.8%)
    // 강의 신청, 매칭/일정 확인
    // 강의보고서 제출
    // 강사비/교통비 산출내역 확인
  }

  createdAt: string
  updatedAt: string
}
```

#### 2) 권한 정책 정의 (`src/shared/config/permissions.ts`)
```typescript
// §2.2 관리자 권한 정의
export const ADMIN_PERMISSIONS = {
  MASTER: {
    label: '마스터 관리자',
    description: '회원/권한 총괄 관리, 시스템 설정, 모든 기능 접근',
    canManageUsers: true,
    canManageSystemSettings: true,
    canAccessAllPrograms: true,
    canDeleteUsers: true,
    canApprovePermissionRequests: true,
  },
  ADMIN: {
    label: '관리자',
    description: '프로그램 운영 전반, 신청 승인/반려, 강사 매칭, 정산',
    canManageUsers: false,
    canManageSystemSettings: false,
    canAccessAllPrograms: false, // 담당 프로그램만
    canDeleteUsers: false,
    canApprovePermissionRequests: false,
  },
  GENERAL: {
    label: '일반',
    description: '조회 기능만 (기본값, 권한부여 0)',
    canManageUsers: false,
    canManageSystemSettings: false,
    canAccessAllPrograms: false,
    canDeleteUsers: false,
    canApprovePermissionRequests: false,
  },
}

// §백오피스 프로그램 역할 정의
export const PROGRAM_ROLE_PERMISSIONS = {
  OWNER: {
    label: '담당자',
    description: '해당 사업 총괄 담당자',
    canCreate: true,
    canUpload: true,
    canDownload: true,
    canDelete: true,
    canApprove: true,
    // 프로그램 생성 시 자동 부여
  },
  PARTNER: {
    label: '파트너',
    description: '총괄사업 담당자와 공동 진행 내부 직원',
    canCreate: true,
    canUpload: true,
    canDownload: false, // 다운로드 기능 제한
    canDelete: false,
    canApprove: true,
  },
  ASSISTANT: {
    label: '보조',
    description: '뷰어 기능만 (업로드/다운로드/삭제 제외)',
    canCreate: false,
    canUpload: false,
    canDownload: false,
    canDelete: false,
    canApprove: false,
  },
}
```

### Hook
- `usePermissions()` : 현재 사용자 권한 체크
- `useProgramRole(programId)` : 특정 프로그램에 대한 역할 체크

### 수용 기준
- [ ] 기존 `STUDENT` → `INDIVIDUAL` 마이그레이션
- [ ] 기존 `VOLUNTEER` 역할 제거 (개인으로 통합)
- [ ] `AdminLevel` 필드 추가
- [ ] `ProgramRole` 구조 추가

---

## Phase 0.1.2 - 회원가입 흐름 (FR-B01, FR-B02)

### 요구사항 매핑
| ID | 요구사항 | 우선순위 |
|----|---------|---------|
| FR-B01 | 가입 유형 선택: 개인 / 학교 / 강사 | P1 |
| FR-B02 | 약관/개인정보 동의 + 마케팅 동의 | P1 |

### 구현 항목

#### 1) 회원가입 페이지 (`src/pages/auth/register-page.tsx`)

**Step 1: 역할 선택**
```typescript
// 가입 유형 선택 UI
const REGISTER_TYPES = [
  { value: 'INDIVIDUAL', label: '개인(참여자)', description: '프로그램 신청, 수료증 발급' },
  { value: 'SCHOOL', label: '학교', description: '학교 단위 프로그램 신청' },
  { value: 'INSTRUCTOR', label: '강사', description: '강의 신청, 정산 관리' },
]
```

**Step 2: 약관 동의 (FR-B02)**
```typescript
interface ConsentForm {
  termsOfService: boolean      // 이용약관 동의 (필수)
  privacyPolicy: boolean       // 개인정보 수집 이용 동의 (필수)
  marketingConsent: boolean    // 뉴스레터/홍보 동의 (선택)
}
```

**Step 3: 정보 입력 (역할별 분기)**
```typescript
// 공통
interface BaseRegisterForm {
  email: string
  password: string
  name: string
  phone: string
}

// 학교 추가 필드 (§2.1)
interface SchoolRegisterForm extends BaseRegisterForm {
  schoolName: string
  schoolAddress: string
  position: string // 담당자 직책
}

// 강사 추가 필드 (§2.1)
interface InstructorRegisterForm extends BaseRegisterForm {
  bankName: string
  accountNumber: string
  accountHolder: string
  isBusinessIncome: boolean
}
```

#### 2) 동의 내역 저장
```typescript
interface UserConsent {
  userId: string
  termsVersion: string
  privacyVersion: string
  marketingConsent: boolean
  consentedAt: string
}
```

### Hook
- `useRegister()` : 회원가입 처리
- `useConsent()` : 동의 내역 관리

### 화면
- `/auth/register` : 회원가입 메인
- `/auth/register/individual` : 개인 가입 폼
- `/auth/register/school` : 학교 가입 폼
- `/auth/register/instructor` : 강사 가입 폼

### 수용 기준
- [ ] 가입 시 역할 선택 UI 제공
- [ ] 필수 동의 체크 없이 진행 불가
- [ ] 마케팅 동의 여부 DB 저장
- [ ] 역할별 추가 정보 입력 폼 분기

---

## Phase 0.1.3 - 로그인 흐름

### 요구사항 매핑
| 요구사항 | 구현 |
|---------|------|
| §1.1 로그인 ID 정책 | 이메일 기반 (휴대폰/간편인증은 추후) |
| §0.2 강사 로그인 | 휴대폰 본인인증 가능 (추후) |

### 구현 항목

#### 1) 로그인 페이지 개선
```typescript
// 현재: 역할 선택 후 로그인
// 변경: 이메일/비밀번호 로그인 → 역할 자동 판별

interface LoginForm {
  email: string
  password: string
}

// 로그인 성공 시 역할에 따라 리다이렉트
const getRedirectPath = (user: User) => {
  if (user.role === 'ADMIN') return '/admin'
  if (user.role === 'INSTRUCTOR') return '/instructor'
  if (user.role === 'SCHOOL') return '/school'
  return '/my' // 개인
}
```

### Hook
- `useAuth()` : 인증 상태 관리 (기존 개선)
- `useLogin()` : 로그인 처리

### 수용 기준
- [ ] 이메일/비밀번호 로그인
- [ ] 역할별 리다이렉트
- [ ] 로그인 실패 시 에러 메시지

---

## Phase 0.1.4 - 상태값/기본 모델 정의

### 요구사항 매핑
| 요구사항 | 구현 |
|---------|------|
| §3.3 상태값(진행상황) | 8단계 상태값 정의 |
| §3.1 학교 신청 프로세스 | 학교 신청 상태 추가 |

### 구현 항목

#### 1) 상태값 정의 (`src/shared/constants/status.ts`)
```typescript
// §3.3 상태값(진행상황) — 최소 요구
export const APPLICATION_STATUS = {
  RECEIVED: { label: '신청(접수)', order: 1, color: 'default' },
  MATCHING_IN_PROGRESS: { label: '매칭 진행중', order: 2, color: 'processing' },
  MATCHING_COMPLETED: { label: '매칭 완료', order: 3, color: 'success' },
  MATERIAL_PREPARING: { label: '교재 배송 준비중', order: 4, color: 'processing' },
  MATERIAL_SHIPPED: { label: '교재 발송 완료', order: 5, color: 'success' },
  IN_PROGRESS: { label: '교육 실시', order: 6, color: 'processing' },
  SURVEY_SUBMITTED: { label: '만족도 조사 제출', order: 7, color: 'success' },
  REPORT_SUBMITTED: { label: '강의보고서 제출', order: 8, color: 'success' },
} as const

// §3.1 학교 신청 프로세스 상세 상태
export const SCHOOL_APPLICATION_STATUS = {
  APPLIED: { label: '신청완료', order: 1 },
  MATCHING: { label: '매칭진행', order: 2 },
  MATCHED: { label: '매칭완료', order: 3 },
  MATERIAL_PREPARING: { label: '교재배송준비', order: 4 },
  MATERIAL_SHIPPED: { label: '교재발송완료', order: 5 },
  EDUCATION_IN_PROGRESS: { label: '교육실시', order: 6 },
  SURVEY_SUBMITTED: { label: '만족도제출', order: 7 },
  REPORT_SUBMITTED: { label: '강의보고서제출', order: 8 },
} as const

// 강사 인터뷰 상태 (신규 강사용)
export const INTERVIEW_STATUS = {
  NOT_REQUIRED: { label: '면접 불필요', color: 'default' },
  PENDING: { label: '면접 대기', color: 'warning' },
  SCHEDULED: { label: '면접 예정', color: 'processing' },
  COMPLETED: { label: '면접 완료', color: 'success' },
  APPROVED: { label: '승인', color: 'success' },
  REJECTED: { label: '불합격', color: 'error' },
} as const
```

#### 2) 프로그램 모델 (`src/types/program.ts`)
```typescript
// §FR-C02 프로그램 상세 정보
export interface Program {
  id: string
  name: string                  // 프로그램 명
  sponsorId: string             // 후원사
  period: {                     // 진행기간
    startDate: string
    endDate: string
  }
  applicationPeriod: {          // 신청접수 기간
    startDate: string
    endDate: string
  }
  description: string           // 세부내용
  curriculum: string            // 커리큘럼
  schedule: string              // 교육일정
  contact: {                    // 문의처
    email: string
    phone: string
  }
  posterImage?: string          // 포스터/키비주얼 이미지
  status: 'DRAFT' | 'OPEN' | 'CLOSED' | 'COMPLETED'
  createdBy: string             // 생성자 (담당자 자동 부여)
  createdAt: string
  updatedAt: string
}
```

#### 3) 신청서 모델 (`src/types/application.ts`)
```typescript
// §FR-C03 신청서 작성: 템플릿 기반 + 커스터마이징
export interface Application {
  id: string
  programId: string
  applicantId: string
  applicantType: 'INDIVIDUAL' | 'SCHOOL' | 'INSTRUCTOR'
  status: keyof typeof APPLICATION_STATUS

  // 학교 신청 시 (§3.1)
  schoolApplication?: {
    schoolName: string
    address: string
    targetGrade: string         // 대상 학년
    classCount: number          // 학급 수
    studentsPerClass: number    // 학급별 인원
    instructorWaitingRoom: string // 강사 대기장소
    studentListFile?: string    // 학생명단 엑셀 파일

    // 신청 이후 공유 정보 (§2.1)
    mealAvailable?: boolean     // 급식 가능 여부
    canEatInWaitingRoom?: boolean // 대기실 내 취식 가능 여부
  }

  // 강사 신청 시
  instructorApplication?: {
    preferredSchedule: string[]
    experience: string
  }

  // 개인 신청 시
  individualApplication?: {
    motivation: string
  }

  // 커스텀 필드 (템플릿 기반)
  customFields?: Record<string, unknown>

  createdAt: string
  updatedAt: string
}
```

### Hook
- `useStatusLabel(status)` : 상태 라벨/색상 반환
- `useStatusTimeline(applicationId)` : 상태 타임라인 데이터

### 수용 기준
- [ ] 8단계 상태값이 UI에 일관되게 표시
- [ ] 상태 변경 시 타임라인 업데이트
- [ ] 프로그램별 일부 단계 비활성화 가능

---

## Phase 0.1.5 - 네비게이션/라우팅 구조

### 요구사항 매핑
| 영역 | 메뉴 구성 |
|------|----------|
| 사용자 (개인) | 홈, 프로그램, 신청내역, 공지사항, 일정, 과제, 수료증 |
| 사용자 (학교) | 홈, 프로그램, 신청내역, 공지사항, 커뮤니티, 만족도설문, 수료증 |
| 사용자 (강사) | 홈, 공지사항, 교육일정, 커뮤니티, 강의보고서, 강사비신청, 경력증명서 |
| 관리자 | §백오피스 메뉴 전체 |

### 구현 항목

#### 1) 라우팅 구조
```typescript
// 개인(참여자) 라우트
const individualRoutes = [
  { path: '/my', element: <IndividualDashboard /> },
  { path: '/my/programs', element: <ProgramList /> },
  { path: '/my/applications', element: <MyApplications /> },
  { path: '/my/notices', element: <NoticeList /> },
  { path: '/my/schedule', element: <MySchedule /> },
  { path: '/my/assignments', element: <MyAssignments /> },
  { path: '/my/certificates', element: <MyCertificates /> },
]

// 학교 라우트
const schoolRoutes = [
  { path: '/school', element: <SchoolDashboard /> },
  { path: '/school/programs', element: <ProgramList /> },
  { path: '/school/applications', element: <SchoolApplications /> },
  { path: '/school/notices', element: <NoticeList /> },
  { path: '/school/community', element: <Community /> },
  { path: '/school/survey', element: <SatisfactionSurvey /> },
  { path: '/school/certificates', element: <SchoolCertificates /> },
]

// 강사 라우트 (§E 강사 마이페이지)
const instructorRoutes = [
  { path: '/instructor', element: <InstructorDashboard /> },
  { path: '/instructor/notices', element: <NoticeList /> },
  { path: '/instructor/schedule', element: <InstructorSchedule /> },
  { path: '/instructor/community', element: <Community /> },
  { path: '/instructor/reports', element: <LectureReports /> },
  { path: '/instructor/settlement', element: <SettlementRequest /> },
  { path: '/instructor/career', element: <CareerCertificate /> },
]

// 관리자 라우트 (기존 유지 + 권한 체크 강화)
const adminRoutes = [
  // ... 기존 관리자 라우트
]
```

#### 2) 메뉴 설정 (`src/shared/config/menu-config.ts`)
```typescript
// 역할별 메뉴 설정
export const MENU_CONFIG = {
  INDIVIDUAL: [
    { key: 'home', label: '홈', path: '/my', icon: HomeOutlined },
    { key: 'programs', label: '프로그램', path: '/my/programs', icon: AppstoreOutlined },
    { key: 'applications', label: '신청내역', path: '/my/applications', icon: FileTextOutlined },
    { key: 'notices', label: '공지사항', path: '/my/notices', icon: BellOutlined },
    { key: 'schedule', label: '일정', path: '/my/schedule', icon: CalendarOutlined },
    { key: 'assignments', label: '과제', path: '/my/assignments', icon: EditOutlined },
    { key: 'certificates', label: '수료증', path: '/my/certificates', icon: SafetyCertificateOutlined },
  ],
  SCHOOL: [
    { key: 'home', label: '홈', path: '/school', icon: HomeOutlined },
    { key: 'programs', label: '프로그램', path: '/school/programs', icon: AppstoreOutlined },
    { key: 'applications', label: '신청내역', path: '/school/applications', icon: FileTextOutlined },
    { key: 'notices', label: '공지사항', path: '/school/notices', icon: BellOutlined },
    { key: 'community', label: '커뮤니티', path: '/school/community', icon: TeamOutlined },
    { key: 'survey', label: '만족도설문', path: '/school/survey', icon: FormOutlined },
    { key: 'certificates', label: '수료증', path: '/school/certificates', icon: SafetyCertificateOutlined },
  ],
  INSTRUCTOR: [
    { key: 'home', label: '홈', path: '/instructor', icon: HomeOutlined },
    { key: 'notices', label: '공지사항', path: '/instructor/notices', icon: BellOutlined },
    { key: 'schedule', label: '교육일정', path: '/instructor/schedule', icon: CalendarOutlined },
    { key: 'community', label: '커뮤니티', path: '/instructor/community', icon: TeamOutlined },
    { key: 'reports', label: '강의보고서', path: '/instructor/reports', icon: FileTextOutlined },
    { key: 'settlement', label: '강사비신청', path: '/instructor/settlement', icon: DollarOutlined },
    { key: 'career', label: '경력증명서', path: '/instructor/career', icon: SafetyCertificateOutlined },
  ],
}
```

### Hook
- `useMenuByRole()` : 역할별 메뉴 반환
- `useRouteGuard()` : 라우트 접근 권한 체크

### 수용 기준
- [ ] 역할별 메뉴 분리 완료
- [ ] 권한 없는 라우트 접근 시 리다이렉트
- [ ] 관리자 프로그램 역할별 메뉴 필터링

---

## Mock 데이터 구조

### 파일 구조
```
src/data/mock/
├── users.ts          # 역할별 테스트 사용자
├── programs.ts       # 프로그램 목록
├── applications.ts   # 신청 내역
├── consents.ts       # 동의 내역
└── permissions.ts    # 권한 설정
```

### 테스트 계정
```typescript
export const MOCK_USERS = [
  // 관리자
  { email: 'master@jakorea.or.kr', role: 'ADMIN', adminLevel: 'MASTER' },
  { email: 'admin@jakorea.or.kr', role: 'ADMIN', adminLevel: 'ADMIN' },
  { email: 'general@jakorea.or.kr', role: 'ADMIN', adminLevel: 'GENERAL' },

  // 프론트 사용자
  { email: 'individual@test.com', role: 'INDIVIDUAL' },
  { email: 'school@test.com', role: 'SCHOOL', schoolInfo: {...} },
  { email: 'instructor@test.com', role: 'INSTRUCTOR', instructorInfo: {...} },
]
```

---

## 완료 기준 체크리스트

- [ ] 역할 체계가 요구사항과 100% 일치
- [ ] 회원가입 시 역할 선택 + 동의 흐름 완료
- [ ] 상태값 8단계가 정확히 구현됨
- [ ] 역할별 메뉴/라우팅 분리 완료
- [ ] Mock 데이터가 새 역할 체계 반영
