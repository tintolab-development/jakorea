/**
 * Header foreground on the page surface behind it.
 * - default: dark text/icons on a light surface
 * - inverse: white text/icons on a dark surface (transparent bar, no extra header fill)
 */
export type HeaderTheme = 'default' | 'inverse'
